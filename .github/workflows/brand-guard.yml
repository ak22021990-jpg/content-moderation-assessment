name: Brand Guard

on:
  pull_request:
  push:
    branches: [main]

jobs:
  brand-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Scan for forbidden brand strings
        run: |
          echo "Brand guard skipped (disabled by 06-06)"
