---
phase: 00-foundations
plan: 03
type: execute
wave: 2
depends_on:
  - 00-01
files_modified:
  - .gitattributes
  - public/videos/placeholder.mp4
autonomous: false
requirements:
  - DEPLOY-04
  - DEPLOY-05
  - DEPLOY-06
  - DEPLOY-08
  - DEPLOY-09
  - CONTENT-04

must_haves:
  truths:
    - "actions/checkout@v6 with lfs: true retrieves placeholder.mp4 as a real MP4 binary (not an LFS pointer text file) — verified by the 'Verify LFS files resolved' CI step defined in Plan 02's deploy.yml"
    - ".gitattributes tracks *.mp4, *.webm, *.lottie, and public/videos/*.jpg via Git LFS"
    - "GitHub billing spending cap is confirmed $0 before the first LFS push"
  artifacts:
    - path: ".gitattributes"
      provides: "LFS tracking rules for binary asset types"
      contains: "*.mp4 filter=lfs"
    - path: "public/videos/placeholder.mp4"
      provides: "5-second ffmpeg-generated H.264 placeholder; verifies LFS end-to-end"
      min_lines: 1
  key_links:
    - from: ".gitattributes"
      to: "public/videos/placeholder.mp4"
      via: "*.mp4 LFS rule must be committed before placeholder.mp4 is staged"
    - from: ".github/workflows/deploy.yml"
      to: "public/videos/placeholder.mp4"
      via: "CI 'Verify LFS files resolved' step (defined in Plan 02) runs against this path after checkout"
---

<objective>
Set up Git LFS tracking and commit the seed placeholder video. Verify end-to-end that GitHub Actions retrieves the real MP4 binary (not a pointer file) when deploying.

Purpose: This proves the LFS infrastructure works before real assessment videos are added in Phase 2. A silent LFS pointer failure in CI would cause video 404s for all candidates — catching it here with a 5-second placeholder is far cheaper than discovering it with production content.
Output: .gitattributes with LFS rules, placeholder.mp4 in LFS, and a CI verification step added to deploy.yml that fails the build if LFS resolution is broken.
</objective>

<execution_context>
@C:\Users\anoop\OneDrive\Desktop\Disney\.planning\phases\00-foundations\00-RESEARCH.md
</execution_context>

<context>
@C:\Users\anoop\OneDrive\Desktop\Disney\.planning\ROADMAP.md
@C:\Users\anoop\OneDrive\Desktop\Disney\.planning\phases\00-foundations\00-CONTEXT.md

<interfaces>
<!-- Constants from decisions D-11 through D-14 -->

Seed video command (D-12 — use EXACTLY):
  ffmpeg -f lavfi -i color=c=black:size=1280x720:rate=30 -t 5 -c:v libx264 -pix_fmt yuv420p -an public/videos/placeholder.mp4

Output file path: public/videos/placeholder.mp4
(The public/videos/ directory must be created first; the .gitkeep from Plan 02 should already exist)

.gitattributes LFS patterns (DEPLOY-06):
  *.mp4 filter=lfs diff=lfs merge=lfs -text
  *.webm filter=lfs diff=lfs merge=lfs -text
  *.lottie filter=lfs diff=lfs merge=lfs -text
  public/videos/*.jpg filter=lfs diff=lfs merge=lfs -text
  (Note: *.jpg is SCOPED to public/videos/ only — not global — to avoid LFS-tracking small UI images. OQ-5 resolution.)

CI verification step (D-14 — insert into build job in deploy.yml after "npm run build"):
  name: Verify LFS files resolved
  run: file public/videos/placeholder.mp4 | grep -i "ISO Media\|mp4"

LFS pointer file signature (for detection): starts with "version https://git-lfs.github.com/spec/v1"
Real MP4 file signature: "file" command returns "ISO Media, MP4 Base Media" or similar containing "ISO Media" or "mp4"
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Verify spending cap is $0 before LFS push</name>
  <what-built>Nothing yet — this is a pre-flight check required by D-17 before any LFS content is committed.</what-built>
  <how-to-verify>
    1. Open https://github.com/settings/billing/spending_limits
    2. Confirm "Git LFS Data" spending limit is set to $0
       (If it shows "Unlimited" or any dollar amount other than $0, set it to $0 now.)
    3. Optionally check current LFS usage: https://github.com/settings/billing
       Should show 0 GB used (fresh repo).

    CRITICAL: If the spending cap is NOT $0 and LFS quota is exceeded, GitHub disables LFS for the ENTIRE account (all repos) until the next billing cycle — not just this repo. This was confirmed in PITFALLS.md Pitfall 2 and RESEARCH.md Pitfall P6.
  </how-to-verify>
  <resume-signal>Type "spending cap confirmed" when GitHub billing shows LFS spending limit = $0.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Initialize Git LFS, commit .gitattributes, generate and commit placeholder video</name>
  <files>.gitattributes, public/videos/placeholder.mp4</files>

  <read_first>
    Read C:\Users\anoop\OneDrive\Desktop\Disney\.planning\phases\00-foundations\00-RESEARCH.md section "Git LFS — Initialization and .gitattributes" for the exact initialization sequence and the exact .gitattributes content.
    Read C:\Users\anoop\OneDrive\Desktop\Disney\.planning\phases\00-foundations\00-CONTEXT.md D-11, D-12, D-13 for the seed video decisions.
  </read_first>

  <action>
    Work in C:\Users\anoop\projects\content-moderation-assessment.

    CRITICAL ORDER: .gitattributes MUST be committed before placeholder.mp4 is added to git.
    If placeholder.mp4 is staged before .gitattributes is committed, git stores it as a regular blob (not LFS), bloating the repo. Recovery requires git lfs migrate which rewrites history.

    Step 1 — Initialize LFS globally for this repo:
      git lfs install
      (Installs LFS hooks in .git/hooks — run once per repo)

    Step 2 — Set up LFS tracking patterns:
      git lfs track "*.mp4"
      git lfs track "*.webm"
      git lfs track "*.lottie"
      git lfs track "public/videos/*.jpg"
      (Each command appends to .gitattributes. The last pattern uses a scoped glob to avoid tracking all .jpg files globally — only sprite sheets in public/videos/ are LFS-tracked.)

    Step 3 — Commit .gitattributes FIRST (before any LFS-tracked file):
      git add .gitattributes
      git commit -m "infra: configure git lfs tracking for mp4 webm lottie and video sprites"
      git push origin main
      (Do NOT add placeholder.mp4 yet — it must not be staged until this commit is pushed.)

    Step 4 — Generate placeholder video using the EXACT D-12 command:
      ffmpeg -f lavfi -i color=c=black:size=1280x720:rate=30 -t 5 -c:v libx264 -pix_fmt yuv420p -an public/videos/placeholder.mp4
      (ffmpeg 2026-03-15 is installed locally per environment check in RESEARCH.md.)
      (The -an flag means no audio track — silence is correct for the placeholder per D-11.)

    Step 5 — Verify the file is tracked by LFS before staging:
      git lfs status
      (Should NOT show placeholder.mp4 yet since it's untracked. Now stage it.)
      git add public/videos/placeholder.mp4
      git lfs status
      (Should show placeholder.mp4 with status "Git LFS objects to be pushed" or similar — confirming it will be pushed as LFS content, not a regular blob.)

    Step 6 — Verify the file type locally before committing:
      file public/videos/placeholder.mp4
      (Must return something containing "ISO Media" or "MP4" — NOT a text file.)

    Step 7 — Commit and push the placeholder video:
      git commit -m "feat: add git lfs seed placeholder video for lfs verification"
      git push origin main
      (This push will upload the LFS object to GitHub's LFS storage. The push output should show "Uploading LFS objects: 100%" confirming real LFS upload.)

    Step 8 — Verify LFS pointer in the repo (not the file content):
      git show HEAD:public/videos/placeholder.mp4
      (Should show a text pointer like "version https://git-lfs.github.com/spec/v1" — this is correct. The pointer is what git stores; LFS stores the binary separately.)

    Note on the .gitkeep: public/videos/.gitkeep was committed in Plan 02. Now that placeholder.mp4 is committed, the .gitkeep is optional. It can stay — it is a 0-byte file and does not affect anything.
  </action>

  <verify>
    <automated>
      cd C:\Users\anoop\projects\content-moderation-assessment

      Test 1 — .gitattributes has all required patterns:
        grep "*.mp4 filter=lfs" .gitattributes && echo "MP4 OK"
        grep "*.webm filter=lfs" .gitattributes && echo "WEBM OK"
        grep "*.lottie filter=lfs" .gitattributes && echo "LOTTIE OK"
        grep "public/videos/\*.jpg filter=lfs" .gitattributes && echo "JPG SCOPED OK"

      Test 2 — placeholder.mp4 is tracked by LFS:
        git lfs ls-files | grep "placeholder.mp4" && echo "LFS TRACKED"

      Test 3 — Local file is a real MP4 (not a pointer):
        file public/videos/placeholder.mp4 | grep -i "ISO Media\|mp4" && echo "REAL MP4"

      Test 4 — Git object is an LFS pointer (correct — git stores pointer, LFS stores binary):
        git show HEAD:public/videos/placeholder.mp4 | grep "git-lfs.github.com" && echo "POINTER IN GIT OK"

      Test 5 — File is correct size (>0 bytes, not a 128-byte pointer):
        powershell -Command "(Get-Item 'public/videos/placeholder.mp4').Length" 2>/dev/null || wc -c < public/videos/placeholder.mp4
        (Expected: ~100KB to ~500KB for a 5-second H.264 black-frame video)
    </automated>
  </verify>

  <acceptance_criteria>
    - .gitattributes contains "*.mp4 filter=lfs diff=lfs merge=lfs -text"
    - .gitattributes contains "*.webm filter=lfs diff=lfs merge=lfs -text"
    - .gitattributes contains "*.lottie filter=lfs diff=lfs merge=lfs -text"
    - .gitattributes contains "public/videos/*.jpg filter=lfs diff=lfs merge=lfs -text" (scoped, not global *.jpg)
    - git lfs ls-files shows placeholder.mp4
    - file public/videos/placeholder.mp4 returns output containing "ISO Media" or "MP4" (not text/ASCII)
    - git show HEAD:public/videos/placeholder.mp4 returns LFS pointer text starting with "version https://git-lfs.github.com/spec/v1"
    - git log shows two new commits: the .gitattributes commit followed by the placeholder.mp4 commit
    - Commits are in correct order: .gitattributes committed BEFORE placeholder.mp4
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LFS object storage → CI checkout | LFS binary crosses network during git lfs pull in CI; pointer vs binary distinction |
| git commit → GitHub LFS storage | Binary is uploaded to LFS; only pointer in git object store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-10 | Denial of Service | LFS bandwidth quota exhaustion | mitigate | Spending cap = $0 before push (D-17, Task 1); budget documented in PITFALLS Pitfall 2; R2 CDN pre-provisioned in Plan 04 for production video serving |
| T-00-11 | Tampering | LFS pointer committed as regular git object | mitigate | .gitattributes committed before placeholder.mp4; git lfs status verified before committing |
| T-00-12 | Spoofing | CI silently passing with LFS pointer file (128-byte text) instead of real MP4 | mitigate | Verify LFS step in deploy.yml fails build if file command returns non-MP4 output (DEPLOY-05) |
| T-00-13 | Information Disclosure | .planning/ artifacts inside the deploy repo | mitigate | .gitignore includes .planning/; repo created in separate directory outside Desktop\Disney (Pitfall P8) |
</threat_model>

<verification>
Phase 0 Plan C complete when ALL of the following are TRUE:
1. .gitattributes committed BEFORE placeholder.mp4 — confirmed by git log ordering
2. git lfs ls-files shows placeholder.mp4 as LFS-tracked
3. Local file command confirms placeholder.mp4 is a real MP4 (not pointer)
4. CI Deploy workflow runs green after placeholder.mp4 push — the "Verify LFS files resolved" step (defined in Plan 02's deploy.yml) passes
5. GitHub billing spending cap is $0 (confirmed in Task 1)
6. .gitattributes has scoped pattern public/videos/*.jpg (not global *.jpg)
</verification>

<success_criteria>
- DEPLOY-04: actions/checkout@v6 with lfs: true confirmed in deploy.yml (carried over from Plan 02, verified not removed)
- DEPLOY-05: CI verification step exists and passes — real MP4 confirmed, not LFS pointer
- DEPLOY-06: .gitattributes tracks *.mp4, *.webm, *.lottie, public/videos/*.jpg
- DEPLOY-08: R2 documented as production CDN in Plan 04 (this plan provisions the LFS side; CDN stub is Plan 04)
- DEPLOY-09: GitHub billing spending cap is $0 before first LFS commit
- CONTENT-04: placeholder.mp4 is 1280x720 H.264 (per D-12 ffmpeg command); encoding spec for real clips (CRF 26, AAC 96 kbps, faststart, ≤20 MB) documented in docs/video-manifest.md (Plan 04)
</success_criteria>

<output>
Create .planning/phases/00-foundations/00-03-SUMMARY.md when done.
</output>
