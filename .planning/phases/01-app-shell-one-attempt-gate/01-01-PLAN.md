---
phase: 01-app-shell-one-attempt-gate
plan: 01
type: execute
wave: 1
wave_name: Foundation Primitives (schema + testing infra + pure logic)
depends_on: []
files_modified:
  - package.json
  - vitest.config.js
  - tests/setup.js
  - src/data/taxonomy.json
  - tests/taxonomy.test.js
  - src/state/screens.js
  - src/utils/validators.js
  - src/utils/dedup.js
  - src/hooks/useOneAttemptGuard.js
  - src/hooks/useAssessmentState.js
  - src/components/ErrorBoundary.jsx
  - tests/utils/validators.test.js
  - tests/utils/dedup.test.js
  - tests/state/screens.test.js
  - tests/hooks/useOneAttemptGuard.test.js
  - tests/hooks/useAssessmentState.test.js
  - tests/components/ErrorBoundary.test.jsx
autonomous: true
requirements:
  - IDENT-01
  - IDENT-02
  - IDENT-03
  - IDENT-04
  - ATTEMPT-01
  - ATTEMPT-02

must_haves:
  truths:
    - "npm test passes after every task commit in this wave (Phase 0 green-build discipline)"
    - "taxonomy.json version is 0.2.0-draft and every L1 has a definition string; every L2 has a definition string and an example string"
    - "SCREENS is a frozen object exported from src/state/screens.js with exactly LANDING, GUIDELINES, ASSESSMENT_PLACEHOLDER, ALREADY_COMPLETED"
    - "validateName / validateEmail are pure functions returning { valid, error } with tested edge cases"
    - "useOneAttemptGuard returns { hasAttempted, record, markAttempted, clear } and survives QuotaExceeded / SecurityError / corrupted-JSON without throwing"
    - "useAssessmentState hydrates identity from sessionStorage on mount; hydrated identity auto-advances initial screen to GUIDELINES"
    - "hashEmail is a Phase 1 STUB (trim + lowercase) marked with a Phase-5 replacement comment"
    - "ErrorBoundary is class-based, catches render errors, renders generic fallback without leaking a stack trace"
  artifacts:
    - path: "src/data/taxonomy.json"
      provides: "Extended taxonomy schema (v0.2.0-draft) with L1 definitions + L2 definitions + L2 examples"
      contains: "0.2.0-draft"
    - path: "src/state/screens.js"
      provides: "Frozen SCREENS enum shared across hooks + screens"
      exports: ["SCREENS"]
    - path: "src/utils/validators.js"
      provides: "Pure validateName + validateEmail"
      exports: ["validateName", "validateEmail"]
    - path: "src/utils/dedup.js"
      provides: "Phase 1 stub hashEmail (Phase 5 replaces with real SHA-256)"
      exports: ["hashEmail"]
    - path: "src/hooks/useOneAttemptGuard.js"
      provides: "localStorage-backed one-attempt hook (full read + write + clear API)"
      exports: ["useOneAttemptGuard"]
    - path: "src/hooks/useAssessmentState.js"
      provides: "Screen state machine + identity slice + transitions"
      exports: ["useAssessmentState"]
    - path: "src/components/ErrorBoundary.jsx"
      provides: "Class-based render-error boundary for App.jsx root"
    - path: "tests/setup.js"
      provides: "Testing Library cleanup registration + happy-dom globals"
  key_links:
    - from: "src/hooks/useAssessmentState.js"
      to: "src/state/screens.js"
      via: "import { SCREENS } — no string literals for screen names"
    - from: "src/hooks/useOneAttemptGuard.js"
      to: "localStorage key 'cma_attempt_v1'"
      via: "safeReadRecord / safeWrite / safeRemove with try/catch"
    - from: "src/hooks/useAssessmentState.js"
      to: "sessionStorage key 'cma_identity_v1'"
      via: "lazy useState initializer reads on mount"
---

<objective>
Build the invisible foundation for Phase 1: schema extension, testing infrastructure, and every pure primitive (enum, validators, dedup stub, storage hooks, ErrorBoundary) that downstream waves will compose into screens. No user-visible change lands in this wave; every subsequent wave depends on the contracts locked here.

Purpose: Interface-first ordering (per planner discipline). Waves 2, 3, 4 must import already-tested primitives — no scavenger hunts in later plans. Locking taxonomy schema now closes RESEARCH.md §8 Gap A1 (Option A adopted); locking SCREENS enum + hook signatures now keeps LandingScreen/GuidelinesScreen from re-negotiating contracts.
Output: 6 new source files, 6 new test files, 3 modified config files, all committed atomically per task, npm test green after each commit.
</objective>

<execution_context>
@C:\Users\anoop\projects\content-moderation-assessment\.planning\phases\01-app-shell-one-attempt-gate\01-RESEARCH.md
@C:\Users\anoop\projects\content-moderation-assessment\.planning\phases\01-app-shell-one-attempt-gate\01-CONTEXT.md
</execution_context>

<context>
@C:\Users\anoop\projects\content-moderation-assessment\.planning\ROADMAP.md
@C:\Users\anoop\projects\content-moderation-assessment\.planning\REQUIREMENTS.md
@C:\Users\anoop\projects\content-moderation-assessment\.planning\research\ARCHITECTURE.md
@C:\Users\anoop\projects\content-moderation-assessment\docs\brand-guardrails.md
@C:\Users\anoop\projects\content-moderation-assessment\src\data\taxonomy.json
@C:\Users\anoop\projects\content-moderation-assessment\tests\taxonomy.test.js
@C:\Users\anoop\projects\content-moderation-assessment\vitest.config.js
@C:\Users\anoop\projects\content-moderation-assessment\package.json

<interfaces>
<!-- Contracts locked in this wave. Downstream plans import verbatim; DO NOT redesign. -->

Screen enum (src/state/screens.js) — D-P1-01:
  export const SCREENS = Object.freeze({
    LANDING: 'LANDING',
    GUIDELINES: 'GUIDELINES',
    ASSESSMENT_PLACEHOLDER: 'ASSESSMENT_PLACEHOLDER',
    ALREADY_COMPLETED: 'ALREADY_COMPLETED',
  });

Validators (src/utils/validators.js) — D-P1-06:
  export function validateName(input): { valid: boolean, error: string | null }
  export function validateEmail(input): { valid: boolean, error: string | null }
  - Name: trim, non-empty, <= 100 chars
  - Email: trim, non-empty, <= 254 chars, matches /^[^\s@]+@[^\s@]+\.[^\s@]+$/
  - Error strings are plain English (no i18n keys — deferred v2)

Dedup stub (src/utils/dedup.js) — CONTEXT §Specifics + RESEARCH §Security:
  export function hashEmail(input: string): string
  - Phase 1 STUB: return (input ?? '').trim().toLowerCase()
  - File header comment MUST include: "TODO(Phase 5): replace with Web Crypto SHA-256 per ATTEMPT-04"

useOneAttemptGuard (src/hooks/useOneAttemptGuard.js) — D-P1-04:
  export function useOneAttemptGuard(): {
    hasAttempted: boolean,                    // derived: record !== null
    record: { emailHash, completedAt, submissionId } | null,
    markAttempted({ emailHash, submissionId }): void,   // Phase 5 imports this
    clear(): void,                             // dev/test only
  }
  - localStorage key: 'cma_attempt_v1'
  - Value shape: { emailHash: string, completedAt: ISO8601 string, submissionId: string }
  - Hydration: synchronous via useState lazy initializer (no flicker)
  - Defenses: try/catch on every getItem/setItem/removeItem; JSON.parse in try/catch; 4-field shape check → treat unknown shape as no record + removeItem the corrupt slot

useAssessmentState (src/hooks/useAssessmentState.js) — D-P1-02, D-P1-03:
  export function useAssessmentState(): {
    screen: SCREENS[keyof SCREENS],
    identity: { name, email, startedAt } | null,
    startAssessment({ name, email }): void,    // LANDING -> GUIDELINES, writes sessionStorage
    enterGuidelines(): void,                    // idempotent -> GUIDELINES
    enterAssessment(): void,                    // GUIDELINES -> ASSESSMENT_PLACEHOLDER
    showAlreadyCompleted(): void,               // any -> ALREADY_COMPLETED (called from Wave 2)
    resetAttempt(): void,                       // dev-only: clears sessionStorage + returns to LANDING
  }
  - sessionStorage key: 'cma_identity_v1'
  - Value shape: { name: string, email: string, startedAt: ISO8601 string }
  - Initial screen: LANDING if no identity; GUIDELINES if identity present (refresh-mid-flow, IDENT-04)
  - Defenses: same try/catch + shape check pattern as useOneAttemptGuard

Taxonomy schema extension (src/data/taxonomy.json) — RESEARCH §8 Option A:
  Root: { version: "0.2.0-draft", categories: [...] }
  L1 node: { id, label, definition, subcategories }
  L2 node: { id, label, definition, example }
  - All 10 L1s must have a definition string (1-2 sentences)
  - All 65 L2s must have a definition string (short) + example string (one concrete instance)
  - Draft wording is subject to Phase 3 client sign-off (CC-03) — do not editorialize beyond generic phrasing
  - version bumped from '0.1.0-draft' -> '0.2.0-draft'

ErrorBoundary (src/components/ErrorBoundary.jsx) — D-P1-10:
  export default class ErrorBoundary extends Component
  - static getDerivedStateFromError + componentDidCatch + render
  - Fallback: <div role="alert"><h1>Something went wrong.</h1><p>Please refresh the page. If the problem persists, contact your recruiter.</p></div>
  - No new npm dependency; ~20 LOC total

Vitest config extensions:
  - globals: true (enables afterEach auto-registration for Testing Library cleanup)
  - setupFiles: ['./tests/setup.js']
  - environment: 'happy-dom' (unchanged)
  - include: keep existing pattern

tests/setup.js contract:
  import '@testing-library/jest-dom/vitest'
  import { afterEach } from 'vitest'
  import { cleanup } from '@testing-library/react'
  afterEach(() => cleanup())

Package additions — D-P1-13 + RESEARCH §6 Risk 3 resolution:
  devDependencies (add only):
    - @testing-library/react@^16.3.0
    - @testing-library/user-event@^14.6.0
    - @testing-library/jest-dom@^6.6.3     (adds .toBeInTheDocument / .toBeDisabled sugar; explicit deviation from D-P1-13 documented per RESEARCH.md §6 Risk 3)
  NO runtime dependencies added.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-01-01: Extend taxonomy schema to v0.2.0-draft (L1 definitions + L2 definitions/examples) and update Phase 0 schema test</name>
  <files>src/data/taxonomy.json, tests/taxonomy.test.js</files>

  <read_first>
    Read src/data/taxonomy.json (current v0.1.0-draft, id + label only on both L1 and L2) and tests/taxonomy.test.js (Phase 0's 8 schema assertions). Read 01-RESEARCH.md §8 "Data-Driven Rendering — Schema Gap Flagged" for Option A rationale and §10 "Draft L1 definitions" for authoring hints. Read docs/brand-guardrails.md to confirm no forbidden word will appear in drafted copy.
  </read_first>

  <behavior>
    - Test 1: taxonomy.version === '0.2.0-draft' (bumped from 0.1.0-draft)
    - Test 2: taxonomy.categories.length === 10 (unchanged)
    - Test 3: every L1 has non-empty string definition
    - Test 4: every L2 has non-empty string definition
    - Test 5: every L2 has non-empty string example
    - Test 6: all existing Phase 0 invariants still hold (id + label present, subcategory id parent.child format, unique ids, L1 ids "1".."10")
    - Test 7: none of the drafted strings contain any forbidden brand token (regression guard — scan definition + example values for /disney|marvel|pixar|star wars|\bthor\b|\bfrozen\b/i)
  </behavior>

  <action>
    RED: Update tests/taxonomy.test.js by (a) changing the version assertion from '0.1.0-draft' to '0.2.0-draft', (b) adding three new it() blocks: "every L1 has non-empty definition string", "every L2 has non-empty definition string", "every L2 has non-empty example string", (c) adding one it() block "no forbidden brand tokens in drafted copy" that concatenates all L1.definition + L2.definition + L2.example strings and asserts a case-insensitive regex from docs/brand-guardrails.md (use the exact FORBIDDEN pattern subset covering the top-tier tokens) returns no match. Run npm test — the version test, the three shape tests, and the brand test MUST fail because taxonomy.json is still v0.1.0-draft with no definition/example fields.

    GREEN: Edit src/data/taxonomy.json in place:
      - Bump root version to "0.2.0-draft".
      - For each of the 10 existing L1 nodes, add a "definition" field: a single 1-2 sentence generic description that names the category behavior without naming any franchise/brand. Example shape for L1 "1 Copyright & IP": "Content that infringes copyright, trademarks, or intellectual-property rights of others — including full-work reproduction, sync misuse, and counterfeit sales."
      - For each of the 65 existing L2 nodes, add "definition" (one short sentence) + "example" (one concrete, generic, brand-neutral instance). Example for L2 "1.1 Unauthorized full-work reproduction": definition "A full copy of a copyrighted work uploaded without rights.", example "A full-length film uploaded to a public feed with no license documentation."
      - Do NOT reorder existing keys (id, label first; then definition; then subcategories on L1 / example on L2). Do NOT change any existing id or label value. Do NOT introduce any franchise or character name — draft strictly from RESEARCH §10 guidance ("Content that ...", "Videos containing ...", "Statements or imagery that ...").
      - Keep file under 500 lines (project rule); pretty-printed 2-space JSON should land ~300-450 lines.

    Run npm test — all Phase 0 tests + the 4 new assertions MUST pass. Then:
      git add src/data/taxonomy.json tests/taxonomy.test.js
      git commit -m "feat(taxonomy): extend schema to v0.2.0-draft with L1/L2 definitions and L2 examples"
  </action>

  <verify>
    <automated>
      cd C:\Users\anoop\projects\content-moderation-assessment
      npm test -- --run 2>&1 | tail -20
      node -e "const t=require('./src/data/taxonomy.json'); const bad=t.categories.filter(c=>!c.definition||!c.definition.length); console.log(bad.length===0?'L1 DEFS OK':'MISSING L1 DEFS: '+bad.length)"
      node -e "const t=require('./src/data/taxonomy.json'); const l2=t.categories.flatMap(c=>c.subcategories); const bad=l2.filter(s=>!s.definition||!s.example); console.log(bad.length===0?'L2 DEFS+EX OK':'MISSING L2 FIELDS: '+bad.length)"
      node -e "const t=JSON.stringify(require('./src/data/taxonomy.json')); const bad=['disney','marvel','pixar','star wars','spider-man']; const hit=bad.filter(b=>t.toLowerCase().includes(b)); console.log(hit.length===0?'BRAND CLEAN':'LEAK: '+hit.join(','))"
    </automated>
  </verify>

  <done>
    - taxonomy.json version is "0.2.0-draft"
    - All 10 L1 entries have a non-empty definition string
    - All 65 L2 entries have non-empty definition + example strings
    - Existing Phase 0 tests still pass; 4 new tests pass; npm test full-suite green
    - Pre-commit brand-guard hook allows the commit (no forbidden strings in drafted copy)
    - No id, label, or subcategory ordering changed
    - Commit landed on main
  </done>
</task>

<task type="auto">
  <name>Task 01-01-02: Add Testing Library devDeps + vitest.config.js extensions + tests/setup.js</name>
  <files>package.json, vitest.config.js, tests/setup.js</files>

  <read_first>
    Read 01-RESEARCH.md §6 "Vitest + happy-dom + @testing-library/react Setup for React 19" for the exact vitest.config.js extension and the jest-dom decision (RESEARCH recommends adding jest-dom per Risk 3 — we accept the +1 devDep). Read current vitest.config.js + package.json (already listed in <context>). Read D-P1-13 in 01-CONTEXT.md.
  </read_first>

  <action>
    Step 1 — Install devDependencies:
      npm install --save-dev @testing-library/react@^16.3.0 @testing-library/user-event@^14.6.0 @testing-library/jest-dom@^6.6.3
    Rationale for jest-dom (deviation from D-P1-13 letter): RESEARCH.md §6 Risk 3 recommends it. Adding jest-dom keeps downstream test files terse (.toBeInTheDocument / .toBeDisabled) and is a well-established Testing Library companion. Document this in the commit message body.

    Step 2 — Extend vitest.config.js. Replace current content with:
      import { defineConfig } from 'vitest/config'
      import react from '@vitejs/plugin-react'
      export default defineConfig({
        plugins: [react()],
        test: {
          environment: 'happy-dom',
          globals: true,
          setupFiles: ['./tests/setup.js'],
          include: ['tests/**/*.test.js', 'tests/**/*.test.jsx'],
        },
      })
    Note: the react() plugin is required for JSX-in-tests (LandingScreen.test.jsx etc. in Waves 2-4). Without it, .jsx imports fail with an esbuild parse error.

    Step 3 — Create tests/setup.js:
      import '@testing-library/jest-dom/vitest'
      import { afterEach } from 'vitest'
      import { cleanup } from '@testing-library/react'
      afterEach(() => { cleanup() })

    Step 4 — Run npm test. All Phase 0 taxonomy tests + Task 01-01-01's new tests MUST still pass. If tests/taxonomy.test.js fails because globals: true changed something, that indicates a config bug — investigate before proceeding.

    Step 5 — Commit:
      git add package.json package-lock.json vitest.config.js tests/setup.js
      git commit -m "chore(testing): add @testing-library/{react,user-event,jest-dom} devDeps and configure vitest globals + setup"
      Body should note: "jest-dom added per RESEARCH.md §6 Risk 3 (deviation from D-P1-13 letter, spirit preserved — no runtime deps added)."
  </action>

  <verify>
    <automated>
      cd C:\Users\anoop\projects\content-moderation-assessment
      npm test -- --run 2>&1 | tail -10
      node -e "const p=require('./package.json'); const d=p.devDependencies; const need=['@testing-library/react','@testing-library/user-event','@testing-library/jest-dom']; const miss=need.filter(n=>!d[n]); console.log(miss.length===0?'DEPS OK':'MISSING: '+miss.join(','))"
      node -e "const c=require('fs').readFileSync('vitest.config.js','utf8'); const need=['globals: true','setupFiles','tests/setup.js','plugin-react']; const miss=need.filter(n=>!c.includes(n)); console.log(miss.length===0?'CONFIG OK':'MISSING: '+miss.join(','))"
      test -f tests/setup.js && echo "SETUP FILE OK"
    </automated>
  </verify>

  <done>
    - package.json devDependencies contains @testing-library/react, @testing-library/user-event, @testing-library/jest-dom
    - vitest.config.js declares plugins: [react()], globals: true, setupFiles: ['./tests/setup.js']
    - tests/setup.js exists and registers @testing-library/react cleanup + jest-dom matchers
    - npm test still passes for existing taxonomy tests
    - Commit landed on main
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-01-03: SCREENS enum + validators + dedup stub (pure logic, unit-tested)</name>
  <files>src/state/screens.js, src/utils/validators.js, src/utils/dedup.js, tests/state/screens.test.js, tests/utils/validators.test.js, tests/utils/dedup.test.js</files>

  <read_first>
    Read 01-CONTEXT.md §Specifics for the frozen SCREENS object shape and hashEmail stub semantics. Read 01-RESEARCH.md §2 for the validator contract + edge cases and §Security for the hashEmail STUB rationale.
  </read_first>

  <behavior>
    tests/state/screens.test.js:
      - SCREENS has exactly 4 keys: LANDING, GUIDELINES, ASSESSMENT_PLACEHOLDER, ALREADY_COMPLETED
      - Each key maps to a string equal to the key name (SCREENS.LANDING === 'LANDING' etc.)
      - Object.isFrozen(SCREENS) === true
      - Attempting to reassign a key throws in strict mode (mutation is refused)

    tests/utils/validators.test.js — validateName:
      - '' → { valid: false, error: /Please enter your full name/i }
      - '   ' → { valid: false, error: /Please enter/i }
      - 'A' → { valid: true, error: null }
      - 'A'.repeat(100) → { valid: true }
      - 'A'.repeat(101) → { valid: false, error: /100 characters/i }
      - '  Alice  ' → { valid: true } (trimmed to 'Alice' — length 5)
      - unicode '名前' → { valid: true }
      - null / undefined input → { valid: false } (no throw)

    tests/utils/validators.test.js — validateEmail:
      - '' → { valid: false }
      - 'foo' → { valid: false, error: /valid email/i } (no @)
      - 'foo@' → { valid: false } (no domain)
      - 'foo@bar' → { valid: false } (no dot)
      - 'foo@bar.co' → { valid: true }
      - 'foo bar@baz.co' → { valid: false } (whitespace)
      - 'foo@bar.baz.co' → { valid: true } (multi-dot)
      - 'a@b.c' → { valid: true } (minimally shaped)
      - 'a'.repeat(250) + '@x.io' → { valid: false, error: /too long/i } (over 254)
      - '  alice@example.com  ' → { valid: true } (trimmed)
      - null → { valid: false }

    tests/utils/dedup.test.js:
      - hashEmail('  Alice@Example.COM  ') === 'alice@example.com'
      - hashEmail('') === ''
      - hashEmail(null) === '' (no throw)
      - hashEmail(undefined) === ''
      - Output is idempotent: hashEmail(hashEmail('x@y.co')) === hashEmail('x@y.co')
  </behavior>

  <action>
    RED: Create tests/state/screens.test.js, tests/utils/validators.test.js, tests/utils/dedup.test.js implementing the behavior blocks above. Run npm test — all new tests must fail (source files do not exist).

    GREEN — create source files:

    src/state/screens.js — 6 lines:
      export const SCREENS = Object.freeze({
        LANDING: 'LANDING',
        GUIDELINES: 'GUIDELINES',
        ASSESSMENT_PLACEHOLDER: 'ASSESSMENT_PLACEHOLDER',
        ALREADY_COMPLETED: 'ALREADY_COMPLETED',
      });

    src/utils/validators.js — pure functions per RESEARCH §2:
      const EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      export function validateName(input) {
        const s = (input ?? '').trim();
        if (s.length === 0) return { valid: false, error: 'Please enter your full name.' };
        if (s.length > 100) return { valid: false, error: 'Name must be 100 characters or fewer.' };
        return { valid: true, error: null };
      }
      export function validateEmail(input) {
        const s = (input ?? '').trim();
        if (s.length === 0) return { valid: false, error: 'Please enter your email.' };
        if (s.length > 254) return { valid: false, error: 'Email is too long.' };
        if (!EMAIL_RE.test(s)) return { valid: false, error: 'Please enter a valid email address.' };
        return { valid: true, error: null };
      }

    src/utils/dedup.js — Phase 1 STUB:
      // TODO(Phase 5): replace with Web Crypto SHA-256 per ATTEMPT-04 (normalize + hash for server dedup)
      // Phase 1 ships trim + lowercase so downstream localStorage records have a recognizable-shape emailHash.
      export function hashEmail(input) {
        return (input ?? '').trim().toLowerCase();
      }

    Run npm test — all new tests must pass alongside existing taxonomy + setup tests. Commit:
      git add src/state/screens.js src/utils/validators.js src/utils/dedup.js tests/state/screens.test.js tests/utils/validators.test.js tests/utils/dedup.test.js
      git commit -m "feat(state,utils): add SCREENS enum, validators, and Phase 1 hashEmail stub with unit tests"
  </action>

  <verify>
    <automated>
      cd C:\Users\anoop\projects\content-moderation-assessment
      npm test -- --run 2>&1 | tail -15
      node -e "import('./src/state/screens.js').then(m=>{const k=Object.keys(m.SCREENS); console.log(k.length===4 && Object.isFrozen(m.SCREENS)?'SCREENS OK':'SCREENS BROKEN: '+k.join(','));})"
      node -e "import('./src/utils/validators.js').then(m=>console.log(typeof m.validateName==='function' && typeof m.validateEmail==='function'?'VALIDATORS OK':'MISSING'))"
      node -e "import('./src/utils/dedup.js').then(m=>console.log(m.hashEmail('  A@B.CO  ')==='a@b.co'?'HASH STUB OK':'STUB BROKEN'))"
    </automated>
  </verify>

  <done>
    - src/state/screens.js exports frozen SCREENS with the 4 required keys
    - src/utils/validators.js exports pure validateName + validateEmail matching RESEARCH §2 contract
    - src/utils/dedup.js exports hashEmail stub with a TODO(Phase 5) header comment
    - tests/state/screens.test.js + tests/utils/validators.test.js + tests/utils/dedup.test.js all pass
    - npm test full-suite green (Phase 0 tests + new tests)
    - No file exceeds 100 lines
    - Commit landed on main
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-01-04: useOneAttemptGuard + useAssessmentState hooks + ErrorBoundary (composable state primitives)</name>
  <files>src/hooks/useOneAttemptGuard.js, src/hooks/useAssessmentState.js, src/components/ErrorBoundary.jsx, tests/hooks/useOneAttemptGuard.test.js, tests/hooks/useAssessmentState.test.js, tests/components/ErrorBoundary.test.jsx</files>

  <read_first>
    Read 01-RESEARCH.md §3 "sessionStorage + localStorage Gotchas in React 19" for the four defense patterns (G1-G4) and the useOneAttemptGuard reference implementation. Read §4 for the useState-not-useSyncExternalStore decision. Read §5 for the ErrorBoundary shape. Read D-P1-04, D-P1-02, D-P1-03, D-P1-05, D-P1-10 in CONTEXT.md. Read src/state/screens.js and src/utils/validators.js from Task 01-01-03.
  </read_first>

  <behavior>
    tests/hooks/useOneAttemptGuard.test.js — clear localStorage in beforeEach:
      - Fresh state (empty localStorage): hasAttempted === false, record === null
      - After markAttempted({ emailHash: 'a@b.co', submissionId: 'sub-1' }): hasAttempted === true, record.emailHash === 'a@b.co', record.submissionId === 'sub-1', record.completedAt is ISO8601 string parseable by new Date()
      - After clear(): hasAttempted === false, record === null, localStorage.getItem('cma_attempt_v1') === null
      - Malformed JSON pre-seeded ('{not json'): hasAttempted === false, and the corrupt slot is removed
      - Missing shape field pre-seeded (JSON.stringify({emailHash:'x'}) — no completedAt / submissionId): hasAttempted === false, corrupt slot removed
      - localStorage.setItem throws (mock via vi.spyOn(Storage.prototype,'setItem').mockImplementation(()=>{throw new Error('quota')})): markAttempted does not throw; hasAttempted remains false; hook stays functional (no unhandled rejection)

    tests/hooks/useAssessmentState.test.js — clear sessionStorage in beforeEach:
      - Initial screen === SCREENS.LANDING when sessionStorage empty
      - Initial screen === SCREENS.GUIDELINES when sessionStorage has valid identity (mid-refresh recovery — IDENT-04)
      - startAssessment({ name: 'Alice', email: 'a@b.co' }): screen becomes GUIDELINES, sessionStorage.cma_identity_v1 parses to { name:'Alice', email:'a@b.co', startedAt: <ISO> }
      - enterAssessment(): screen becomes ASSESSMENT_PLACEHOLDER
      - showAlreadyCompleted(): screen becomes ALREADY_COMPLETED
      - resetAttempt(): screen returns to LANDING, sessionStorage cleared
      - Malformed JSON in sessionStorage: initial screen === LANDING (graceful), corrupt slot removed
      - sessionStorage.setItem throws: startAssessment does not throw; screen still transitions to GUIDELINES (in-memory truth)

    tests/components/ErrorBoundary.test.jsx:
      - When children render normally, children are shown and no role="alert" node exists
      - When a child throws during render, fallback UI is shown: role="alert" node exists, heading "Something went wrong." present, "contact your recruiter" copy present, no stack trace text visible
      - console.error is silenced in the throw test via vi.spyOn to keep output clean (per RESEARCH §6)
  </behavior>

  <action>
    RED: Create the 3 test files against the specs above. Use renderHook from @testing-library/react for hooks. Use render + a throwing child component for ErrorBoundary. Run npm test — the 3 new files must fail (sources absent).

    GREEN:

    src/hooks/useOneAttemptGuard.js — implement per RESEARCH §3 reference:
      - KEY = 'cma_attempt_v1'
      - safeReadRecord(): try/catch around getItem + JSON.parse; 4-field shape check (emailHash / completedAt / submissionId strings); on unknown shape → removeItem + return null
      - safeWrite(value), safeRemove(): try/catch, return boolean
      - export function useOneAttemptGuard():
          const [record, setRecord] = useState(() => safeReadRecord());
          const markAttempted = useCallback(({emailHash, submissionId}) => { const v = { emailHash, submissionId, completedAt: new Date().toISOString() }; if (safeWrite(v)) setRecord(v); }, []);
          const clear = useCallback(() => { if (safeRemove()) setRecord(null); }, []);
          return { hasAttempted: record !== null, record, markAttempted, clear };
      - NO useSyncExternalStore, NO storage event listener (per RESEARCH §4).

    src/hooks/useAssessmentState.js — same defensive shape:
      - import { SCREENS } from '../state/screens.js'
      - IDENTITY_KEY = 'cma_identity_v1'
      - safeReadIdentity(): try/catch + 3-field shape check (name, email, startedAt strings) + removeItem on unknown shape
      - safeWriteIdentity(v) / safeRemoveIdentity(): try/catch, return boolean
      - export function useAssessmentState():
          const [identity, setIdentity] = useState(() => safeReadIdentity());
          const [screen, setScreen]   = useState(() => (safeReadIdentity() !== null ? SCREENS.GUIDELINES : SCREENS.LANDING));
          startAssessment({name, email}): build v = { name: name.trim(), email: email.trim(), startedAt: new Date().toISOString() }; safeWriteIdentity(v); setIdentity(v); setScreen(SCREENS.GUIDELINES);
          enterGuidelines(): setScreen(SCREENS.GUIDELINES);
          enterAssessment(): setScreen(SCREENS.ASSESSMENT_PLACEHOLDER);
          showAlreadyCompleted(): setScreen(SCREENS.ALREADY_COMPLETED);
          resetAttempt(): safeRemoveIdentity(); setIdentity(null); setScreen(SCREENS.LANDING);
          return { screen, identity, startAssessment, enterGuidelines, enterAssessment, showAlreadyCompleted, resetAttempt };
      - All transition functions wrapped in useCallback with [] deps so consumers can safely pass them as props.

    src/components/ErrorBoundary.jsx — per RESEARCH §5 verbatim:
      - Class component, constructor sets { hasError: false }
      - static getDerivedStateFromError() returns { hasError: true }
      - componentDidCatch(error, info) logs via console.error with '[cma]' prefix
      - render(): if hasError, return <div role="alert"><h1>Something went wrong.</h1><p>Please refresh the page. If the problem persists, contact your recruiter.</p></div>; else return this.props.children.

    Run npm test — all new tests pass; existing tests still pass. Commit:
      git add src/hooks/useOneAttemptGuard.js src/hooks/useAssessmentState.js src/components/ErrorBoundary.jsx tests/hooks/useOneAttemptGuard.test.js tests/hooks/useAssessmentState.test.js tests/components/ErrorBoundary.test.jsx
      git commit -m "feat(hooks,ui): add useOneAttemptGuard, useAssessmentState, ErrorBoundary primitives with defensive storage handling"
  </action>

  <verify>
    <automated>
      cd C:\Users\anoop\projects\content-moderation-assessment
      npm test -- --run 2>&1 | tail -20
      node -e "const s=require('fs').readFileSync('src/hooks/useOneAttemptGuard.js','utf8'); ['try','catch','JSON.parse','useState','useCallback','markAttempted','clear'].forEach(t=>{if(!s.includes(t))throw new Error('missing '+t);}); console.log('GUARD OK')"
      node -e "const s=require('fs').readFileSync('src/hooks/useAssessmentState.js','utf8'); ['SCREENS','startAssessment','enterAssessment','showAlreadyCompleted','resetAttempt','sessionStorage'].forEach(t=>{if(!s.includes(t))throw new Error('missing '+t);}); console.log('STATE OK')"
      node -e "const s=require('fs').readFileSync('src/components/ErrorBoundary.jsx','utf8'); ['getDerivedStateFromError','componentDidCatch','role=\"alert\"'].forEach(t=>{if(!s.includes(t))throw new Error('missing '+t);}); console.log('BOUNDARY OK')"
    </automated>
  </verify>

  <done>
    - useOneAttemptGuard exports the full 4-field API and survives all 6 test cases
    - useAssessmentState exports the 5 transitions and hydrates screen based on sessionStorage identity
    - ErrorBoundary is class-based, class name matches file name, no external error-boundary dep
    - All 3 hook/component tests pass; full npm test green
    - No source file exceeds 200 lines
    - Commit landed on main
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keystrokes → Landing form (Wave 2 consumer) | Untrusted string input crosses the validator boundary |
| localStorage → hydrated app state | Attacker with browser access can pre-seed the one-attempt slot |
| sessionStorage → hydrated app state | Same attacker can pre-seed identity (soft state; no auth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | localStorage cma_attempt_v1 slot | accept | Client-side flag is by-design defeatable; Phase 5 server-side dedup is the authoritative gate. Documented in RESEARCH §Security. |
| T-01-02 | Denial of service | localStorage / sessionStorage throws (Safari private mode, kiosk browsers, quota) | mitigate | try/catch on every read/write in useOneAttemptGuard + useAssessmentState; failure returns null / no-op → user still gets a working Landing (best-effort UX). |
| T-01-03 | Tampering | Corrupted JSON in cma_attempt_v1 (dev bug, hostile inspection) | mitigate | JSON.parse in try/catch; 4-field shape check; removeItem on unknown shape (defense-in-depth against future dev bugs). |
| T-01-04 | Tampering | Long-string DoS on validators (e.g., paste a 1MB string) | mitigate | maxLength boundary in Wave 2 form + 100/254 char caps returned by validators (input trimmed before length check). |
| T-01-05 | Information disclosure | ErrorBoundary leaking stack trace to end user | mitigate | Fallback UI shows generic copy only; componentDidCatch logs to console.error but does NOT surface error.message in the DOM. |
| T-01-SC | Tampering | npm install of Testing Library packages | mitigate | @testing-library/react (8+ yrs), @testing-library/user-event (7+ yrs), @testing-library/jest-dom (6+ yrs) — all Approved in RESEARCH §Package Legitimacy Audit; zero postinstall scripts verified. |
</threat_model>

<nyquist_validation>
Observable signals that verify Wave 1 landed correctly. Executor MUST confirm each before advancing to Wave 2.

1. **Signal:** `npm test -- --run` prints "PASS" for every test file in tests/ (taxonomy + setup + screens + validators + dedup + useOneAttemptGuard + useAssessmentState + ErrorBoundary). **How to verify:** run the command, tail 20 lines, confirm zero failures.
2. **Signal:** taxonomy.json.version === '0.2.0-draft' with every L1 having `definition` and every L2 having `definition` + `example`. **How to verify:** `node -e "..."` shape checks in Task 01-01-01 <verify>.
3. **Signal:** SCREENS is frozen and mutation attempts fail silently (or throw in strict mode). **How to verify:** unit test asserts `Object.isFrozen(SCREENS)`.
4. **Signal:** useOneAttemptGuard survives QuotaExceeded and corrupted JSON without throwing an unhandled error. **How to verify:** the 6 unit tests in Task 01-01-04.
5. **Signal:** useAssessmentState hydrates `screen` to GUIDELINES when sessionStorage has valid identity — the mid-refresh recovery path required by IDENT-04. **How to verify:** the "Initial screen === GUIDELINES when identity populated" test.
6. **Signal:** ErrorBoundary renders fallback without stack trace text. **How to verify:** ErrorBoundary throw-test asserts absence of `error.message` in DOM.
7. **Signal:** No forbidden brand string appears in newly authored L1/L2 definition + example fields. **How to verify:** Task 01-01-01 test 7 + pre-commit brand-guard hook + node one-liner scanning JSON.stringify(taxonomy) for tokens.
8. **Signal:** Four atomic commits landed with conventional messages ("feat(taxonomy):", "chore(testing):", "feat(state,utils):", "feat(hooks,ui):"). **How to verify:** `git log --oneline -n 4` shows the expected sequence.
</nyquist_validation>

<risks>
Wave-specific risks (global risks live in 01-RESEARCH.md):

1. **Authoring drift on taxonomy definitions.** Draft copy in Task 01-01-01 is subject to Phase 3 client sign-off (CC-03). The person writing definitions must use generic phrasing per RESEARCH §10 — a stray franchise word triggers the brand-guard hook and blocks the commit. Mitigation: draft copy alongside the executor's local checkout so a rejected commit surfaces the token before push.
2. **jest-dom deviation from D-P1-13 letter.** We add a third devDep beyond the two named in D-P1-13. Justification in RESEARCH §6 Risk 3 (readability + established Testing Library companion). Mitigation: commit message explicitly documents the deviation so future auditors trace the decision.
3. **happy-dom coverage of Storage API.** The one-attempt tests require reliable window.localStorage in happy-dom — verified in Phase 0. If a happy-dom regression breaks Storage, tests will fail visibly (not silently) and the executor pins happy-dom to the current 20.10.6.
4. **useState lazy initializer double-invocation under StrictMode.** RESEARCH §Pitfall 16 confirms it's idempotent (reads storage twice, returns same value). Tests do NOT wrap components in <StrictMode> (RESEARCH §6) to keep behavior focused.
5. **shape-check false-rejections on future field additions.** The 4-field / 3-field shape check will reject records that gain new fields in Phase 5. Mitigation: shape check uses `typeof === 'string'` on required fields only, not `Object.keys().length === N` — extra fields pass through.
</risks>

<verification>
Wave 1 complete when ALL of the following are TRUE:
1. `npm test -- --run` prints zero failures across tests/taxonomy.test.js, tests/setup.js integration, tests/state/screens.test.js, tests/utils/validators.test.js, tests/utils/dedup.test.js, tests/hooks/useOneAttemptGuard.test.js, tests/hooks/useAssessmentState.test.js, tests/components/ErrorBoundary.test.jsx.
2. `npm run build` still succeeds (Vite build unaffected by new source files).
3. Brand-guard pre-commit hook passed on every commit in this wave (no forbidden strings).
4. 4 atomic commits landed on main with conventional-commit messages.
5. taxonomy.json v0.2.0-draft with 10 L1 definitions + 65 L2 definition/example pairs is on main.
6. package.json has the three new @testing-library/* devDeps.
7. Every file created is under 500 lines (project rule).
</verification>

<success_criteria>
- IDENT-01 (partial — validator layer): validateName covers non-empty + <=100 chars + trim
- IDENT-02 (partial — validator layer): validateEmail covers RFC-5322 basic + trim + 254 char cap
- IDENT-03 (partial — hook layer): useAssessmentState exposes startAssessment which will be gated by the form's disabled-until-valid state in Wave 2
- IDENT-04 (hook layer): useAssessmentState hydrates GUIDELINES on refresh when identity persists in sessionStorage
- ATTEMPT-01 (hook layer): markAttempted writes the full ATTEMPT-01 record shape (Phase 5 imports this)
- ATTEMPT-02 (hook layer): useOneAttemptGuard exposes hasAttempted for Wave 2's LandingScreen to gate on
- Foundation for GUIDE-01, GUIDE-02, GUIDE-06 landed: taxonomy.json v0.2.0-draft with definitions + examples is ready for Wave 3's GuidelinesScreen to consume
</success_criteria>

<output>
Create .planning/phases/01-app-shell-one-attempt-gate/01-01-SUMMARY.md when Wave 1 is complete.
</output>
