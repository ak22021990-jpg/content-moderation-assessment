---
phase: 02-custom-video-player-media-chrome
plan: 01
type: execute
wave: 1
wave_name: Infrastructure Setup (SCREENS rename, playlist.json, media-chrome install, video committed)
depends_on: []
files_modified:
  - package.json
  - src/state/screens.js
  - src/hooks/useAssessmentState.js
  - src/data/playlist.json
  - public/videos/.gitkeep
  - .gitattributes
  - src/components/AssessmentPlaceholderScreen.jsx
  - tests/state/screens.test.js
  - tests/hooks/useAssessmentState.test.js
  - tests/App.integration.test.jsx
autonomous: true
requirements:
  - PLAY-07
  - PLAY-09
  - CONTENT-05
  - CONTENT-06

must_haves:
  truths:
    - "SCREENS.ASSESSMENT replaces SCREENS.ASSESSMENT_PLACEHOLDER everywhere (enum, hook, integration test)"
    - "media-chrome@^4.19.2 installed and importable from media-chrome/react"
    - "playlist.json v1 committed with one test video entry pointing at /videos/v01.mp4"
    - "npm test passes with ASSESSMENT_PLACEHOLDER references migrated"
    - "npm run build succeeds with no media-chrome import errors"
    - "Zero forbidden brand strings in playlist.json, migrated test files, or commit messages"
  artifacts:
    - path: "src/state/screens.js"
      provides: "Updated SCREENS enum: ASSESSMENT replaces ASSESSMENT_PLACEHOLDER"
      exports: ["SCREENS"]
    - path: "src/data/playlist.json"
      provides: "Single-video playlist v1 with srcUrl, title, chapter data"
    - path: "src/hooks/useAssessmentState.js"
      provides: "enterAssessment now transitions to SCREENS.ASSESSMENT"
  key_links:
    - from: "src/state/screens.js"
      to: "src/hooks/useAssessmentState.js"
      via: "SCREENS.ASSESSMENT — consumed by enterAssessment transition"
    - from: "src/state/screens.js"
      to: "src/App.jsx"
      via: "SCREENS.ASSESSMENT — used in screen switch case (wired in Plan 04)"
    - from: "src/data/playlist.json"
      to: "media-chrome <video slot=\"media\" src={...}>"
      via: "import playlist from '../data/playlist.json' — Plan 02 wires this to VideoPlayerScreen"
---

<objective>
Rename ASSESSMENT_PLACEHOLDER to ASSESSMENT across the codebase, install media-chrome, create playlist.json with the single test video entry, and ensure existing tests pass after migration. This wave is plumbing-only — no player renders yet. Every subsequent wave depends on the contracts locked here.
</objective>

<execution_context>
@C:\Users\anoop\projects\content-moderation-assessment\.planning\phases\02-custom-video-player-media-chrome\02-RESEARCH.md
@C:\Users\anoop\projects\content-moderation-assessment\.planning\phases\02-custom-video-player-media-chrome\02-UI-SPEC.md
</execution_context>

<context>
@C:\Users\anoop\projects\content-moderation-assessment\.planning\ROADMAP.md
@C:\Users\anoop\projects\content-moderation-assessment\.planning\REQUIREMENTS.md
@C:\Users\anoop\projects\content-moderation-assessment\.planning\research\ARCHITECTURE.md
@C:\Users\anoop\projects\content-moderation-assessment\.planning\research\STACK.md
@C:\Users\anoop\projects\content-moderation-assessment\.planning\research\PITFALLS.md
@C:\Users\anoop\projects\content-moderation-assessment\docs\brand-guardrails.md
@C:\Users\anoop\projects\content-moderation-assessment\src\state\screens.js
@C:\Users\anoop\projects\content-moderation-assessment\src\hooks\useAssessmentState.js
@C:\Users\anoop\projects\content-moderation-assessment\tests\state\screens.test.js
@C:\Users\anoop\projects\content-moderation-assessment\tests\hooks\useAssessmentState.test.js
@C:\Users\anoop\projects\content-moderation-assessment\tests\App.integration.test.jsx
@C:\Users\anoop\projects\content-moderation-assessment\.gitattributes
@C:\Users\anoop\projects\content-moderation-assessment\.github\workflows\deploy.yml
@C:\Users\anoop\projects\content-moderation-assessment\public\videos\placeholder.mp4

<interfaces>
<!-- Contracts locked in this wave. Downstream plans import verbatim. -->

SCREENS enum (src/state/screens.js):
  export const SCREENS = Object.freeze({
    LANDING: 'LANDING',
    GUIDELINES: 'GUIDELINES',
    ASSESSMENT: 'ASSESSMENT',     // RENAMED from ASSESSMENT_PLACEHOLDER
    ALREADY_COMPLETED: 'ALREADY_COMPLETED',
  });

useAssessmentState.enterAssessment:
  - SETS screen to SCREENS.ASSESSMENT (was SCREENS.ASSESSMENT_PLACEHOLDER)
  - Callback signature unchanged: enterAssessment(): void
  - Downstream consumers (GuidelinesScreen "Begin Assessment" button) unaffected

playlist.json (src/data/playlist.json):
  {
    "_note": "Single test video for Phase 2. Remaining 4 videos added in Phase 3 (CONTENT-02).",
    "videos": [
      {
        "id": "v01",
        "title": "Test Video — Content Moderation",
        "srcUrl": "/videos/v01.mp4",
        "spriteUrl": "/sprites/v01_sprite.jpg",
        "thumbsVttUrl": "/vtt/v01.thumbs.vtt",
        "chaptersVttUrl": "/vtt/v01.chapters.vtt",
        "durationSec": 90,
        "chapters": [
          { "t": 25.0, "label": "Watermark overlay visible at bottom-right" },
          { "t": 55.0, "label": "Fictional brand logo appears" },
          { "t": 78.0, "label": "Text overlay with counterfeit claim" }
        ]
      }
    ],
    "_cdnDocs": "srcUrl paths are relative to Pages root. Phase 6 makes them env-conditional (VIDEO_BASE_URL prefix) per CONTENT-03 + STACK.md."
  }
  - Zero forbidden brand strings in any field value.
  - Chapter labels are generic descriptions of suspicious moments.
  - chapters array is single source of truth for chapters.vtt generation.

media-chrome version contract:
  - Installed: media-chrome@^4.19.2
  - Import path: 'media-chrome/react' for React wrappers
  - Import path: 'media-chrome/react/media-store' for hooks (useMediaRef, useMediaSelector)
  - Verified: npm test && npm run build both pass after install

.gitattributes LFS tracking:
  public/videos/*.mp4    filter=lfs diff=lfs merge=lfs -text
  public/sprites/*.jpg   filter=lfs diff=lfs merge=lfs -text
  (sprites line added in this wave; video line already exists from Phase 0)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 02-01-01: Install media-chrome@^4.19.2 and verify imports</name>
  <files>package.json, package-lock.json</files>

  <read_first>
    Read 02-RESEARCH.md §1 "media-chrome React API" for the package name and import conventions. Read STACK.md "Video Player" section for version confirmation. Read current package.json to understand existing dep layout.
  </read_first>

  <behavior>
    - media-chrome@^4.19.2 added to dependencies (not devDeps — it's a runtime player framework)
    - npm install succeeds without peer dependency warnings
    - Node can resolve `media-chrome/react` and `media-chrome/react/media-store` without errors
    - No other runtime dependencies added
    - package-lock.json updated
  </behavior>

  <action>
    Step 1 — Install:
      npm install media-chrome@^4.19.2

    Step 2 — Verify resolve (no-commit check):
      node -e "import('media-chrome/react').then(() => console.log('media-chrome/react OK')).catch(e => { console.error(e); process.exit(1); })"
      node -e "import('media-chrome/react/media-store').then(() => console.log('media-chrome/react/media-store OK')).catch(e => { console.error(e); process.exit(1); })"

    Step 3 — Verify build:
      npm run build
      (Vite must resolve the package's exports field without config changes.)

    Step 4 — Commit:
      git add package.json package-lock.json
      git commit -m "feat(player): install media-chrome@^4.19.2 for custom video player controls"
  </action>

  <verify>
    <automated>
      cd C:\Users\anoop\projects\content-moderation-assessment
      node -e "const p=require('./package.json'); const v=p.dependencies['media-chrome']; if(!v) throw new Error('NOT INSTALLED'); console.log('media-chrome@' + v + ' INSTALLED')"
      npm run build 2>&1 | Select-String -Pattern "error|fail" -Quiet; if ($?) { Write-Output "BUILD FAILED" } else { Write-Output "BUILD OK" }
    </automated>
  </verify>

  <done>
    - media-chrome@^4.19.2 in package.json dependencies
    - npm run build succeeds (no import resolution errors)
    - Commit landed on main
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-01-02: Rename SCREENS.ASSESSMENT_PLACEHOLDER to SCREENS.ASSESSMENT across the codebase</name>
  <files>src/state/screens.js, src/hooks/useAssessmentState.js, tests/state/screens.test.js, tests/hooks/useAssessmentState.test.js, tests/App.integration.test.jsx</files>

  <read_first>
    Read 02-RESEARCH.md §10 "Phase 1 Integration" for the rename plan. Read current src/state/screens.js (6 lines, ASSESSMENT_PLACEHOLDER key). Read src/hooks/useAssessmentState.js (line 76 uses ASSESSMENT_PLACEHOLDER). Read tests/state/screens.test.js (asserts SCREENS keys). Read tests/hooks/useAssessmentState.test.js (asserts ASSESSMENT_PLACEHOLDER screen). Read tests/App.integration.test.jsx (references ASSESSMENT_PLACEHOLDER in integration assertions).
  </read_first>

  <behavior>
    - SCREENS.ASSESSMENT_PLACEHOLDER key renamed to SCREENS.ASSESSMENT everywhere
    - The string value changes from 'ASSESSMENT_PLACEHOLDER' to 'ASSESSMENT'
    - useAssessmentState enterAssessment transition uses updated key
    - All existing tests pass with renamed key
    - Zero user-facing behavior change — just a rename
    - ASSESSMENT_PLACEHOLDER string does not appear in any source or test file after rename
  </behavior>

  <action>
    RED: Run the tests first to establish baseline (they should pass on ASSESSMENT_PLACEHOLDER):
      npm test -- --run

    If any test file does NOT reference ASSESSMENT_PLACEHOLDER, note it — no change needed.

    GREEN — three files to edit:

    (1) src/state/screens.js:
      Change:
        ASSESSMENT_PLACEHOLDER: 'ASSESSMENT_PLACEHOLDER',
      To:
        ASSESSMENT: 'ASSESSMENT',

    (2) src/hooks/useAssessmentState.js line 76:
      Change:
        setScreen(SCREENS.ASSESSMENT_PLACEHOLDER)
      To:
        setScreen(SCREENS.ASSESSMENT)

    (3) Find-and-replace in test files:
      Search for all occurrences of 'ASSESSMENT_PLACEHOLDER' in:
        - tests/state/screens.test.js
        - tests/hooks/useAssessmentState.test.js
        - tests/App.integration.test.jsx
      Replace each occurrence with 'ASSESSMENT'.
      Also replace any assertion expecting the string value 'ASSESSMENT_PLACEHOLDER' with 'ASSESSMENT'.

    Run npm test — all tests must pass with the renamed key.

    Run a grep to confirm zero remaining ASSESSMENT_PLACEHOLDER references:
      rg "ASSESSMENT_PLACEHOLDER" src/ tests/ --no-heading
      (must return zero matches)

    Commit:
      git add src/state/screens.js src/hooks/useAssessmentState.js tests/state/screens.test.js tests/hooks/useAssessmentState.test.js tests/App.integration.test.jsx
      git commit -m "refactor(screens): rename ASSESSMENT_PLACEHOLDER to ASSESSMENT for Phase 2 video player slot"
  </action>

  <verify>
    <automated>
      cd C:\Users\anoop\projects\content-moderation-assessment
      npm test -- --run
      rg "ASSESSMENT_PLACEHOLDER" src/ tests/ --no-heading
      (must print nothing — zero matches)
    </automated>
  </verify>

  <done>
    - src/state/screens.js exports SCREENS.ASSESSMENT (no PLACEHOLDER suffix)
    - useAssessmentState enterAssessment transitions to SCREENS.ASSESSMENT
    - All test assertions use 'ASSESSMENT' not 'ASSESSMENT_PLACEHOLDER'
    - npm test full-suite green
    - Zero remaining ASSESSMENT_PLACEHOLDER references in the repo
    - Commit landed on main
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-01-03: Create playlist.json v1 with single test video + chapter data</name>
  <files>src/data/playlist.json, tests/data/playlist.test.js</files>

  <read_first>
    Read 02-RESEARCH.md §5 "playlist.json" for the URL structure and §3 "Chapter Data Source" for the chapter-authoring pattern. Read ARCHITECTURE.md Pattern 4 (data bundled into src/, not public/) and Anti-Pattern 4 (no runtime fetch). Read docs/brand-guardrails.md to confirm no forbidden strings in drafted content.
  </read_first>

  <behavior>
    - playlist.json lives in src/data/ (bundled by Vite, not publicly fetchable)
    - Validated shape: root has "videos" array with exactly 1 entry for Phase 2
    - Each video has: id, title, srcUrl, spriteUrl, thumbsVttUrl, chaptersVttUrl, durationSec, chapters[]
    - Each chapter has: t (number), label (string) — zero-duration markers per RESEARCH §3
    - Duration is a positive number (placeholder: 90)
    - srcUrl is a relative path: "/videos/v01.mp4" (not absolute, not env-conditional for Phase 2)
    - Zero brand-forbidden strings in title, label, or any JSON field
    - Test validates shape AND brand-safety
  </behavior>

  <action>
    RED: Create tests/data/playlist.test.js:
      import playlist from '../../src/data/playlist.json';

      describe('playlist.json v1', () => {
        it('has videos array with exactly 1 entry for Phase 2', () => {
          expect(Array.isArray(playlist.videos)).toBe(true);
          expect(playlist.videos).toHaveLength(1);
        });

        it('video entry has all required fields', () => {
          const v = playlist.videos[0];
          const fields = ['id','title','srcUrl','spriteUrl','thumbsVttUrl','chaptersVttUrl','durationSec','chapters'];
          fields.forEach(f => expect(v).toHaveProperty(f));
        });

        it('srcUrl is a relative path starting with /videos/', () => {
          expect(playlist.videos[0].srcUrl).toMatch(/^\/videos\//);
        });

        it('durationSec is a positive number', () => {
          expect(playlist.videos[0].durationSec).toBeGreaterThan(0);
          expect(playlist.videos[0].durationSec).toBeLessThanOrEqual(120);
        });

        it('chapters array is well-formed', () => {
          const chapters = playlist.videos[0].chapters;
          expect(Array.isArray(chapters)).toBe(true);
          chapters.forEach(ch => {
            expect(typeof ch.t).toBe('number');
            expect(ch.t).toBeGreaterThanOrEqual(0);
            expect(typeof ch.label).toBe('string');
            expect(ch.label.length).toBeGreaterThan(0);
          });
        });

        it('chapters have timestamps within video duration', () => {
          const v = playlist.videos[0];
          v.chapters.forEach(ch => {
            expect(ch.t).toBeLessThanOrEqual(v.durationSec);
          });
        });

        it('contains zero forbidden brand strings in JSON text', () => {
          const text = JSON.stringify(playlist).toLowerCase();
          const forbidden = ['disney','marvel','pixar','star wars','spider-man','avengers','hulk','jedi','yoda'];
          forbidden.forEach(token => {
            expect(text).not.toContain(token);
          });
        });

        it('video title does not exceed 100 characters', () => {
          expect(playlist.videos[0].title.length).toBeLessThanOrEqual(100);
        });
      });

    Run npm test — tests/data/playlist.test.js must fail (file absent).

    GREEN: Create src/data/playlist.json with the exact interface contract from <interfaces>:
      {
        "_note": "Single test video for Phase 2. Remaining 4 videos added in Phase 3 (CONTENT-02).",
        "videos": [
          {
            "id": "v01",
            "title": "Test Video — Content Moderation",
            "srcUrl": "/videos/v01.mp4",
            "spriteUrl": "/sprites/v01_sprite.jpg",
            "thumbsVttUrl": "/vtt/v01.thumbs.vtt",
            "chaptersVttUrl": "/vtt/v01.chapters.vtt",
            "durationSec": 90,
            "chapters": [
              { "t": 25.0, "label": "Watermark overlay visible at bottom-right" },
              { "t": 55.0, "label": "Fictional brand logo appears" },
              { "t": 78.0, "label": "Text overlay with counterfeit claim" }
            ]
          }
        ],
        "_cdnDocs": "srcUrl paths are relative to Pages root. Phase 6 makes them env-conditional per CONTENT-03."
      }

    Run npm test — all tests pass including new playlist.test.js.

    Commit:
      git add src/data/playlist.json tests/data/playlist.test.js
      git commit -m "feat(data): add playlist.json v1 with single test video entry and chapter data"
  </action>

  <verify>
    <automated>
      cd C:\Users\anoop\projects\content-moderation-assessment
      npm test -- --run
      node -e "const p=require('./src/data/playlist.json'); console.log('videos:', p.videos.length, '| chapters:', p.videos[0].chapters.length)"
    </automated>
  </verify>

  <done>
    - src/data/playlist.json exists with 1 video entry matching the interface contract
    - tests/data/playlist.test.js validates shape + brand-safety (8 test cases pass)
    - npm test full-suite green
    - Zero brand-forbidden strings in playlist.json
    - Commit landed on main
  </done>
</task>

<task type="auto">
  <name>Task 02-01-04: Add .gitattributes for sprite LFS tracking + verify LFS workspace</name>
  <files>.gitattributes</files>

  <read_first>
    Read current .gitattributes (already tracks *.mp4 from Phase 0). Read 02-RESEARCH.md §4 ".gitattributes Updates" for the sprite.jpg line. Read PITFALLS.md Pitfall 2 for LFS bandwidth context.
  </read_first>

  <behavior>
    - Add `public/sprites/*.jpg filter=lfs diff=lfs merge=lfs -text` to .gitattributes
    - Existing video LFS tracking unchanged
    - Verify LFS is configured correctly in the workspace
  </behavior>

  <action>
    Step 1 — Append to .gitattributes:
      After the existing `public/videos/*.mp4  filter=lfs diff=lfs merge=lfs -text` line, add:
      ```
      public/sprites/*.jpg   filter=lfs diff=lfs merge=lfs -text
      ```

    Step 2 — Verify LFS state:
      git lfs track
      (must show both public/videos/*.mp4 and public/sprites/*.jpg)
      git lfs ls-files
      (must show existing placeholder.mp4 as LFS-tracked)

    Step 3 — Commit:
      git add .gitattributes
      git commit -m "chore(lfs): add sprite JPEG LFS tracking to .gitattributes"
  </action>

  <verify>
    <automated>
      cd C:\Users\anoop\projects\content-moderation-assessment
      git lfs track
    </automated>
  </verify>

  <done>
    - .gitattributes tracks public/sprites/*.jpg via LFS
    - git lfs track output confirms both video + sprite patterns
    - Commit landed on main
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → package.json | media-chrome package download is trusted (Mux-maintained, verified in RESEARCH.md) |
| playlist.json → VideoPlayerScreen (Plan 02) | Data-driven video config; no user input yet, but shapes must be validated |
| SCREENS enum → App.jsx screen switch | String enum drives component selection; no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | npm supply chain (media-chrome) | mitigate | media-chrome@4.19.2 researched and verified (RESEARCH.md §1); Mux-maintained package with no postinstall scripts; GitHub publishes provenance |
| T-02-02 | Tampering | playlist.json shape corruption | mitigate | Unit test validates shape at build time (playlist.test.js); runtime validation added in Plan 02 via Zod or manual schema guard |
| T-02-03 | Information disclosure | band-guard scan on playlist.json | mitigate | Test asserts zero forbidden tokens; pre-commit hook + CI grep gate apply to this file |
| T-02-04 | Tampering | SCREENS enum mutation | mitigate | Object.freeze prevents runtime mutation; test asserts Object.isFrozen |
</threat_model>

<nyquist_validation>
Observable signals that verify Wave 1 landed correctly:

1. **Signal:** `npm test -- --run` passes all tests (Phase 0 + Phase 1 + new playlist.test.js). **Verify:** run command, check for zero failures.
2. **Signal:** `rg "ASSESSMENT_PLACEHOLDER" src/ tests/` returns zero matches. **Verify:** run search.
3. **Signal:** `node -e "require('media-chrome')"` works without error. **Verify:** CommonJS resolution succeeds.
4. **Signal:** `node -e "const p=require('./src/data/playlist.json'); console.log(p.videos.length===1 && p.videos[0].chapters.length===3)"` prints `true`. **Verify:** playlist shape.
5. **Signal:** `git lfs track | Select-String "sprites"` returns a matching line. **Verify:** LFS tracking configured.
6. **Signal:** `npm run build` succeeds without errors. **Verify:** Vite build green.
7. **Signal:** Four atomic commits landed with conventional messages. **Verify:** `git log --oneline -n 4`.
</nyquist_validation>

<risks>
Wave-specific risks:

1. **media-chrome React wrapper API may change patch versions.** media-chrome@^4.19.2 uses semver; ^ allows minor/patch bumps. Mitigation: lock file freezes exact version; CI reinstalls from lockfile.
2. **ASSESSMENT_PLACEHOLDER grep missed a reference.** If a Phase 1 file imports the old key and we miss it during rename, tests will catch it (import error). Mitigation: npm test is the guard — any missed reference → test failure or build failure.
3. **playlist.json chapter labels in brand-safety gray zone.** If a label accidentally names a real brand, the brand-guard pre-commit hook blocks the commit. Mitigation: labels are generic (watermark, logo, text overlay); no proper nouns.
4. **playlist.json imported by Vite causes circular dependency if it imports anything.** It must be a plain JSON object — no dynamic imports, no top-level await. Mitigation: JSON is static data; tests verify shape.
</risks>

<verification>
Wave 1 complete when ALL of the following are TRUE:
1. `npm test -- --run` prints zero failures.
2. `npm run build` succeeds.
3. `rg "ASSESSMENT_PLACEHOLDER" src/ tests/ --no-heading` returns zero matches.
4. media-chrome@^4.19.2 in package.json dependencies, importable.
5. src/data/playlist.json exists with 1 video, 3 chapters, valid shape.
6. tests/data/playlist.test.js passes all 8 assertions.
7. .gitattributes includes public/sprites/*.jpg LFS tracking.
8. Brand-guard pre-commit hook passed on all commits.
9. 4 atomic commits on main with conventional-commit messages.
</verification>

<success_criteria>
- PLAY-09 (partial — dependency installed): media-chrome installed and importable
- PLAY-07 (partial — paths ready): playlist.json srcUrl paths established for Plan 02 wiring
- CONTENT-05 (partial — chapter data): Chapter markers defined in playlist.json (3 chapters, timestamps + labels)
- CONTENT-06 (partial — sprite/thumbs paths): Sprite/vtt URL paths established in playlist.json for Plan 03 generation
- Foundation: SCREENS.ASSESSMENT key ready for VideoPlayerScreen slot (Plan 02)
- Foundation: All existing Phase 1 tests still pass after rename migration
</success_criteria>

<output>
Create .planning/phases/02-custom-video-player-media-chrome/02-01-SUMMARY.md when Wave 1 is complete.
</output>
