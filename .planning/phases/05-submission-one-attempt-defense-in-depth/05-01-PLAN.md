---
phase: 05-submission-one-attempt-defense-in-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/submission.js
  - src/utils/dedup.js
  - src/state/screens.js
  - src/components/SubmitDoneScreen.jsx
  - src/components/scoreboard/SubmissionOverlay.jsx
  - src/components/scoreboard/ScoreboardScreen.jsx
  - src/App.jsx
  - src/index.css
  - tests/utils/submission.test.js
  - tests/utils/dedup.test.js
  - tests/components/SubmitDoneScreen.test.jsx
  - tests/state/screens.test.js
  - tests/components/scoreboard/ScoreboardScreen.test.jsx
  - .env.example
autonomous: true
requirements:
  - SUBMIT-01
  - SUBMIT-02
  - SUBMIT-08
  - ATTEMPT-01
  - ATTEMPT-04

must_haves:
  truths:
    - "Scoreboard renders fully with all sections before any submission activity starts"
    - "Submission overlay appears with spinner and 'Submitting your results' text after a 500ms delay"
    - "On backend 200, overlay changes to success banner 'Results sent' for exactly 5 seconds then auto-transitions to SubmitDoneScreen"
    - "SubmitDoneScreen displays 'Thank you, {name}' heading and generic confirmation body text; terminal screen with no back navigation"
    - "localStorage flag cma_attempt_v1 is written inside the backend-200 handler before screen transition (ATTEMPT-01)"
    - "sessionStorage identity cma_identity_v1 is cleared on SubmitDoneScreen mount (D-P5-05)"
    - "SCREENS enum has exactly 7 keys including SUBMIT_DONE"
    - "emailHash computed via Web Crypto SHA-256 of normalized email (ATTEMPT-04)"
  artifacts:
    - path: "src/utils/submission.js"
      provides: "buildSubmissionPayload, buildHmac functions"
      exports:
        - "buildSubmissionPayload"
        - "buildHmac"
    - path: "src/utils/dedup.js"
      provides: "normalizeEmail + hashEmail with Web Crypto SHA-256"
      exports:
        - "normalizeEmail"
        - "hashEmail"
    - path: "src/components/SubmitDoneScreen.jsx"
      provides: "Terminal thank-you screen"
      min_lines: 25
    - path: "src/components/scoreboard/SubmissionOverlay.jsx"
      provides: "Overlay component with idle/submitting/success states"
      min_lines: 45
  key_links:
    - from: "src/components/scoreboard/ScoreboardScreen.jsx"
      to: "src/utils/submission.js"
      via: "imports buildSubmissionPayload and buildHmac — assembles payload after scoreboard renders"
      pattern: "from.*submission"
    - from: "src/components/scoreboard/ScoreboardScreen.jsx"
      to: "src/components/scoreboard/SubmissionOverlay.jsx"
      via: "conditionally renders SubmissionOverlay when submissionPhase !== 'idle'"
      pattern: "SubmissionOverlay"
    - from: "src/App.jsx"
      to: "src/components/SubmitDoneScreen.jsx"
      via: "case SCREENS.SUBMIT_DONE renders SubmitDoneScreen with identity prop"
      pattern: "SUBMIT_DONE"
    - from: "src/components/scoreboard/ScoreboardScreen.jsx"
      to: "src/hooks/useOneAttemptGuard.js"
      via: "calls markAttempted with { emailHash, submissionId } on backend 200"
      pattern: "markAttempted"
---

## Phase Goal

**As a** candidate who has completed the assessment, **I want to** submit my results to the hiring team and see confirmation they were sent, **so that** my assessment is recorded and I know the process is complete.

---

<objective>
Deliver the client-side submission module and the UI integration for the happy path: scoreboard renders → submission fires → success → thank-you screen. After this plan, a candidate can see the full submission flow in the UI (submitting overlay, success banner, SubmitDoneScreen) with payload assembly, Web Crypto SHA-256 email hashing, HMAC signing, localStorage flag writing, and sessionStorage identity clearing all functioning. The actual backend (Apps Script doPost) is stubbed via a mockable endpoint — the client-side pipeline is real but hits a simulated success response.

Purpose: Establish the client-side submission core and UI contract that Plan 05-02 (backend) and Plan 05-03 (fallback) will build on.
Output: `src/utils/submission.js`, rewritten `src/utils/dedup.js`, `SubmissionOverlay.jsx`, `SubmitDoneScreen.jsx`, updated `ScoreboardScreen.jsx`, updated `App.jsx`, updated `SCREENS` enum, new overlay CSS tokens, test files for all new modules
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-submission-one-attempt-defense-in-depth/05-CONTEXT.md
@.planning/phases/05-submission-one-attempt-defense-in-depth/05-RESEARCH.md
@.planning/phases/05-submission-one-attempt-defense-in-depth/05-UI-SPEC.md

# Existing source (patterns + integration points)
@src/App.jsx
@src/state/screens.js
@src/hooks/useOneAttemptGuard.js
@src/hooks/useAssessmentState.js
@src/hooks/useScoreboard.js
@src/stores/useAssessmentStore.js
@src/utils/dedup.js
@src/data/taxonomy.json
@src/data/answerKeys.json
@src/index.css

# Existing tests (patterns to follow)
@tests/state/screens.test.js
@tests/utils/dedup.test.js
@tests/components/scoreboard/ScoreboardScreen.test.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test Scaffold — Failing Tests for Submission Module, Dedup, SubmitDoneScreen, and Screens</name>
  <read_first>tests/state/screens.test.js, tests/utils/dedup.test.js</read_first>
  <files>tests/utils/submission.test.js, tests/utils/dedup.test.js, tests/components/SubmitDoneScreen.test.jsx, tests/state/screens.test.js</files>
  <action>
    Create failing test files that define the contracts before implementation.

    1. **tests/utils/submission.test.js** (new) — Cover:
       - `buildSubmissionPayload({ identity, answers, scores, competency, hmac })` returns object with all SUBMIT-02 fields: name, email, emailHash, answers[], scores{}, competency, strengthsWeaknesses, timeToCompleteMs, answerKeyVersion, taxonomyVersion, sessionStartedAt, sessionEndedAt, userAgent, screenResolution, hmac
       - Payload uses `null` for missing optional fields, never `undefined` (PITFALL: JSON.stringify drops undefined)
       - answers[] maps each store answer to { videoId, selectedL1, selectedL2, verdict, timeSpentMs, timedOut }
       - timeToCompleteMs sums all answer.timeSpentMs
       - Empty answers produces payload with empty answers[] and totalTimeMs=0
     - All tests MUST fail on first run (imports reference non-existent modules). Use `vi.mock` for crypto.subtle in submission tests. Use `vi.fn().mockResolvedValue()` for `hashEmail`.

    2. **tests/utils/dedup.test.js** (rewrite existing) — Cover:
       - `normalizeEmail` trims whitespace and lowercases
       - Gmail-family: strips dots from local part, strips +alias suffix
       - Non-Gmail: lowercase only, dots preserved
       - Null/undefined/empty string produce empty string without throwing
       - `hashEmail` returns a 64-char hex string (SHA-256)
       - `hashEmail` is async (returns Promise)
       - Same input produces same hash (deterministic)
       - Different inputs produce different hashes
       - Null/undefined input → hashEmail returns empty string hex or rejects gracefully (wrap in try/catch)
     - The existing dedup.test.js has Phase 1 placeholder tests (trim+lowercase only). Replace all test cases with the above. Tests MUST fail because dedup.js still has placeholder implementation.

    3. **tests/components/SubmitDoneScreen.test.jsx** (new) — Cover:
       - Renders heading "Thank you, {name}" where name comes from identity.name prop
       - Renders body text containing "Your assessment results have been sent"
       - Body text includes "contact your recruiter"
       - Has no buttons, no links, no navigation elements (terminal screen)
       - Clears sessionStorage key `cma_identity_v1` on mount (mock sessionStorage.removeItem)
       - Renders without crashing when identity.name is empty string
     - Tests MUST fail (component does not exist yet).

    4. **tests/state/screens.test.js** (modify existing) — Update:
       - Change `toHaveLength(6)` → `toHaveLength(7)`
       - Add `expect(SCREENS).toHaveProperty('SUBMIT_DONE')` to required keys test
       - Add `expect(SCREENS.SUBMIT_DONE).toBe('SUBMIT_DONE')` to key-value test
     - This test MUST fail on first run (SUBMIT_DONE does not exist in SCREENS yet).
  </action>
  <verify>
    <automated>npx vitest run tests/utils/submission.test.js tests/utils/dedup.test.js tests/components/SubmitDoneScreen.test.jsx tests/state/screens.test.js 2>&1 | grep -c 'FAIL' | grep -E '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - All 4 test files exist and compile (imports may fail on non-existent exports but test structure is valid)
    - tests/utils/submission.test.js has tests for buildSubmissionPayload covering at least 5 SUBMIT-02 fields
    - tests/utils/dedup.test.js covers normalizeEmail Gmail dots/+alias + hashEmail async SHA-256
    - tests/components/SubmitDoneScreen.test.jsx covers heading, body, no-navigation, sessionStorage clear
    - tests/state/screens.test.js expects 7 keys and has SUBMIT_DONE property check
    - All tests FAIL (red state) — implementation does not exist yet
    - Commit: `test(05-01): add failing tests for submission module, dedup, SubmitDoneScreen, screens`
  </acceptance_criteria>
  <done>Failing test suite exists. Running the 4 test files produces FAIL results (not PASS). Ready for implementation in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client Submission Pipeline — submission.js, dedup.js rewrite, buildHmac</name>
  <read_first>tests/utils/submission.test.js, tests/utils/dedup.test.js, src/utils/dedup.js, src/data/taxonomy.json, src/data/answerKeys.json</read_first>
  <files>src/utils/submission.js, src/utils/dedup.js</files>
  <behavior>
    - buildSubmissionPayload assembles SUBMIT-02-compliant JSON with all fields present, null for missing, never undefined
    - hashEmail produces 64-char hex via crypto.subtle.digest('SHA-256')
    - normalizeEmail handles Gmail dots/+alias normalization per D-P5-15
    - buildHmac produces hex HMAC-SHA256 via crypto.subtle.sign over payload JSON (hmac field excluded)
  </behavior>
  <action>
    Implement the client-side submission pipeline per D-P5-13, D-P5-14, D-P5-15.

    1. **src/utils/dedup.js** (rewrite) — Replace Phase 1 placeholder:
    ```
    export function normalizeEmail(email) {
      let e = (email ?? '').trim().toLowerCase();
      // Gmail-family: strip dots from local part, strip +alias suffix
      if (e.includes('@gmail.com') || e.includes('@googlemail.com')) {
        const [local, domain] = e.split('@');
        const stripped = local.replace(/\./g, '').replace(/\+.*$/, '');
        e = `${stripped}@${domain}`;
      }
      return e;
    }

    export async function hashEmail(email) {
      const normalized = normalizeEmail(email);
      if (!normalized) return '';
      const encoder = new TextEncoder();
      const data = encoder.encode(normalized);
      const hashBuffer = await crypto.subtle.digest('SHA-256', data);
      return Array.from(new Uint8Array(hashBuffer))
        .map(b => b.toString(16).padStart(2, '0')).join('');
    }
    ```
    - hashEmail is async (returns Promise). Callers must `await`.
    - Wrap `crypto.subtle.digest` in try/catch — if it throws (non-HTTPS origin), return empty string '' as graceful degradation (the assessment requires modern browser + HTTPS via GitHub Pages per RESEARCH.md Assumption A1).

    2. **src/utils/submission.js** (new) — Pure functions, no React deps:
    ```
    export function buildSubmissionPayload({ identity, answers, scores, competency, taxonomyVersion, hmac }) {
      const totalTimeMs = answers.reduce((sum, a) => sum + (a.timeSpentMs || 0), 0);
      return {
        name: identity.name ?? null,
        email: identity.email ?? null,
        emailHash: '', // filled by caller after async hashEmail
        answers: answers.map(a => ({
          videoId: a.videoId ?? '',
          selectedL1: a.selectedL1 ?? [],
          selectedL2: a.selectedL2 ?? [],
          verdict: a.verdict ?? null,
          timeSpentMs: a.timeSpentMs ?? 0,
          timedOut: a.timedOut ?? false,
        })),
        scores: {
          overallPct: scores.overallPct ?? null,
          perVideo: scores.perVideo ?? [],
          perL1Accuracy: scores.perL1Accuracy ?? {},
        },
        competency: competency.title ?? null,
        strengthsWeaknesses: competency.strengthsWeaknesses ?? null,
        timeToCompleteMs: totalTimeMs,
        answerKeyVersion: scores.answerKeyVersion ?? '',
        taxonomyVersion: taxonomyVersion ?? '',
        sessionStartedAt: identity.startedAt ?? null,
        sessionEndedAt: new Date().toISOString(),
        userAgent: (typeof navigator !== 'undefined' ? navigator.userAgent : '') ?? '',
        screenResolution: (typeof window !== 'undefined' ? `${window.screen.width}x${window.screen.height}` : '') ?? '',
        hmac: hmac ?? '',
      };
    }
    ```

    ```
    export async function buildHmac(payload, secretHex) {
      if (!secretHex) return '';
      const secretBytes = new Uint8Array(
        secretHex.match(/.{1,2}/g).map(b => parseInt(b, 16))
      );
      const key = await crypto.subtle.importKey(
        'raw', secretBytes,
        { name: 'HMAC', hash: 'SHA-256' },
        false, ['sign']
      );
      const { hmac, ...payloadWithoutHmac } = payload;
      const encoder = new TextEncoder();
      const signature = await crypto.subtle.sign('HMAC', key, encoder.encode(JSON.stringify(payloadWithoutHmac)));
      return Array.from(new Uint8Array(signature))
        .map(b => b.toString(16).padStart(2, '0')).join('');
    }
    ```
    - buildHmac excludes the `hmac` field from signing per PITFALLS.md Pitfall 1 (HMAC mismatch avoidance).
    - HMAC secret sourced from `import.meta.env.VITE_HMAC_SECRET` (build-time injection per D-P5-09).
    - Wrap in try/catch, return empty string on failure.

    Use `npm test -- tests/utils/submission.test.js tests/utils/dedup.test.js` as feedback loop. Implement until all tests pass GREEN (not red). Do not proceed past GREEN without committed tests.
  </action>
  <verify>
    <automated>npx vitest run tests/utils/submission.test.js tests/utils/dedup.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/utils/submission.js` exports `buildSubmissionPayload` and `buildHmac`
    - `src/utils/dedup.js` exports `normalizeEmail` and `hashEmail` (async)
    - `hashEmail('test@gmail.com')` produces 64-char hex (not plain text like old stub)
    - `normalizeEmail('Alice.Doe+test@GMAIL.com')` → `'alicedoe@gmail.com'`
    - `normalizeEmail('alice@outlook.com')` preserves dots → `'alice@outlook.com'`
    - `buildSubmissionPayload` includes all 17 fields from SUBMIT-02 spec; no undefined values in result
    - `buildHmac({ name: 'test', hmac: '' }, 'a1b2...')` returns any hex string (HMAC computation works)
    - All tests in `tests/utils/submission.test.js` and `tests/utils/dedup.test.js` PASS
    - Commit: `feat(05-01): implement client submission pipeline — submission.js + dedup.js Web Crypto rewrite`
  </acceptance_criteria>
  <done>Client submission module works: payload assembly, email normalization + SHA-256 hashing, HMAC signing all test-green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UI Integration — Scoreboard Overlay, SubmitDoneScreen, Screen Enum, App Wiring</name>
  <read_first>src/components/scoreboard/ScoreboardScreen.jsx, src/App.jsx, src/state/screens.js, src/index.css, src/hooks/useOneAttemptGuard.js, src/hooks/useAssessmentState.js, src/hooks/useScoreboard.js, tests/components/SubmitDoneScreen.test.jsx, tests/state/screens.test.js, tests/components/scoreboard/ScoreboardScreen.test.jsx</read_first>
  <files>src/state/screens.js, src/components/SubmitDoneScreen.jsx, src/components/scoreboard/SubmissionOverlay.jsx, src/components/scoreboard/ScoreboardScreen.jsx, src/App.jsx, src/index.css, .env.example, tests/state/screens.test.js, tests/components/scoreboard/ScoreboardScreen.test.jsx</files>
  <behavior>
    - ScoreboardScreen gains submissionPhase state (idle → submitting → success) with 500ms initial delay before auto-submit
    - SubmissionOverlay renders semi-transparent backdrop with centered card showing spinner + text in submitting phase, checkmark + green card in success phase
    - On success phase, 5s setTimeout → goToScreen(SUBMIT_DONE), localStorage flag written via markAttempted
    - SubmitDoneScreen clears sessionStorage identity on mount, renders "Thank you, {name}"
    - SCREENS enum has SUBMIT_DONE: 'SUBMIT_DONE'
    - App.jsx renders SubmitDoneScreen on SCREENS.SUBMIT_DONE
  </behavior>
  <action>
    Wire the submission pipeline into the UI per D-P5-01 through D-P5-06, D-P5-16, and UI-SPEC.md.

    **3a. `src/state/screens.js`** — Add one line per D-P5-16:
    ```
    SUBMIT_DONE: 'SUBMIT_DONE',
    ```
    Insert after SCOREBOARD, before closing `})`. Keep Object.freeze intact.

    **3b. `src/components/SubmitDoneScreen.jsx`** (new) — Per UI-SPEC §Component Inventory #2:
    - Props: `{ identity: { name } }`
    - On mount: clear `sessionStorage.removeItem('cma_identity_v1')` per D-P5-05. Wrap in try/catch (sessionStorage may be blocked).
    - Render: `.cma-screen` wrapper, centered heading "Thank you, {name}" (24px/700), body paragraph with exact copy from UI-SPEC: "Your assessment results have been sent to the hiring team. If you have questions about next steps, contact your recruiter."
    - No buttons, no links, no navigation. Terminal screen.
    - If identity is null (defensive edge case — screen only reachable after success), render heading "Thank you" without name.

    **3c. `src/components/scoreboard/SubmissionOverlay.jsx`** (new) — Per UI-SPEC §Component Inventory #1:
    - Props: `{ phase, attempt, totalAttempts, onRetry }`
    - `phase = 'idle'`: return null (not rendered)
    - `phase = 'submitting'`: Full-screen fixed overlay (`position: fixed; inset: 0; z-index: 100; background: rgba(10,11,20,0.85)`) with centered card (max-width 400px, bg-elevated, 8px radius, border-subtle). Card contains: CSS spinner (20px, accent border-top, infinite spin), heading "Submitting your results" (24px/700), body "Please wait while we send your assessment data." (14px/400, text-secondary), attempt counter "Attempt {N} of {total}" (14px, text-secondary). Use CSS classes: `cma-overlay`, `cma-overlay-card`, `cma-spinner`.
    - `phase = 'success'`: Same overlay backdrop. Card background tinted with `rgba(16,185,129,0.1)`. Checkmark (Unicode ✓, 48px, --cma-success color). Heading "Results sent" (24px/700, --cma-success). Body "Your assessment results have been sent to the hiring team." (16px/400). No spinner, no buttons.
    - Do NOT implement error phase in this task (that is Plan 05-02). Error phase renders null for now.

    **3d. `src/index.css`** — Add overlay tokens to `:root` per UI-SPEC §Color:
    ```
    --cma-overlay-bg: rgba(10, 11, 20, 0.85);
    --cma-overlay-card-bg: var(--cma-bg-elevated);
    --cma-spinner-color: var(--cma-accent);
    ```
    Add spinner + overlay CSS at end of file per UI-SPEC spinner + overlay card CSS snippets (lines 153-181 of UI-SPEC). Use `.cma-spinner`, `.cma-overlay`, `.cma-overlay-card`, `@keyframes cma-spin`, `@keyframes cma-fade-in` as specified.

    **3e. `src/components/scoreboard/ScoreboardScreen.jsx`** (modify) — Per D-P5-01, D-P5-02, D-P5-06:
    - Add imports: `useState, useEffect, useCallback, useRef` from React; `hashEmail` from `../../utils/dedup.js`; `buildSubmissionPayload, buildHmac` from `../../utils/submission.js`; `SubmissionOverlay` from `./SubmissionOverlay.jsx`; `SCREENS` from `../../state/screens.js`; `useOneAttemptGuard` from `../../hooks/useOneAttemptGuard.js`; `useAssessmentState` from `../../hooks/useAssessmentState.js`; `taxonomy` from `../../data/taxonomy.json`
    - Add new props: the component needs `goToScreen` function. Import `useAssessmentState` hook inside the component.
    - Add state: `submissionPhase` ('idle'|'submitting'|'success'), `attemptCount` (number, default 0)
    - Compute `scoreboardData` snapshot from `useScoreboard()` before submission (memoized with useRef so it survives re-renders during submission).
    - Add `useEffect` with 500ms delay (setTimeout) on mount after GSAP stagger: starts submission. Only fires once (empty deps + guard flag).
    - Submission logic for happy path (this plan):
      1. Set `submissionPhase = 'submitting'`, `attemptCount = 1`
      2. `await hashEmail(state.identity.email)` → get emailHash
      3. Assemble payload: `buildSubmissionPayload({ identity: state.identity, answers: storeAnswers, scores: scoring, competency: { title: scoring.tier, strengthsWeaknesses: scoring.competencyText }, taxonomyVersion: taxonomy.version, hmac: '' })`
      4. Set `payload.emailHash = emailHash`
      5. `payload.hmac = await buildHmac(payload, import.meta.env.VITE_HMAC_SECRET || '')`
      6. POST via fetch to `import.meta.env.VITE_APPS_SCRIPT_URL`. For this plan, the endpoint may not exist yet — use a fallback: if VITE_APPS_SCRIPT_URL is empty or fetch throws, simulate success to allow UI testing. Wrap in try/catch.
      7. On success (200): `guard.markAttempted({ emailHash, submissionId: data.id || 'simulated' })` — this writes localStorage. Then set `submissionPhase = 'success'`.
      8. On success: start 5s setTimeout → call `goToScreen(SCREENS.SUBMIT_DONE)`.
      9. On failure: for now, set `submissionPhase = 'success'` so the UI flow is testable (real retry logic in Plan 05-02).
    - Pass `goToScreen` prop from ScoreboardScreen's parent. ScoreboardScreen imports `useAssessmentState` to call `goToScreen` directly.
    - Render `<SubmissionOverlay phase={submissionPhase} attempt={attemptCount} totalAttempts={1} />` after the scoreboard content when phase !== 'idle'.
    - The GSAP `useGSAP` stagger must still run. Overlay renders on top of the animated scoreboard.

    **3f. `src/App.jsx`** (modify) — Per D-P5-16:
    - Add import: `import SubmitDoneScreen from './components/SubmitDoneScreen.jsx'`
    - In `renderScreen` switch, add case before default:
    ```
    case SCREENS.SUBMIT_DONE:
      return <SubmitDoneScreen identity={state.identity} />
    ```
    - Update ScoreboardScreen case to pass `goToScreen`: `return <ScoreboardScreen goToScreen={state.goToScreen} />` OR let ScoreboardScreen import the hook directly (preferred — less prop drilling, consistent with existing pattern where RunnerScreen calls `onComplete`).

    **3g. `.env.example`** (new) — Template with placeholder values:
    ```
    VITE_HMAC_SECRET=generate_with_node_-e_console.log_crypto.randomBytes_32_toString_hex
    VITE_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
    ```
    Document the HMAC key generation command in a comment: `# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`

    **3h. Test updates:**
    - `tests/state/screens.test.js`: The changes from Task 1 pass now (7 keys, SUBMIT_DONE present).
    - `tests/components/SubmitDoneScreen.test.jsx`: Passes now (component exists).
    - `tests/components/scoreboard/ScoreboardScreen.test.jsx`: Add tests for submission overlay rendering (mock fetch, mock crypto.subtle). Test that overlay appears after mount (use `waitFor` with >500ms timeout). Test success state transition. Mock `useOneAttemptGuard` and verify `markAttempted` called.

    Run `npx vitest run` after all changes. Fix any unexpected test failures in unrelated tests (screens.test.js count bump from 6→7 may affect App.integration.test.jsx — check and update if needed).
  </action>
  <verify>
    <automated>npx vitest run tests/state/screens.test.js tests/components/SubmitDoneScreen.test.jsx tests/components/scoreboard/ScoreboardScreen.test.jsx tests/utils/submission.test.js tests/utils/dedup.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `SCREENS.SUBMIT_DONE` exists and equals `'SUBMIT_DONE'`
    - `screens.test.js` expects 7 keys and all pass
    - `SubmitDoneScreen` renders heading "Thank you, {name}" when given identity
    - `SubmitDoneScreen` renders confirmation body text from UI-SPEC
    - `SubmitDoneScreen` clears `cma_identity_v1` from sessionStorage on mount
    - `SubmitDoneScreen` has no buttons or navigation elements
    - `SubmissionOverlay` renders null when phase='idle'
    - `SubmissionOverlay` renders spinner + "Submitting your results" when phase='submitting'
    - `SubmissionOverlay` renders checkmark ✓ + "Results sent" when phase='success'
    - `ScoreboardScreen` fires submission after ~500ms mount delay
    - `ScoreboardScreen` calls `markAttempted` from `useOneAttemptGuard` on success state
    - `App.jsx` renders `SubmitDoneScreen` for `SCREENS.SUBMIT_DONE`
    - `.env.example` exists with VITE_HMAC_SECRET and VITE_APPS_SCRIPT_URL placeholders
    - All test suites PASS
    - Commit: `feat(05-01): wire submission UI — overlay, SubmitDoneScreen, screen enum, App routing`
  </acceptance_criteria>
  <done>Full submission happy path renders: scoreboard → submitting overlay → success banner → SubmitDoneScreen. localStorage flag written, sessionStorage cleared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Apps Script (fetch) | Untrusted user-supplied identity + answers cross trust boundary to server |
| VITE_HMAC_SECRET (build-time) | HMAC shared secret baked into JS bundle — any visitor can extract |
| localStorage (cma_attempt_v1) | Client-side tamperable; server dedup is authoritative gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | src/utils/submission.js buildSubmissionPayload | mitigate | buildSubmissionPayload returns explicit null for missing fields, never undefined — prevents JSON.stringify from silently dropping keys |
| T-05-02 | Information Disclosure | VITE_HMAC_SECRET in JS bundle | accept | Accepted risk per D-P5-09 — same model as bundled answerKeys.json (SCORE-08). Defense-in-depth: server also dedups. HMAC prevents casual replay but not determined extraction. |
| T-05-03 | Repudiation | localStorage cma_attempt_v1 | mitigate | Server-side SHA-256 email dedup (ATTEMPT-03 in Plan 05-02) is authoritative gate. Client localStorage is UX convenience only. |
| T-05-04 | Elevation of Privilege | src/utils/dedup.js normalizeEmail | mitigate | Gmail normalization (+alias/dots stripping) prevents email-variant bypass. Non-Gmail domains: lowercase + trim only per ATTEMPT-04. |
| T-05-05 | Information Disclosure | src/components/SubmitDoneScreen.jsx | mitigate | sessionStorage identity cleared on mount (D-P5-05) — prevents name/email from persisting across sessions. |
| T-05-SC | Tampering | npm install (no new packages in this plan) | accept | No new npm packages installed in this plan. All deps use native Web Crypto + fetch — no supply-chain risk. |
</threat_model>

<verification>
1. `npx vitest run` — all test suites pass, including updated screens count
2. Manual: Start app, complete assessment, observe overlay appears after scoreboard renders, success banner shows, auto-transitions to SubmitDoneScreen
3. Manual: Check localStorage `cma_attempt_v1` is set after success, sessionStorage `cma_identity_v1` is cleared
4. `npx vitest run tests/utils/dedup.test.js` — validate Gmail normalization: `'User.Name+tag@gmail.com'` → hash matches `'username@gmail.com'` hash
</verification>

<success_criteria>
- [ ] SubmitDoneScreen renders terminal thank-you with candidate name and generic confirmation text
- [ ] Submission overlay renders spinner during submitting, checkmark during success, backdrop over scoreboard
- [ ] Scoreboard content is visible underneath semi-transparent overlay
- [ ] SCREENS enum has 7 keys including SUBMIT_DONE
- [ ] buildSubmissionPayload produces complete payload with all SUBMIT-02 fields
- [ ] hashEmail produces 64-char SHA-256 hex (not plaintext like Phase 1 stub)
- [ ] normalizeEmail strips Gmail dots and +aliases correctly
- [ ] buildHmac computes HMAC-SHA256 over payload body excluding hmac field
- [ ] localStorage flag written inside success handler before screen transition
- [ ] sessionStorage identity cleared on SubmitDoneScreen mount
</success_criteria>

<output>
Create `.planning/phases/05-submission-one-attempt-defense-in-depth/05-01-SUMMARY.md` when done
</output>
