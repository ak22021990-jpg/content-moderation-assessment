---
phase: 05-submission-one-attempt-defense-in-depth
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - scripts/apps-script/Code.gs
  - scripts/apps-script/appsscript.json
  - scripts/apps-script/README.md
  - src/utils/submission.js
  - src/components/scoreboard/SubmissionOverlay.jsx
  - src/components/scoreboard/ScoreboardScreen.jsx
  - src/index.css
  - .env.example
  - tests/utils/submission.test.js
  - tests/components/scoreboard/ScoreboardScreen.test.jsx
autonomous: false
requirements:
  - SUBMIT-03
  - SUBMIT-04
  - SUBMIT-05
  - SUBMIT-06
  - SUBMIT-07
  - SUBMIT-09
  - ATTEMPT-03

must_haves:
  truths:
    - "Apps Script doPost validates HMAC token against shared secret before accepting payload (SUBMIT-04)"
    - "Apps Script doPost validates Origin header against configured ALLOWED_ORIGIN (SUBMIT-05)"
    - "Apps Script doPost rate-limits to 3 submissions per IP per minute using CacheService (SUBMIT-06)"
    - "Apps Script doPost scans Sheet column A for duplicate emailHash and rejects with {ok: false, error: 'duplicate'} (ATTEMPT-03)"
    - "Apps Script doPost writes row to Google Sheet with emailHash, name, email, answers, scores, competency, timestamps, UA, resolution and returns {ok: true, id: rowId} (SUBMIT-07)"
    - "submitResults retries on 5xx and network errors with exponential backoff (1s, 3s, 9s) up to 3 attempts (SUBMIT-03)"
    - "Retries are visible: overlay shows attempt counter 'Attempt {N} of 3' updating per attempt (D-P5-10)"
    - "On final failure, overlay shows error card with heading 'Unable to submit your results', retry button, and secondary message 'If the problem persists, contact your recruiter.' (SUBMIT-09, D-P5-11)"
    - "4xx errors surface immediately without retry (D-P5-10)"
    - "Retry button resets attempt counter and re-initiates submission with fresh scoreboardData snapshot (D-P5-12)"
  artifacts:
    - path: "scripts/apps-script/Code.gs"
      provides: "Apps Script doPost webhook handler"
      contains: "function doPost(e)"
      min_lines: 60
    - path: "scripts/apps-script/README.md"
      provides: "Deployment instructions + required Properties keys"
      min_lines: 30
    - path: "src/utils/submission.js"
      provides: "submitResults with exponential backoff (+ existing exports)"
      exports:
        - "buildSubmissionPayload"
        - "buildHmac"
        - "submitResults"
  key_links:
    - from: "src/utils/submission.js submitResults"
      to: "VITE_APPS_SCRIPT_URL"
      via: "fetch POST to Apps Script deployment URL"
      pattern: "fetch.*APPS_SCRIPT_URL"
    - from: "src/components/scoreboard/SubmissionOverlay.jsx"
      to: "src/components/scoreboard/ScoreboardScreen.jsx"
      via: "error phase renders retry button that calls handleRetry callback in ScoreboardScreen"
      pattern: "onRetry"
    - from: "scripts/apps-script/Code.gs"
      to: "PropertiesService.getScriptProperties()"
      via: "reads HMAC_SECRET, ALLOWED_ORIGIN, SHEET_ID, RATE_LIMIT_PER_IP, RATE_LIMIT_WINDOW_SEC from script properties"
      pattern: "getScriptProperties"
---

<objective>
Deliver the Google Apps Script `doPost` webhook backend and wire the client retry logic with error UX. After this plan, the submission pipeline is end-to-end real: client POSTs to Apps Script with HMAC signing, server validates origin + HMAC + rate-limit + deduplication, writes a Google Sheet row, and returns `{ok: true, id}`. Client retries with visible exponential backoff on 5xx/network errors, and shows a structured error overlay with manual retry button on exhaustion.

Purpose: Complete the defense-in-depth server side (ATTEMPT-03 server dedup, SUBMIT-04 HMAC, SUBMIT-05 origin, SUBMIT-06 rate-limit) and the client-side resilience (SUBMIT-03 backoff, SUBMIT-09 failure UX).
Output: `scripts/apps-script/Code.gs`, `scripts/apps-script/README.md`, `submitResults` export in `submission.js`, error state in `SubmissionOverlay.jsx`, updated `ScoreboardScreen.jsx`
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-submission-one-attempt-defense-in-depth/05-CONTEXT.md
@.planning/phases/05-submission-one-attempt-defense-in-depth/05-RESEARCH.md

# Plan 05-01 outputs (types, exports, UI patterns)
@.planning/phases/05-submission-one-attempt-defense-in-depth/05-01-SUMMARY.md
@src/utils/submission.js
@src/components/scoreboard/SubmissionOverlay.jsx
@src/components/scoreboard/ScoreboardScreen.jsx

# Existing patterns
@src/App.jsx
@src/hooks/useOneAttemptGuard.js
@src/hooks/useAssessmentState.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apps Script doPost Webhook + Deployment Documentation</name>
  <read_first>scripts/apps-script/ (check if directory exists; list contents), .planning/phases/05-submission-one-attempt-defense-in-depth/05-RESEARCH.md §"Code Examples: Server: Apps Script doPost", .planning/phases/05-submission-one-attempt-defense-in-depth/05-CONTEXT.md §D-P5-07, D-P5-08, D-P5-09</read_first>
  <files>scripts/apps-script/Code.gs, scripts/apps-script/appsscript.json, scripts/apps-script/README.md, .env.example</files>
  <behavior>
    - doPost validates Origin header, rate-limits via CacheService, validates HMAC, deduplicates by SHA-256 email hash, writes Sheet row, returns JSON
    - All secrets stored in PropertiesService.getScriptProperties(), never hardcoded in Code.gs
    - Rate limit threshold, window, and Sheet ID configurable via script properties
    - README.md documents deployment steps, required properties, endpoint URL format
  </behavior>
  <action>
    Create the Apps Script backend per RESEARCH.md §Code Examples (lines 473-554) and CONTEXT.md D-P5-07/D-P5-08.

    **1a. `scripts/apps-script/Code.gs`** (new) — Full doPost implementation:

    Implement the `doPost(e)` function following RESEARCH.md lines 473-554 exactly, with these additions:

    - Origin check: read `Origin` header from `e.postData.headers?.Origin` (Apps Script passes request headers in postData). Compare against `ALLOWED_ORIGIN` property. If missing or mismatch, return 403-style JSON `{ ok: false, error: 'invalid-origin' }`.
    - Parse JSON body from `e.postData.contents`. Return 400-style JSON on parse failure.
    - Rate limit: use `CacheService.getScriptCache()`. Key format `rl:${ip}` where `ip` comes from `e.queryString` parameter or header. Store integer counter string. TTL = `RATE_LIMIT_WINDOW_SEC`. If count >= `RATE_LIMIT_PER_IP`, return `{ ok: false, error: 'rate-limited' }`.
    - HMAC validation: extract `hmac` field from payload. Strip `,"hmac":"<hex>"` from raw body string `e.postData.contents` using regex (per Pitfall 1 avoidance). Recompute HMAC with `Utilities.computeHmacSha256Signature(bodyWithoutHmac, HMAC_SECRET)`. Convert server HMAC bytes to hex. Compare against client HMAC. Return `{ ok: false, error: 'invalid-hmac' }` on mismatch.
    - Dedup: compute SHA-256 of `payload.emailHash` (client already hashed). Scan Sheet column A for existing hash value. For initial MVP volumes (<1000 rows), use `sheet.getRange('A:A').getValues().flat().filter(Boolean)`. If found, return `{ ok: false, error: 'duplicate' }`.
    - Row write: append row at `sheet.getLastRow() + 1`. Columns: A=emailHash, B=name, C=email, D=answers(JSON), E=scores(JSON), F=competency, G=sessionEndedAt, H=userAgent, I=screenResolution, J=strengthsWeaknesses, K=answerKeyVersion, L=taxonomyVersion, M=sessionStartedAt, N=timeToCompleteMs. **Prefix all user-supplied string values with `'` (single quote) to prevent Google Sheets formula injection** per RESEARCH.md Security Domain §"Formula injection in Sheet".
    - Return: `ContentService.createTextOutput(JSON.stringify({ ok: true, id: String(row) })).setMimeType(ContentService.MimeType.JSON)`.

    Important implementation notes:
    - Use `var` not `const`/`let` if targeting older Apps Script runtime, or use `const`/`let` for V8 runtime (Apps Script uses V8 by default since 2020 — safe to use `const`/`let`).
    - All Properties keys: `HMAC_SECRET`, `SHEET_ID`, `ALLOWED_ORIGIN`, `RATE_LIMIT_PER_IP` (default: '3'), `RATE_LIMIT_WINDOW_SEC` (default: '60').
    - If `SHEET_ID` property is set, use `SpreadsheetApp.openById(SHEET_ID).getActiveSheet()`. Otherwise fall back to `SpreadsheetApp.getActiveSheet()` (for bound scripts).
    - The `Utilities.computeHmacSha256Signature` returns an array of signed bytes. Convert to hex:
      ```
      serverHmac.map(function(b) { return ('0' + (b & 0xFF).toString(16)).slice(-2); }).join('');
      ```
    - Formula injection defense: all values from payload that go into Sheet cells must be prefixed with `'`. Use: `sheet.getRange(row, col).setValue("'" + (value || ''));`

    **1b. `scripts/apps-script/appsscript.json`** (new) — Manifest:
    ```json
    {
      "timeZone": "UTC",
      "dependencies": {},
      "exceptionLogging": "STACKDRIVER",
      "runtimeVersion": "V8",
      "webapp": {
        "executeAs": "USER_DEPLOYING",
        "access": "ANYONE_ANONYMOUS"
      }
    }
    ```

    **1c. `scripts/apps-script/README.md`** (new) — Per D-P5-08:

    Document:
    - **Endpoint URL format**: `https://script.google.com/macros/s/{deploymentId}/exec`
    - **How to deploy**:
      1. Go to [script.google.com](https://script.google.com) → New Project
      2. Copy `Code.gs` and `appsscript.json` contents into the editor
      3. Set Script Properties: File → Project Properties → Script Properties tab
      4. Add rows: `HMAC_SECRET`, `SHEET_ID`, `ALLOWED_ORIGIN`, `RATE_LIMIT_PER_IP`, `RATE_LIMIT_WINDOW_SEC`
      5. Deploy: Deploy → New Deployment → Type: Web App → Execute as: Me → Who has access: Anyone
      6. Copy deployment URL → set as `VITE_APPS_SCRIPT_URL` in `.env`
    - **Required Script Properties** table with key, description, example value
    - **HMAC key generation**: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` — same key must be set in both VITE_HMAC_SECRET (.env) and HMAC_SECRET (Apps Script Properties)
    - **Google Sheet setup**: Column headers row: Email Hash, Name, Email, Answers (JSON), Scores (JSON), Competency, Submitted At, User Agent, Screen Resolution, Strengths/Weaknesses, Answer Key Version, Taxonomy Version, Session Started, Time to Complete (ms)
    - **Testing the webhook**: `curl -X POST -H 'Content-Type: application/json' -d '{"name":"Test","email":"t@t.com","hmac":"test"}' <URL>` — expect 401 (invalid-hmac) until proper HMAC is provided
    - **Alternative: clasp CLI**: Optional — `npx @google/clasp push` if clasp is configured. Not required for Phase 5.
    - **Brand safety reminder** (CC-01): Sheet name and Apps Script project name must be generic — no client brand references.

    **1d. `.env.example`** (modify) — Add to existing:
    ```
    # Rate limit config (used by Apps Script, documented here for visibility)
    # RATE_LIMIT_PER_IP=3
    # RATE_LIMIT_WINDOW_SEC=60
    ```

    **1e. Verify:** Run `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` to generate an HMAC key. Store this as VITE_HMAC_SECRET (for client build) and document that the same key goes into Apps Script Properties `HMAC_SECRET`.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const code = fs.readFileSync('scripts/apps-script/Code.gs','utf8'); const checks = ['function doPost', 'CacheService', 'PropertiesService', 'computeHmacSha256Signature', 'computeDigest', 'ContentService', 'SpreadsheetApp', 'getScriptProperties', 'getScriptCache', 'HMAC_SECRET', 'ALLOWED_ORIGIN', 'SHEET_ID', 'rate-limited', 'duplicate', 'invalid-hmac', 'invalid-origin'].filter(c => code.includes(c)); console.log(checks.length + '/16 checks passed'); if(checks.length !== 16) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/apps-script/Code.gs` exists with `function doPost(e)` containing all 7 steps (origin, parse, rate-limit, HMAC, dedup, write, return)
    - `scripts/apps-script/appsscript.json` exists with V8 runtime and web app config
    - `scripts/apps-script/README.md` exists with deployment steps, properties table, HMAC generation command, Sheet column layout
    - Code.gs references all 5 script properties (HMAC_SECRET, SHEET_ID, ALLOWED_ORIGIN, RATE_LIMIT_PER_IP, RATE_LIMIT_WINDOW_SEC)
    - Code.gs uses `'` prefix for Sheet cell values (formula injection defense)
    - Code.gs uses `CacheService.getScriptCache()` for rate limiting (not ScriptProperties)
    - Code.gs strips hmac field from raw body string before server-side HMAC recomputation (Pitfall 1 avoidance)
    - Code.gs returns JSON responses with `ContentService.MimeType.JSON`
    - README.md documents HMAC key must match between `.env` VITE_HMAC_SECRET and Apps Script HMAC_SECRET
    - Commit: `feat(05-02): implement Apps Script doPost webhook with HMAC, rate-limit, dedup, row write`
  </acceptance_criteria>
  <done>Apps Script doPost code is ready for manual deployment. README.md provides step-by-step deploy instructions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client Retry Logic + Error Overlay UI</name>
  <read_first>src/utils/submission.js, src/components/scoreboard/SubmissionOverlay.jsx, src/components/scoreboard/ScoreboardScreen.jsx, src/index.css, tests/utils/submission.test.js, tests/components/scoreboard/ScoreboardScreen.test.jsx</read_first>
  <files>src/utils/submission.js, src/components/scoreboard/SubmissionOverlay.jsx, src/components/scoreboard/ScoreboardScreen.jsx, src/index.css, tests/utils/submission.test.js, tests/components/scoreboard/ScoreboardScreen.test.jsx</files>
  <behavior>
    - submitResults wraps fetch in for-loop with delays [1000, 3000, 9000], retries on 5xx and network errors, surfaces 4xx immediately
    - submitResults calls onProgress callback with { attempt, total, phase } per RESEARCH.md Pattern 1
    - SubmissionOverlay error phase renders ⚠ warning icon, error heading, body, secondary message, and "Retry Submission" button
    - ScoreboardScreen wires real submitResults instead of simulated success; stores scoreboardData snapshot for reuse across retries
  </behavior>
  <action>
    Add exponential backoff retry and error UX per D-P5-10, D-P5-11, D-P5-12, and RESEARCH.md Pattern 1.

    **2a. `src/utils/submission.js`** (modify) — Add `submitResults` export:

    Append to existing module per RESEARCH.md Pattern 1 (lines 223-267):
    ```
    export async function submitResults({ payload, endpoint, onProgress, maxAttempts = 3 }) {
      const delays = [1000, 3000, 9000]; // 1s, 3s, 9s exponential backoff

      for (let attempt = 0; attempt < maxAttempts; attempt++) {
        onProgress?.({ attempt: attempt + 1, total: maxAttempts, phase: 'submitting' });

        try {
          const response = await fetch(endpoint, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(payload),
          });

          if (response.ok) {
            const data = await response.json();
            onProgress?.({ attempt: attempt + 1, total: maxAttempts, phase: 'success' });
            return { ok: true, id: data.id };
          }

          // 4xx — do not retry (client error: validation, duplicate, rate-limited)
          if (response.status >= 400 && response.status < 500) {
            const data = await response.json().catch(() => ({}));
            onProgress?.({ attempt: attempt + 1, total: maxAttempts, phase: 'error' });
            return { ok: false, error: data.error || `Server rejected: ${response.status}` };
          }

          // 5xx — retry if attempts remain
          if (attempt < maxAttempts - 1) {
            await new Promise(r => setTimeout(r, delays[attempt]));
          }
        } catch (err) {
          // Network error — retry if attempts remain
          if (attempt < maxAttempts - 1) {
            await new Promise(r => setTimeout(r, delays[attempt]));
          } else {
            onProgress?.({ attempt: attempt + 1, total: maxAttempts, phase: 'error' });
            return { ok: false, error: err.message || 'Network error' };
          }
        }
      }

      onProgress?.({ attempt: maxAttempts, total: maxAttempts, phase: 'error' });
      return { ok: false, error: 'All retry attempts exhausted' };
    }
    ```

    **2b. `tests/utils/submission.test.js`** (modify) — Add tests for `submitResults`:
    - Mock global `fetch` with `vi.fn()`. Test scenarios:
      - Success on first attempt (200 response, returns { ok: true, id: '5' })
      - Success on third attempt after two 503 failures
      - 4xx error surfaces immediately without retry (fetch called only once)
      - Network error retries, fails on third attempt (fetch called 3 times)
      - `onProgress` called with correct `{ attempt, total, phase }` on each state
      - Exponential delays between retries (verify setTimeout called with correct ms values — use `vi.useFakeTimers()`)
      - Payload body is JSON-stringified correctly
    - Use `vi.useFakeTimers()` to test delays without real waiting. Use `vi.advanceTimersByTimeAsync()` between attempts.

    **2c. `src/components/scoreboard/SubmissionOverlay.jsx`** (modify) — Add error phase:

    Per UI-SPEC §Component Inventory #1 (error row):
    - Add `onRetry` prop: `{ onRetry: () => void }`
    - Add `totalAttempts` prop (used by both submitting and error phases)
    - `phase = 'error'`: Same overlay backdrop. Card with ⚠ Unicode warning character (48px, --cma-destructive color). Heading "Unable to submit your results" (24px/700, --cma-text-primary). Body "Something went wrong while sending your data. Please try again." (16px/400, --cma-text-secondary). Secondary message "If the problem persists, contact your recruiter." (14px, --cma-text-secondary). "Retry Submission" button — primary CTA styled per UI-SPEC: accent background (#0085FF), 8px border-radius, 700 weight, 1rem font, text-primary color, padding `--cma-space-sm` horizontal `--cma-space-md` vertical, hover: `--cma-accent-hover` background, focus ring `--cma-focus-outline`. Button calls `onRetry()` on click.
    - The error phase has NO auto-dismiss — user must click retry.
    - Card max-width 400px, centered, same card styling as submitting/success phases.

    **2d. `src/index.css`** (modify) — Add error-specific styles:
    - Add `.cma-overlay-btn-retry` class for the retry button with the properties specified above. Use existing `--cma-*` tokens.
    - No additional tokens needed (error uses --cma-destructive and existing tokens).

    **2e. `src/components/scoreboard/ScoreboardScreen.jsx`** (modify) — Wire real retry:

    Replace the simulated success logic from Plan 05-01 with real `submitResults` call:
    - Import `submitResults` from `../../utils/submission.js`
    - Store `scoreboardData` snapshot using `useRef` — computed once on first submission, reused across retries per D-P5-12
    - Create `handleSubmit` async function:
      1. Snapshot scoreboard data if not already captured (useRef guard)
      2. Set `submissionPhase = 'submitting'`, `attemptCount = 1`
      3. Call `await hashEmail(identity.email)` to get emailHash
      4. Assemble payload via `buildSubmissionPayload` + set emailHash + compute HMAC via `buildHmac`
      5. Call `submitResults({ payload, endpoint: import.meta.env.VITE_APPS_SCRIPT_URL, onProgress: (p) => { setAttemptCount(p.attempt); if (p.phase === 'success') setSubmissionPhase('success'); else if (p.phase === 'error') setSubmissionPhase('error'); }, maxAttempts: 3 })`
      6. On result.ok === true: `guard.markAttempted({ emailHash, submissionId: result.id })` → `setSubmissionPhase('success')` → 5s setTimeout → `goToScreen(SCREENS.SUBMIT_DONE)`
      7. On result.ok === false: `setSubmissionPhase('error')` (overlay shows error state)
    - Create `handleRetry` function: resets `attemptCount` to 0, calls `handleSubmit()` fresh
    - The initial `useEffect` (500ms delay) calls `handleSubmit()` instead of inline logic
    - Remove the simulated success code (the try/catch with fallback to success)

    Pass `onRetry={handleRetry}` and `totalAttempts={3}` to `<SubmissionOverlay>`.

    **2f. `tests/components/scoreboard/ScoreboardScreen.test.jsx`** (modify) — Update for real retry:
    - Mock `submitResults` from `../../utils/submission.js` (vi.mock)
    - Test that `submitResults` is called after mount delay
    - Test error phase: mock `submitResults` to return `{ ok: false }`, verify error overlay renders with retry button
    - Test retry: click retry button, verify `submitResults` called again
    - Test attempt counter updates via `onProgress` callback
    - Test success path still works (mock `{ ok: true, id: '1' }`)
    - Test that `markAttempted` is called on success with correct emailHash and submissionId
  </action>
  <verify>
    <automated>npx vitest run tests/utils/submission.test.js tests/components/scoreboard/ScoreboardScreen.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `submitResults` exported from `src/utils/submission.js`
    - `submitResults` retries on 5xx (status 500-599) up to 3 times with 1s/3s/9s delays
    - `submitResults` retries on network errors (fetch throws) up to 3 times
    - `submitResults` does NOT retry on 4xx — returns `{ ok: false }` immediately
    - `onProgress` fires with `{ attempt, total, phase }` on each attempt
    - `SubmissionOverlay` renders error card with ⚠ icon, heading, body, secondary message, retry button in `phase='error'`
    - Retry button calls `onRetry` prop on click
    - `ScoreboardScreen` uses `submitResults` (not simulated success)
    - `ScoreboardScreen` scoreboardData snapshot reused across retries (useRef)
    - `handleRetry` resets attempt counter and re-initiates submission
    - All tests pass GREEN
    - Commit: `feat(05-02): add exponential backoff retry + error overlay with manual retry button`
  </acceptance_criteria>
  <done>Submission retries with visible exponential backoff. Error overlay shows retry button. Apps Script doPost ready for manual deploy.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Deploy Apps Script and Verify End-to-End Submission</name>
  <files>scripts/apps-script/Code.gs, scripts/apps-script/README.md, .env</files>
  <action>This is a human-action checkpoint — the agent cannot automate Apps Script deployment. Follow the how-to-verify steps below to deploy the Apps Script backend and verify the full submission pipeline end-to-end.</action>
  <what-built>Apps Script doPost webhook and client-side retry pipeline with HMAC, rate-limit, dedup, and error handling</what-built>
  <how-to-verify>
    **Prerequisites:**
    1. The HMAC secret is generated: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
    2. A Google Sheet has been created for submission data

    **Steps:**
    1. Follow `scripts/apps-script/README.md` deployment instructions:
       - Create new Apps Script project at script.google.com
       - Copy `Code.gs` + `appsscript.json` contents
       - Set all 5 Script Properties (HMAC_SECRET, SHEET_ID, ALLOWED_ORIGIN, RATE_LIMIT_PER_IP, RATE_LIMIT_WINDOW_SEC)
       - Deploy as Web App (Execute as: Me, Access: Anyone)
       - Copy deployment URL
    2. Set the deployment URL as `VITE_APPS_SCRIPT_URL` in `.env`
    3. Set the same HMAC secret as `VITE_HMAC_SECRET` in `.env`
    4. Build: `npm run build`
    5. Start assessment flow: complete all 5 videos, verify scoreboard renders
    6. Verify submission overlay appears with spinner after scoreboard
    7. Verify success banner shows "Results sent" after backend 200
    8. Verify after 5 seconds, auto-transitions to "Thank you, {name}" screen
    9. Open Google Sheet — verify new row written with all columns populated
    10. Refresh page — verify localStorage `cma_attempt_v1` is set → "already completed" screen appears
    11. Test dedup: manually POST same emailHash via curl → verify 409 duplicate response
    **Edge cases to check:**
    - Invalid HMAC → 401 response, error overlay shown
    - Missing Origin → 403 response, error overlay shown
  </how-to-verify>
  <verify>
    <human-check>Deploy Apps Script per README.md, set env vars, build, run full assessment flow, verify Sheet row written, verify localStorage flag set, verify dedup rejection</human-check>
  </verify>
  <resume-signal>Type "approved" after verifying end-to-end submission works, or describe what failed</resume-signal>
  <done>Apps Script deployed, end-to-end submission verified, localStorage flag written, Sheet row populated, dedup confirmed working</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Apps Script (HTTPS POST) | JSON payload crosses trust boundary — HMAC provides integrity, Origin check limits sources, rate-limit prevents abuse |
| Apps Script→Google Sheet | Row data written to Sheet — formula injection mitigated via `'` prefix on all string values |
| Apps Script CacheService | Per-IP rate limit counters stored ephemerally — 60s TTL, auto-expiry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Spoofing | scripts/apps-script/Code.gs (Origin check) | mitigate | Validate Origin header against ALLOWED_ORIGIN property — rejects POSTs from any origin other than the deployed GitHub Pages URL |
| T-05-07 | Tampering | scripts/apps-script/Code.gs (HMAC) | mitigate | HMAC-SHA256 validated on server using raw body string minus hmac field (Pitfall 1 avoidance). Shared secret from PropertiesService. |
| T-05-08 | Denial of Service | scripts/apps-script/Code.gs (rate limit) | mitigate | CacheService rate-limits to 3 requests per IP per 60s window. Apps Script quota ceiling provides additional backstop. |
| T-05-09 | Repudiation | scripts/apps-script/Code.gs (dedup) | mitigate | SHA-256 of normalized email checked against Sheet column A. Duplicate submissions rejected with `{ ok: false, error: 'duplicate' }`. |
| T-05-10 | Tampering | scripts/apps-script/Code.gs (formula injection) | mitigate | All user-supplied string values prefixed with `'` before writing to Sheet cells — prevents `=CMD()`, `HYPERLINK()`, and other formula injection attacks. |
| T-05-11 | Denial of Service | src/utils/submission.js (retry) | mitigate | Exponential backoff (1s, 3s, 9s) — prevents thundering herd on transient outages. Max 3 attempts. 4xx errors surface immediately without retry. |
| T-05-12 | Information Disclosure | src/components/scoreboard/SubmissionOverlay.jsx (error) | mitigate | Error overlay shows generic "Unable to submit" message — no stack traces, no internal error codes exposed to user. |
| T-05-SC | Tampering | npm install (no new packages in this plan) | accept | No new npm packages installed in this plan. All server-side code uses built-in Apps Script services — no third-party libraries. |
</threat_model>

<verification>
1. `npx vitest run tests/utils/submission.test.js` — all submitResults tests pass with mocked fetch
2. `npx vitest run tests/components/scoreboard/ScoreboardScreen.test.jsx` — retry and error overlay tests pass
3. Manual: Deploy Apps Script via README.md steps, set env vars, build, run full assessment flow
4. Manual: Verify Sheet row written with correct columns after successful submission
5. Manual: Verify error overlay renders when endpoint is unreachable (stop Apps Script deployment or use wrong URL)
</verification>

<success_criteria>
- [ ] Apps Script doPost deployed and accepting POSTs at configured URL
- [ ] doPost returns `{ ok: false, error: 'invalid-hmac' }` without valid HMAC
- [ ] doPost returns `{ ok: false, error: 'duplicate' }` on repeated emailHash
- [ ] doPost returns `{ ok: false, error: 'rate-limited' }` after 3 requests in 60s
- [ ] doPost returns `{ ok: true, id: '<N>' }` on valid submission with correct HMAC
- [ ] Google Sheet row written with emailHash, name, email, answers, scores, competency, timestamps
- [ ] Client retries with visible attempt counter on 5xx/network errors
- [ ] Client surfaces error overlay with retry button on exhaustion
- [ ] Retry button resets counter and re-initiates submission
- [ ] 4xx errors surface immediately without retry (no spinners)
- [ ] Scoreboard data snapshot preserved across retries (no data loss)
</success_criteria>

<output>
Create `.planning/phases/05-submission-one-attempt-defense-in-depth/05-02-SUMMARY.md` when done
</output>
