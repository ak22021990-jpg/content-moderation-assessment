---
phase: 06-polish-content-freeze-launch-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.jsx
  - tests/transitions.test.jsx
  - tests/App.integration.test.jsx
autonomous: true
requirements:
  - QUALITY-06
must_haves:
  truths:
    - "Screen transitions animate with fade+slide on every screen change (LANDING→GUIDELINES, GUIDELINES→RUNNER, RUNNER→SCOREBOARD, SCOREBOARD→SUBMIT_DONE)"
    - "GSAP useGSAP uses scope ref to confine selectors — no double-fire in React 19 StrictMode dev mode"
    - "Existing ScoreboardScreen GSAP stagger animation continues to work unbroken after App-level transition is added"
    - "All 22 screen-transition events across a full assessment run produce exactly one animation each (no overlap, no flicker)"
  artifacts:
    - path: "src/App.jsx"
      provides: "useGSAP screen transition with containerRef scope + dependencies: [state.screen]"
      contains: "useGSAP"
      contains: "gsap.from"
      contains: ".cma-screen"
    - path: "tests/transitions.test.jsx"
      provides: "Verifies useGSAP fires gsap.from on screen change; mocks GSAP to avoid DOM dependency"
      exports:
        - "describe.*transitions"
  key_links:
    - from: "src/App.jsx useGSAP callback"
      to: "state.screen (Zustand/useAssessmentState)"
      via: "dependencies: [state.screen] — animation triggers on every screen state transition"
      pattern: "dependencies.*state\\.screen"
    - from: "src/App.jsx .cma-screen selector"
      to: "all screen components (LandingScreen, GuidelinesScreen, RunnerScreen, ScoreboardScreen, SubmitDoneScreen, AlreadyCompletedScreen)"
      via: "CSS class .cma-screen on each screen component's root div"
      pattern: "cma-screen"
---

<objective>
Add GSAP screen-to-screen transitions to App.jsx using the `useGSAP` hook with `scope` and `dependencies: [state.screen]`. Every screen change produces a fade-in + slide-up animation (0.35s, power2.out). Must coexist with ScoreboardScreen's existing independent `useGSAP` stagger on `.sb-section` elements without double-fire or selector collision in React 19 StrictMode.

Purpose: Polish the assessment UX so candidates perceive smooth production-quality transitions between screens, matching the research-specified animation pattern already verified on ScoreboardScreen.

Output: Animated screen transitions in App.jsx + test verifying useGSAP invokes gsap.from on screen change.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-content-freeze-launch-gates/06-RESEARCH.md
@src/App.jsx
@src/components/scoreboard/ScoreboardScreen.jsx
@tests/App.integration.test.jsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add useGSAP screen transitions to App.jsx</name>
  <files>src/App.jsx, tests/transitions.test.jsx</files>
  <behavior>
    - Test 1: Mock GSAP; render App; change screen; assert gsap.from called with '.cma-screen' and correct animation props
    - Test 2: useGSAP callback receives scope ref — gsap.from targets are scoped to containerRef.current
    - Test 3: dependencies: [state.screen] causes useGSAP to re-fire on each screen change (verify call count increases)
    - Test 4: Existing gsap.registerPlugin(useGSAP) line in module scope (ScoreboardScreen already does this; App.jsx must also register)
  </behavior>
  <action>
    In `src/App.jsx`:
    - Import `useRef` from 'react', `gsap` from 'gsap', `useGSAP` from '@gsap/react'.
    - Call `gsap.registerPlugin(useGSAP)` at module scope (before the component, same pattern as ScoreboardScreen line 20).
    - Add `const containerRef = useRef(null)` inside App component.
    - Add useGSAP block BEFORE the return statement, matching the ScoreboardScreen pattern (lines 44-52):
      ```
      useGSAP(() => {
        gsap.from('.cma-screen', {
          opacity: 0,
          y: 20,
          duration: 0.35,
          ease: 'power2.out',
        })
      }, { scope: containerRef, dependencies: [state.screen] })
      ```
    - The `dependencies: [state.screen]` array causes useGSAP to re-fire the animation every time `state.screen` changes. The `scope: containerRef` confines GSAP selectors to the App root, preventing selector collision with ScoreboardScreen's independent `.sb-section` stagger (which uses its own `containerRef` with `scope`).
    - Change the return statement: wrap `{renderScreen()}` in `<div ref={containerRef}>{renderScreen()}</div>`.
    - Existing `<ErrorBoundary>` wrapper stays as the outermost element. The new containerRef div goes inside ErrorBoundary.
    - Do NOT remove or change anything in other files. ScoreboardScreen's useGSAP is independent and must continue working.

    In `tests/transitions.test.jsx` (NEW FILE):
    - Create test file that mocks gsap + useGSAP to verify the hook fires on screen change.
    - Mock pattern: `vi.mock('gsap', () => ({ default: { from: vi.fn(), registerPlugin: vi.fn() } }))` and `vi.mock('@gsap/react', () => ({ useGSAP: vi.fn((fn, opts) => fn()) }))`.
    - Render App with test providers (wrap in assessment state context as existing integration tests do — follow `tests/App.integration.test.jsx` patterns).
    - Test 1: Verify gsap.from is called with '.cma-screen' and animation props on initial render.
    - Test 2: Trigger a screen change and verify gsap.from call count increments.
    - Test 3: Verify useGSAP received `scope` and `dependencies` options.

    Do NOT animate screen OUT — only animate IN. The `gsap.from` pattern creates a one-way enter animation. The old screen is replaced by React's conditional rendering; GSAP animates the new screen's mount. This is the simplest correct approach and avoids exit-animation timing complexity.
  </action>
  <verify>
    <automated>npx vitest run tests/transitions.test.jsx</automated>
  </verify>
  <done>
    - `src/App.jsx` imports useRef, gsap, useGSAP; calls gsap.registerPlugin(useGSAP); wraps renderScreen in `<div ref={containerRef}>`.
    - useGSAP block fires gsap.from('.cma-screen', { opacity: 0, y: 20, duration: 0.35, ease: 'power2.out' }) with scope: containerRef and dependencies: [state.screen].
    - `tests/transitions.test.jsx` passes: verifies GSAP called on screen change, useGSAP receives correct options.
    - `npx vitest run` — all existing tests still green. App.integration.test.jsx not broken by the new wrapping div.
    - Manual dev-server check: run `npm run dev`, complete assessment flow, verify 0.35s fade+slide on each screen transition, no flicker, no double-fire (check browser console for GSAP warnings).
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify ScoreboardScreen GSAP co-existence + update integration test</name>
  <files>tests/App.integration.test.jsx</files>
  <action>
    Read `tests/App.integration.test.jsx` to understand current structure. It may already test screen transitions through the full flow.

    If the integration test already covers screen-to-screen transitions (LANDING → GUIDELINES → RUNNER → SCOREBOARD → SUBMIT_DONE), verify it still passes with the new GSAP transition wrapper div.

    If the test is sensitive to the exact DOM structure (e.g., querying for direct children of a specific element), update selectors to account for the new containerRef div wrapper (the div sits between ErrorBoundary and the rendered screen component). Use `within()` or `closest()` patterns rather than brittle direct-child selectors.

    Add one assertion: after a screen change, the rendered screen's root element has class `cma-screen`. This verifies the GSAP selector target exists on every screen component and the transition can fire.

    Do NOT add manual-animation-verification assertions (GSAP is mocked in test environment). The assertion is structural: the `.cma-screen` class must be present because that's what `gsap.from('.cma-screen')` targets.
  </action>
  <verify>
    <automated>npx vitest run tests/App.integration.test.jsx</automated>
  </verify>
  <done>
    - `tests/App.integration.test.jsx` passes all existing assertions.
    - At least one assertion confirms `.cma-screen` class is present on rendered screen after transition.
    - No GSAP warnings in browser console during manual dev-server test (run full assessment flow).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | GSAP animations are client-side DOM manipulations only. No data crosses any trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | App.jsx useGSAP | accept | GSAP animation failure (JS error) degrades to instant screen switch — React conditional rendering still works without GSAP. No data loss, no security impact. |
| T-06-SC | Tampering | npm installs | mitigate | slopcheck audit already done in 06-RESEARCH.md — zero new packages added. GSAP + @gsap/react already installed and verified in prior phases. |
</threat_model>

<verification>
- `npx vitest run tests/transitions.test.jsx` — GSAP transitions unit test passes
- `npx vitest run tests/App.integration.test.jsx` — existing integration test still green
- `npx vitest run` — full suite green (425+ tests from prior phases)
- Manual: `npm run dev`, walk through all 6 screens, confirm each transition animates smoothly with no double-fire or flicker in Chrome
</verification>

<success_criteria>
- [ ] App.jsx wraps renderScreen in containerRef div, uses useGSAP with dependencies: [state.screen]
- [ ] Screen transitions are visually smooth (fade-in + slide-up, 0.35s) in Chrome dev server
- [ ] ScoreboardScreen's existing GSAP stagger on .sb-section works correctly alongside App-level transition
- [ ] No GSAP console warnings in React 19 StrictMode dev mode
- [ ] All existing tests pass (full suite green)
- [ ] transitions.test.jsx verifies GSAP hook invocation on screen change
</success_criteria>

<output>
Create `.planning/phases/06-polish-content-freeze-launch-gates/06-01-SUMMARY.md` when done
</output>
