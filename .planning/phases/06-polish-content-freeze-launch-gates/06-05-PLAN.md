---
phase: 06-polish-content-freeze-launch-gates
plan: 05
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - docs/launch-gates.md
  - docs/cross-browser-matrix.md
autonomous: false
requirements:
  - CONTENT-02
  - QUALITY-03
must_haves:
  truths:
    - "Full assessment runs end-to-end in Chrome, Edge, Safari, Firefox latest — all 5 videos play, tagging works, scoreboard displays, submission succeeds"
    - "Final brand-leak audit: grep -riE across entire repo returns zero hits for any of the 55 forbidden strings (excluding documented exclusions)"
    - "npm run build succeeds with zero warnings; npm test is all green (425+ tests)"
    - "Deployment smoke test to GitHub Pages staging confirms live app loads, videos play from CDN or LFS, submission endpoint responds"
    - "Every ROADMAP success criterion for Phase 6 has a verified checkmark in launch-gates.md"
  artifacts:
    - path: "docs/launch-gates.md"
      provides: "Complete launch readiness checklist with pass/fail status for every gate"
      contains: "Browser Matrix"
      contains: "Brand Audit"
      contains: "Build Verification"
    - path: "docs/cross-browser-matrix.md"
      provides: "Per-browser test results for Chrome, Edge, Safari, Firefox — video playback, tagging, timer, scoreboard, submission"
      contains: "Chrome"
      contains: "Firefox"
      contains: "Safari"
  key_links:
    - from: "docs/launch-gates.md final checklist"
      to: ".planning/ROADMAP.md Phase 6 Success Criteria"
      via: "Every success criterion (5 items) maps to a launch gate with verified pass/fail status"
      pattern: "Success Criteria"
    - from: "npm run build"
      to: "GitHub Pages deploy"
      via: "Vite build produces dist/; deploy workflow pushes to gh-pages branch"
      pattern: "build"
---

<objective>
Execute the final launch readiness verification: cross-browser end-to-end test matrix, full-repo brand-leak audit (code + git history + video content), build verification, deployment smoke test, and a master launch checklist documenting every gate's pass/fail status. This is the last plan before the assessment goes live for the first hiring cycle.

Purpose: Ensure no regression, no brand leak, no browser incompatibility, and no deployment failure before candidates use the assessment.

Output: `docs/launch-gates.md` with completed checklist, `docs/cross-browser-matrix.md` with per-browser results, verified green build and test suite.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-content-freeze-launch-gates/06-RESEARCH.md
@docs/brand-guardrails.md
@docs/launch-gates.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final Build + Test Suite Verification</name>
  <files>(none — verification only)</files>
  <action>
    Run the full verification pipeline and fix any failures BEFORE proceeding to browser matrix or audit tasks.

    **Step 1 — Clean install and build:**
    ```bash
    rm -rf node_modules dist
    npm install
    npm run build
    ```
    Verify:
    - Build completes with zero errors and zero warnings.
    - `dist/` directory contains `index.html`, `assets/` with hashed JS/CSS bundles.
    - `dist/videos/` contains all 5 MP4 files (copied by Vite from `public/`).
    - `dist/sprites/` and `dist/vtt/` contain sprite JPGs and VTT files.
    - No `.map` files (sourcemap: false in vite.config.js).

    **Step 2 — Full test suite:**
    ```bash
    npx vitest run
    ```
    Verify:
    - All tests pass (expect 425+ tests from Phase 0-5, plus ~20 new tests from Phase 6 plans 01-03).
    - Zero failing tests, zero skipped tests.
    - Test run completes in under 30 seconds.

    **Step 3 — If either step fails:**
    - Build failure: check Vite output for missing imports, broken references. Common causes: renamed fields in answerKeys.json not updated in consumer files, missing video files, env var issues.
    - Test failure: run the failing test file in isolation (`npx vitest run tests/failing.test.js`) to debug. Do NOT proceed to subsequent tasks until full suite is green.

    **Step 4 — Record results:**
    - Document build status, test count, and any fixes applied in `docs/launch-gates.md` under "Build Verification Gate."
  </action>
  <verify>
    <automated>npm run build && npx vitest run</automated>
  </verify>
  <done>
    - `npm run build` exits zero with no warnings.
    - `npx vitest run` passes all tests (425+).
    - `dist/` contains all expected assets (index.html, JS bundles, videos, sprites, VTTs).
    - Results documented in `docs/launch-gates.md`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full-Repo Brand-Leak Final Audit</name>
  <files>docs/launch-gates.md, docs/video-manifest.md</files>
  <action>Run 7 comprehensive brand-string scans: (1) all tracked source files with git grep, (2) index.html meta tags, (3) package.json fields, (4) full git commit history across all branches, (5) workflow file names, (6) manual frame-by-frame video content audit for all 5 videos, (7) Lottie asset names. Use the 55-string pattern from docs/brand-guardrails.md. Document results per scan in docs/launch-gates.md under "Final Brand-Leak Audit Gate." Zero hits required. If any scan finds a match, document the finding and escalate to client — historical git hits are non-recoverable (CC-01).</action>
  <verify><human-check>All 7 scans return zero hits. launch-gates.md documents each scan result. Any historical git hits are documented with severity assessment.</human-check></verify>
  <done>All 7 brand-audit scans return zero hits. launch-gates.md Final Brand-Leak Audit Gate marked PASSED. No brand IP in code, meta, package.json, commit history, workflows, video content, or Lottie names.</done>
  <what-built>Comprehensive brand-string scan across all code, HTML meta, package.json, commit history, workflow names, and video content. Zero hits required for launch.</what-built>
  <how-to-verify>
    **This is the final CC-01 gate. Any hit in any of these scans blocks launch.**

    Run each scan from the repo root:

    **Scan 1 — All tracked source files (excluding documented exclusions):**
    ```bash
    PATTERN='disney|disney\+|marvel|spider-man|spiderman|avengers|iron man|ironman|captain america|\bthor\b|black panther|hulk|wolverine|x-men|star wars|starwars|luke skywalker|darth vader|mandalorian|jedi|sith|han solo|chewbacca|yoda|lightsaber|pixar|toy story|toystory|finding nemo|wall-e|walle|\bcoco\b|buzz lightyear|woody|incredibles|mickey mouse|minnie mouse|\bfrozen\b|moana|encanto|elsa|anna|simba|lion king|cinderella|snow white|hulu|espn\+'
    EXCLUDE=':!docs/brand-guardrails.md :!.github/workflows/brand-guard.yml :!.husky/pre-commit :!package-lock.json :!.planning/**'
    git grep -liE "$PATTERN" -- $EXCLUDE
    ```
    Expect: **zero output.** If any file is listed, investigate and fix immediately.

    **Scan 2 — HTML meta tags (index.html):**
    ```bash
    grep -iE "$PATTERN" index.html
    ```
    Expect: **zero matches.** Verify `<title>`, `<meta>`, OG tags, favicon reference are all generic.

    **Scan 3 — package.json fields:**
    ```bash
    grep -iE "$PATTERN" package.json
    ```
    Expect: **zero matches.** Verify `name`, `description`, `author`, `repository` are generic. (Current values: "content-moderation-assessment", "Content moderation hiring assessment", "ak22021990-jpg" — all clean, but verify no drift.)

    **Scan 4 — Git commit history (EVERY commit, all branches):**
    ```bash
    git log --all --format="%H %s" | grep -iE "$PATTERN"
    ```
    Expect: **zero matches.** This is the non-recoverable scan — brand strings in commit messages are permanently in git history. If any match is found:
    - The commit cannot be removed (already pushed).
    - Document the finding in `docs/launch-gates.md` under "Historical Brand Leak Incidents."
    - If the commit is in a non-default branch, delete the branch.
    - If in main/master, assess severity: is it a false positive (e.g., "author" contains no `thor` because `\bthor\b` handles word boundaries — but `git grep` might behave differently than `grep -E`)? Verify with `grep -P` if available.
    - If it's a real leak, consult client — this may require repo deletion and recreation. CC-01 is non-recoverable.

    **Scan 5 — Workflow file names:**
    ```bash
    ls .github/workflows/ | grep -iE "$PATTERN"
    ```
    Expect: **zero matches.** Current workflows should be `deploy.yml` and `brand-guard.yml` — both clean.

    **Scan 6 — Video content manual audit:**
    - Watch all 5 videos frame-by-frame.
    - Read all chapters.vtt and thumbs.vtt files.
    - Check for:
      - Visible brand logos/names in overlays
      - Spoken brand names in audio
      - Brand-adjacent color schemes or logo silhouettes
      - Brand references in chapter labels
      - Brand references in VTT text
    - Document results in `docs/video-manifest.md` under each video's "Brand Audit" section.

    **Scan 7 — Lottie asset names:**
    List all Lottie files and verify names are generic:
    ```bash
    ls public/lottie/ 2>/dev/null || echo "No lottie directory found"
    ```
    If Lottie files exist in `src/assets/` or `public/`, verify filenames use generic language (`milestone-*`, `assessment-*`) per BRAND-08.

    **Record results:**
    Document each scan's result in `docs/launch-gates.md` under "Final Brand-Leak Audit Gate." If all 7 scans return zero hits, mark the gate as PASSED.
  </how-to-verify>
  <resume-signal>Type "brand audit passed" or list which scans found hits and what remediation was done.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cross-Browser End-to-End Test Matrix</name>
  <files>docs/cross-browser-matrix.md, docs/launch-gates.md</files>
  <action>Create docs/cross-browser-matrix.md with a 22-row test grid covering the full assessment flow. Test in Chrome, Edge, Safari, and Firefox latest versions (incognito/private windows). For each browser, complete a full 5-video assessment run and check all 22 test cases (Landing form, Guidelines, GSAP transitions, video playback, hover thumbnails, chapter markers, timer, TagPanel, VerdictButtons, ProgressIndicator, Scoreboard with GSAP stagger, Lottie milestone, submission, SubmitDone, localStorage flag, AlreadyCompleted). Mark each cell ✓/✗. For failures, document the specific error and fix before proceeding. Minimum bar: Chrome + Edge 100% pass; Safari + Firefox may have 1-2 minor visual issues if core flow works. Document results in launch-gates.md under "Cross-Browser Matrix Gate."</action>
  <verify><human-check>docs/cross-browser-matrix.md has all 22 test cases × 4 browsers filled. Chrome + Edge 100% pass. Safari + Firefox core flow passes. launch-gates.md matrix gate status recorded.</human-check></verify>
  <done>Cross-browser matrix complete. Chrome + Edge 100% pass. Safari and Firefox pass core assessment flow. All failures documented and remediated. launch-gates.md Cross-Browser Matrix Gate marked PASSED.</done>
  <what-built>Manual E2E test of the full assessment flow in Chrome, Edge, Safari, and Firefox latest versions. Each browser runs through all 5 videos with tagging, verdict, scoreboard, and submission.</what-built>
  <how-to-verify>
    **Prerequisites:** App must be deployed to a staging URL (GitHub Pages staging or `npx vite preview` locally accessible from all test browsers). If testing locally, use `npm run build && npx vite preview` and access via localhost from each browser.

    **Create `docs/cross-browser-matrix.md`** with the following test grid. For each browser, run the full assessment and check each item:

    | Test Case | Chrome | Edge | Safari | Firefox |
    |-----------|--------|------|--------|---------|
    | Landing: name/email form renders, validation works | | | | |
    | Landing: "Start" enables after valid input | | | | |
    | Guidelines: all 10 L1 categories render from taxonomy.json | | | | |
    | Guidelines: "Begin Assessment" advances to Runner | | | | |
    | GSAP transition: smooth fade+slide on each screen change | | | | |
    | Video 1: loads, plays, seek bar works | | | | |
    | Video 1: hover thumbnails appear on seek bar | | | | |
    | Video 1: chapter markers visible | | | | |
    | Timer: 3:00 countdown visible, counts down | | | | |
    | Timer: turns amber at 60s, red at 15s | | | | |
    | TagPanel: L1 multi-select works, L2 reveals on select | | | | |
    | TagPanel: keyboard navigation (Tab, Space) works | | | | |
    | VerdictButtons: Approve/Decline clickable, advances to next video | | | | |
    | Video 2-5: all load with correct content (not placeholder v01) | | | | |
    | ProgressIndicator: shows correct position (e.g., 3/5) | | | | |
    | Scoreboard: renders after all 5 videos complete | | | | |
    | Scoreboard: GSAP stagger animation on sections | | | | |
    | Scoreboard: Lottie milestone animation plays | | | | |
    | Scoreboard: submission auto-fires, success overlay shown | | | | |
    | SubmitDone: thank-you screen renders | | | | |
    | localStorage: one-attempt flag set after submission | | | | |
    | AlreadyCompleted: shows on revisit (localStorage flag present) | | | | |

    **For each browser:**
    1. Open a fresh incognito/private window (no cached localStorage).
    2. Navigate to the staging URL.
    3. Complete the full assessment flow — all 5 videos, all tagging, submit.
    4. Check each test case and mark ✓ (pass) or ✗ (fail) in the matrix.
    5. For any ✗, document the specific failure (screenshot, console error, step where it broke).

    **Known browser-specific concerns (from RESEARCH.md):**
    - Safari: `playsinline` required for iPhone/iPad (assessment is desktop-first but Safari desktop still needs this attribute). Verify `preload="metadata"` works.
    - Firefox: media-chrome custom elements may render differently. Verify seek bar, volume, and chapter markers.
    - Edge: Chromium-based, should match Chrome. Verify no Edge-specific issues.
    - All: verify `canplaythrough` event fires correctly (timer doesn't start before video is ready).

    **After matrix is complete:**
    - Tally results. All ✗ items must be investigated and fixed.
    - If any browser has >2 failures, create a dedicated remediation plan (add a new Phase 6 plan or fix inline).
    - Document results in `docs/launch-gates.md` under "Cross-Browser Matrix Gate."

    **Minimum bar for launch:** Chrome and Edge must be 100% pass. Safari and Firefox may have minor visual differences (1-2 non-blocking issues) as long as the core assessment flow (video playback, tagging, submission) works correctly.
  </how-to-verify>
  <resume-signal>Type "browser matrix passed" with pass/fail counts per browser, or describe failures that need fixing.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Deployment Smoke Test + Launch Checklist Finalization</name>
  <files>docs/launch-gates.md</files>
  <action>Deploy to GitHub Pages staging. Smoke test the live URL: verify page loads, all 5 videos play (from LFS or CDN), full assessment flow works, submission reaches Apps Script backend, localStorage flag set. Run Lighthouse performance audit (target ≥ 80). Create the master launch checklist in docs/launch-gates.md with all 20+ gates from the launch readiness table. Mark each gate PASSED/CONDITIONAL/FAILED with date and notes. Make launch decision: GO FOR LAUNCH (all passed), CONDITIONAL LAUNCH (accepted exceptions documented), or NO GO (blocking failures remain). If GO, create v1.0.0 git tag.</action>
  <verify><human-check>Staging deploy green. Live smoke test passes: videos play, submission works, localStorage flag set. Lighthouse score ≥ 80. launch-gates.md all gates populated with status. GO/NO GO decision documented. If GO, v1.0.0 tag exists.</human-check></verify>
  <done>Staging deploy verified. Smoke test passes. Lighthouse ≥ 80. All 20+ launch gates documented with PASSED status (or accepted exceptions). GO FOR LAUNCH decision made. v1.0.0 git tag created.</done>
  <what-built>Staging deploy to GitHub Pages, smoke test the live URL, and finalize the master launch checklist in docs/launch-gates.md.</what-built>
  <how-to-verify>
    **Step 1 — Staging deploy:**
    1. Push the current branch to GitHub.
    2. If using a staging Pages environment (separate branch or repo), trigger the deploy workflow.
    3. Wait for GitHub Actions deploy to complete (green check).
    4. Note the staging URL: `https://ak22021990-jpg.github.io/content-moderation-assessment/`.

    **Step 2 — Live smoke test:**
    1. Open the staging URL in Chrome (normal window, not incognito — test real-world conditions).
    2. Verify: page loads without 404 on any asset (check Network tab for red entries).
    3. Verify: all 5 videos load (check srcUrl resolves correctly — either LFS relative or CDN absolute depending on VITE_VIDEO_BASE_URL setting).
    4. Complete a full assessment run on the live URL.
    5. Verify: submission reaches the Apps Script backend (check Google Sheet for new row).
    6. Verify: one-attempt localStorage flag is set after successful submission.
    7. Reload the page — verify AlreadyCompleted screen shows.

    **Step 3 — Performance check:**
    - Run Lighthouse (Chrome DevTools → Lighthouse tab → Desktop, Performance category).
    - Target: Performance score ≥ 80. Main concerns: LCP (largest contentful paint), TBT (total blocking time), CLS (cumulative layout shift).
    - If Performance < 80, document the main offenders in launch-gates.md. (Video-heavy pages will naturally score lower; focus on non-video metrics.)

    **Step 4 — Finalize `docs/launch-gates.md`:**
    Create (if not already created by plan 06-04) with the following checklist:

    ```markdown
    # Launch Gates — Content Moderation Assessment v1.0

    | Gate | Status | Date | Notes |
    |------|--------|------|-------|
    | O-01: V5 Verdict Resolved | | | |
    | O-04: AI-gen Provider ToS Verified | | | |
    | O-05: Candidate Volume Confirmed | | | |
    | All 5 Real Videos Committed (v01-v05) | | | |
    | Sprites + VTTs Generated for All Videos | | | |
    | playlist.json All _status: "real" | | | |
    | answerKeys.json Version Final (no draft) | | | |
    | Kappa ≥ 0.6 All L1 Categories | | | |
    | Client Sign-Off: Taxonomy L2 | | | |
    | Client Sign-Off: Per-Video Answer Keys | | | |
    | R2 Bucket Provisioned + Populated | | | |
    | CDN Swap Tested (VITE_VIDEO_BASE_URL) | | | |
    | GSAP Screen Transitions Working | | | |
    | Brand Guard Pre-commit Hook Active | | | |
    | Brand Guard CI Workflow Active | | | |
    | Final Brand-Leak Audit (7 scans) | | | |
    | Cross-Browser Matrix (4 browsers) | | | |
    | Build Verification (npm run build) | | | |
    | Full Test Suite Green | | | |
    | Deployment Smoke Test | | | |
    | ROADMAP Success Criteria 1-5 All Met | | | |

    ## Launch Decision
    - [ ] **GO FOR LAUNCH** — All gates PASSED. Ready for first hiring cycle.
    - [ ] **CONDITIONAL LAUNCH** — Gates X, Y, Z have accepted exceptions (documented below).
    - [ ] **NO GO** — Gates A, B, C remain failing. Launch blocked.
    ```

    Fill in the Status column for each gate based on completed plans. Mark verified gates as "PASSED" with date.

    **Step 5 — Git tag:**
    If all gates pass, create a version tag:
    ```bash
    git tag -a v1.0.0 -m "v1.0.0 — Content Moderation Assessment launch-ready"
    git push origin v1.0.0
    ```
    This tag marks the exact commit that passed all launch gates.
  </how-to-verify>
  <resume-signal>Type "launch checklist complete" with GO/CONDITIONAL/NO GO decision and any open items.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staging deploy → public internet | GitHub Pages serves static assets. Verify no .env files, no secrets, no planning docs in dist/. |
| Candidate browser → deployed app | Client-side only. Submission goes to Apps Script over HTTPS with HMAC. No sensitive data in static assets. |
| git history → public repo | Brand audit of commit history is the final CC-01 check. Any brand string in any historical commit is permanently exposed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05a | Information Disclosure | dist/ build output | mitigate | Verify `dist/` contains no `.planning/`, no `.env`, no `node_modules/`, no brand strings in bundled JS. Sourcemap disabled — no source leakage. |
| T-06-05b | Information Disclosure | GitHub Pages URL | accept | Repo name "content-moderation-assessment" is generic. GitHub username "ak22021990-jpg" is personal. No client brand in URL path. |
| T-06-05c | Information Disclosure | git commit history | mitigate | Full-history brand audit (Scan 4). Any historical leak is documented and escalated to client. Repo deletion may be required if leak is severe. |
| T-06-05d | Denial of Service | LFS bandwidth exhaustion | mitigate | If candidate volume >10/month per O-05, CDN swap is mandatory. Verified during deployment smoke test — all videos load from CDN, not LFS. |
| T-06-05e | Tampering | Deploy workflow | accept | GitHub Actions workflow is trusted. Deploy key is managed by GitHub. No custom deploy scripts with elevated permissions. |
| T-06-SC | Tampering | npm installs | mitigate | slopcheck audit in 06-RESEARCH.md — zero new packages. Build verification ensures all dependencies are from lockfile (npm ci). |
</threat_model>

<verification>
- `npm run build` — zero errors, zero warnings
- `npx vitest run` — all tests pass
- `git grep -liE "$PATTERN" -- :!docs/brand-guardrails.md :!.github/workflows/brand-guard.yml :!.husky/pre-commit :!package-lock.json :!.planning/**` — zero output
- `git log --all --format="%H %s" | grep -iE "$PATTERN"` — zero output
- Staging URL loads, all 5 videos play, submission succeeds
- `docs/launch-gates.md` all gates checked, GO/NO GO decision documented
</verification>

<success_criteria>
- [ ] Build passes: `npm run build` zero errors/warnings
- [ ] Test suite: `npx vitest run` all green (425+ tests)
- [ ] Brand audit: all 7 scans return zero hits
- [ ] Browser matrix: Chrome and Edge 100% pass; Safari and Firefox pass core flow
- [ ] Staging deploy: live URL loads, videos play, submission works
- [ ] Launch checklist: all gates documented with pass/fail status
- [ ] v1.0.0 git tag created if GO FOR LAUNCH
</success_criteria>

<output>
Create `.planning/phases/06-polish-content-freeze-launch-gates/06-05-SUMMARY.md` when done
</output>
