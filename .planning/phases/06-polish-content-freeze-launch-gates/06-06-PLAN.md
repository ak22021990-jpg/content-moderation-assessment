---
phase: 06-polish-content-freeze-launch-gates
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - docs/brand-guardrails.md
  - .husky/pre-commit
  - .github/workflows/brand-guard.yml
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - BRAND-04
  - BRAND-05
  - BRAND-06
  - BRAND-07
  - BRAND-08
must_haves:
  truths:
    - "Brand guard no longer blocks commits containing strings like 'disney', 'mickey mouse', 'marvel', 'star wars'"
    - "Pre-commit hook passes when a test file contains a formerly forbidden string"
    - "CI brand-guard.yml workflow passes unconditionally"
    - "PROJECT.md Key Decisions logs the guard removal with risk acceptance note"
    - "REQUIREMENTS.md BRAND-* items are marked v2/deferred or removed"
  artifacts:
    - path: "docs/brand-guardrails.md"
      provides: "Disabled guardrails document explaining guard is off and referencing risk acceptance"
      contains: "disabled"
    - path: ".husky/pre-commit"
      provides: "Pre-commit hook that no longer scans for forbidden brand strings"
      contains: "brand"
    - path: ".github/workflows/brand-guard.yml"
      provides: "CI workflow that exits success without scanning"
      contains: "Brand check skipped"
  key_links:
    - from: ".husky/pre-commit"
      to: ".github/workflows/brand-guard.yml"
      via: "Both guard locations updated in same commit"
      pattern: "brand"
---

<objective>
Disable the brand-name guardrails system so the project can reference Disney-related strings, characters, and iconography in UI, code, and commits. Update the three canonical guard locations (docs, pre-commit hook, CI workflow) in one commit and record the legal-risk decision in PROJECT.md.

Purpose: User has explicitly accepted legal risk and wants Disney/Mickey references allowed for celebration graphics and video-related copy.

Output: Guardrails disabled across docs, pre-commit, and CI; decision logged; requirements updated.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@docs/brand-guardrails.md
@.husky/pre-commit
@.github/workflows/brand-guard.yml
</context>

<tasks>

<task type="manual">
  <name>Task 1: Accept risk and disable brand guard</name>
  <files>docs/brand-guardrails.md, .husky/pre-commit, .github/workflows/brand-guard.yml, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <behavior>
    - Do not delete files; convert them into "disabled" documentation.
    - Keep a clear audit trail of the original guard pattern so it can be re-enabled.
    - Update REQUIREMENTS.md BRAND-* items to v2/deferred status.
  </behavior>
  <action>
    In `docs/brand-guardrails.md`:
    - Add a prominent warning header: "BRAND GUARD DISABLED — legal risk accepted by project owner on [date]".
    - Keep the original forbidden-string list for reference, but clearly mark it as archived/no longer enforced.
    - Explain that the guard was disabled because assessment videos reference Disney content and the UI will use Mickey Mouse iconography.

    In `.husky/pre-commit`:
    - Remove or comment out the `grep -riE "$FORBIDDEN" ...` scan.
    - Replace with an echo: "Brand guard skipped (disabled by 06-06)".
    - Ensure the hook still exits 0.

    In `.github/workflows/brand-guard.yml`:
    - Replace the scan step with a single `run: echo "Brand guard skipped (disabled by 06-06)"`.
    - Keep the job shell so the workflow remains visible in CI history.

    In `.planning/PROJECT.md`:
    - Add a Key Decisions row: "Disabled brand-name guardrails" | "User accepted legal risk; Disney references required for UI/video context" | "⚠️ Revisit".
    - Update the Constraints section if it mentions brand guard as non-negotiable; append a note that it was disabled by owner decision.

    In `.planning/REQUIREMENTS.md`:
    - Move BRAND-* requirements to v2/deferred, or mark each as "Deferred — guard disabled by owner decision".
    - Do not delete REQ-IDs; preserve traceability.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    - `docs/brand-guardrails.md` documents disabled status with original pattern archived.
    - `.husky/pre-commit` no longer fails on Disney strings.
    - `.github/workflows/brand-guard.yml` passes unconditionally.
    - `PROJECT.md` logs the decision with "⚠️ Revisit" outcome.
    - `REQUIREMENTS.md` BRAND-* items moved/deferred.
    - All existing tests still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify guard is truly disabled</name>
  <files>tests/brand-guard-disabled.test.js (new)</files>
  <behavior>
    - Create a throwaway test that contains a formerly forbidden string inside a comment or string literal.
    - Verify husky pre-commit and CI workflow do not block it.
  </behavior>
  <action>
    Create `tests/brand-guard-disabled.test.js`:
    - File contains a trivial test and a comment like `// formerly forbidden: disney, mickey mouse, marvel`.
    - Run `npx vitest run tests/brand-guard-disabled.test.js` to confirm the file does not break tests.
    - Simulate pre-commit: run `.husky/pre-commit` from repo root; assert exit code 0.
    - Delete the throwaway test file after verification (or keep it if user prefers an ongoing canary).
  </action>
  <verify>
    <automated>.husky/pre-commit</automated>
  </verify>
  <done>
    - Pre-commit exits 0 even with Disney strings in staged file.
    - CI workflow yml is syntactically valid and would pass.
    - Throwaway test file removed (or kept as canary with user approval).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public repository | Guard removal exposes repo to accidental or intentional Disney/IP strings in commits/code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Legal/Compliance | Brand guard | accept | User explicitly accepted legal risk; decision logged in PROJECT.md. |
| T-06-06-02 | Repudiation | Commit history | mitigate | Keep archived guard pattern in `docs/brand-guardrails.md` so future maintainers know what was disabled. |
</threat_model>

<verification>
- `.husky/pre-commit` exits 0 when run manually
- `npx vitest run` — full suite green
- GitHub Actions workflow syntax validated (if possible via `act` or manual review)
</verification>

<success_criteria>
- [ ] Brand guard disabled in all three locations (docs, pre-commit, CI)
- [ ] PROJECT.md records the decision with risk note
- [ ] REQUIREMENTS.md BRAND-* items deferred
- [ ] Pre-commit and CI pass on commits containing formerly forbidden strings
- [ ] All existing tests pass
</success_criteria>

<output>
Create `.planning/phases/06-polish-content-freeze-launch-gates/06-06-SUMMARY.md` when done
</output>
