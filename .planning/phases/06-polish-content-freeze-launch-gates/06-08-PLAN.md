---
phase: 06-polish-content-freeze-launch-gates
plan: 08
type: execute
wave: 2
depends_on:
  - 06-09
files_modified:
  - src/components/RunnerScreen.jsx
  - src/components/player/VideoPlayerScreen.jsx
  - src/styles/runner.css
  - tests/components/RunnerScreen.test.jsx
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - TAG-07
  - VERDICT-01
must_haves:
  truths:
    - "RunnerScreen has a fixed desktop width and does not adapt to narrow viewports"
    - "The 'Video N — Content Moderation' title overlay is removed from the runner"
    - "Video column and tag column are visually connected with minimal empty space"
    - "Layout does not break when verdict feedback overlay appears"
    - "Runner remains usable at 1280px wide and up"
  artifacts:
    - path: "src/components/RunnerScreen.jsx"
      provides: "Fixed-desktop runner layout without video title"
      contains: "cma-runner"
    - path: "src/components/player/VideoPlayerScreen.jsx"
      provides: "Video player without title overlay"
      contains: "VideoPlayerScreen"
    - path: "src/styles/runner.css"
      provides: "Centralized runner layout CSS"
      contains: "cma-runner"
  key_links:
    - from: "src/components/RunnerScreen.jsx"
      to: "src/components/player/VideoPlayerScreen.jsx"
      via: "Renders VideoPlayerScreen with videoIndex prop"
      pattern: "VideoPlayerScreen"
    - from: "src/components/RunnerScreen.jsx"
      to: "src/components/tagging/TagPanel.jsx"
      via: "Renders TagPanel in right column"
      pattern: "TagPanel"
---

<objective>
Fix the RunnerScreen layout so it feels intentional and not distorted on desktop. Remove the per-video title overlay. Fix the container at a desktop width and tighten the space between the video and the tagging panel. Stop trying to be responsive on narrow viewports.

Purpose: The current runner has too much empty space and a title that adds clutter. A fixed desktop layout will look polished and readable.

Output: Cleaner, fixed-width RunnerScreen with no video title and better proportions.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@src/components/RunnerScreen.jsx
@src/components/player/VideoPlayerScreen.jsx
@src/components/tagging/TagPanel.jsx
@src/components/tagging/VerdictButtons.jsx
@src/components/ProgressIndicator.jsx
@tests/components/RunnerScreen.test.jsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove video title overlay</name>
  <files>src/components/player/VideoPlayerScreen.jsx, tests/components/VideoPlayerScreen.test.jsx</files>
  <behavior>
    - Remove any rendered title/heading that shows "Video N — Content Moderation".
    - Keep the video player, controls, and chapter markers intact.
    - Update tests that assert on the title text.
  </behavior>
  <action>
    In `src/components/player/VideoPlayerScreen.jsx`:
    - Find the title element (likely rendering video title or "Content Moderation").
    - Remove it or hide it via CSS `display: none` if it must stay in DOM for a11y.
    - If the title is needed for screen readers only, render as `aria-label` on the player container instead.

    Update `tests/components/VideoPlayerScreen.test.jsx`:
    - Remove assertions that look for "Video 1" or "Content Moderation" title.
    - Add assertion that the title element is not visible (or absent).
  </action>
  <verify>
    <automated>npx vitest run tests/components/VideoPlayerScreen.test.jsx</automated>
  </verify>
  <done>
    - Video title overlay no longer visible.
    - VideoPlayerScreen tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor RunnerScreen to fixed desktop layout</name>
  <files>src/components/RunnerScreen.jsx, src/styles/runner.css, tests/components/RunnerScreen.test.jsx</files>
  <behavior>
    - Use a centered fixed-width container (e.g., `min-width: 1200px`, `max-width: 1400px`).
    - Reduce gap between video and tag panel (e.g., `gap: 1rem`).
    - Video column ~60%, tag column ~40% or fixed ~440px.
    - Header/progress bar sits above both columns.
    - No mobile breakpoints; narrow viewports get horizontal scroll or clip.
  </behavior>
  <action>
    In `src/components/RunnerScreen.jsx`:
    - Replace inline styles with CSS classes where possible.
    - Wrap main content in `cma-runner__container`.
    - Set container styles:
      - `min-width: 1200px`
      - `max-width: 1400px`
      - `margin: 0 auto`
      - `padding: 1rem 1.5rem`
    - Grid: `grid-template-columns: 1fr 420px` (or `minmax(0, 1fr) 420px`).
    - Gap: `1rem`.
    - Ensure `CountdownDisplay` sits below video, left-aligned.
    - Ensure feedback overlay still centers correctly.

    Create/update `src/styles/runner.css`:
    - Define `.cma-runner`, `.cma-runner__container`, `.cma-runner__video-col`, `.cma-runner__tag-col`.
    - Keep existing color/background inline if needed for theming.

    Update `tests/components/RunnerScreen.test.jsx`:
    - Assert runner container has fixed min-width class/style.
    - Assert video and tag columns are side-by-side.
    - Assert title overlay is not present.
  </action>
  <verify>
    <automated>npx vitest run tests/components/RunnerScreen.test.jsx</automated>
  </verify>
  <done>
    - RunnerScreen has fixed desktop container.
    - Video + tag panel are side-by-side with tight gap.
    - No visible video title.
    - Runner tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Manual visual verification</name>
  <files>N/A</files>
  <behavior>
    - Run dev server and visually inspect runner at 1280px, 1440px, and 1920px.
    - Verify no overlap, no excessive whitespace, and tag panel is readable.
  </behavior>
  <action>
    - `npm run dev`
    - Complete a full assessment run or use test data.
    - Check that feedback overlay does not break layout.
    - Check that tag panel scrolling still works.
  </action>
  <verify>
    <manual>Visual check across 1280px/1440px/1920px</manual>
  </verify>
  <done>
    - Layout looks intentional at all tested widths.
    - No console errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser viewport | Layout is CSS-only; no data crosses trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | Denial of Service | Fixed desktop width | accept | User explicitly wants desktop-only; narrow viewports may clip. |
| T-06-08-02 | Tampering | CSS file | mitigate | Styles scoped to `.cma-runner*` classes to avoid leaking. |
</threat_model>

<verification>
- `npx vitest run tests/components/RunnerScreen.test.jsx`
- `npx vitest run tests/components/VideoPlayerScreen.test.jsx`
- `npx vitest run` — full suite green
- Manual: `npm run dev`, verify runner layout at 1280px, 1440px, 1920px
</verification>

<success_criteria>
- [ ] Video title overlay removed
- [ ] RunnerScreen uses fixed desktop width container
- [ ] Gap between video and tag panel reduced
- [ ] No mobile breakpoints added
- [ ] Feedback overlay still centers correctly
- [ ] All tests pass
</success_criteria>

<output>
Create `.planning/phases/06-polish-content-freeze-launch-gates/06-08-SUMMARY.md` when done
</output>
