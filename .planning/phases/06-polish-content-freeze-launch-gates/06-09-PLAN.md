---
phase: 06-polish-content-freeze-launch-gates
plan: 09
type: execute
wave: 2
depends_on: []
files_modified:
  - src/components/RunnerScreen.jsx
  - src/components/tagging/TagPanel.jsx
  - src/components/tagging/tagReducer.js
  - tests/components/RunnerScreen.test.jsx
  - tests/components/tagging/TagPanel.test.jsx
autonomous: true
requirements:
  - TAG-01
  - TAG-02
  - TAG-03
  - TAG-04
must_haves:
  truths:
    - "TagPanel selections are cleared when the runner advances to the next video"
    - "TagPanel selections are cleared when the runner advances via timer auto-submit"
    - "L1/L2 selections from a previous video do not leak into the next video"
    - "Keyboard focus returns to a sensible starting point after reset"
    - "TagPanel state reset does not trigger double renders or race conditions"
  artifacts:
    - path: "src/components/tagging/TagPanel.jsx"
      provides: "Tag panel that resets when resetKey prop changes"
      contains: "TagPanel"
    - path: "src/components/RunnerScreen.jsx"
      provides: "Passes currentVideoIndex as resetKey to TagPanel"
      contains: "currentVideoIndex"
  key_links:
    - from: "src/components/RunnerScreen.jsx"
      to: "src/components/tagging/TagPanel.jsx"
      via: "resetKey prop forces remount/reset on video change"
      pattern: "resetKey"
    - from: "src/components/tagging/TagPanel.jsx"
      to: "src/components/tagging/tagReducer.js"
      via: "Reducer manages selectedL1/selectedL2 state"
      pattern: "tagReducer"
---

<objective>
Fix the bug where L1/L2 tag selections persist when advancing from one video to the next. Ensure every new video starts with a clean TagPanel state.

Purpose: Each video is an independent moderation decision. Leftover tags from video 1 would corrupt the candidate's answer for video 2.

Output: TagPanel resets automatically on every video advance.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@src/components/RunnerScreen.jsx
@src/components/tagging/TagPanel.jsx
@src/components/tagging/tagReducer.js
@src/components/tagging/L1Chip.jsx
@src/components/tagging/L2Chip.jsx
@tests/components/RunnerScreen.test.jsx
@tests/components/tagging/TagPanel.test.jsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reset capability to TagPanel</name>
  <files>src/components/tagging/TagPanel.jsx, src/components/tagging/tagReducer.js</files>
  <behavior>
    - Choose the simplest correct reset strategy.
    - Preferred: add `resetKey` prop; when it changes, dispatch RESET action or reinitialize state.
    - Alternative: use `key={resetKey}` on TagPanel to remount.
  </behavior>
  <action>
    In `src/components/tagging/tagReducer.js`:
    - Add a `RESET` action handler that returns `initialTagState`.

    In `src/components/tagging/TagPanel.jsx`:
    - Accept optional `resetKey` prop.
    - Add a `useEffect`:
      ```js
      useEffect(() => {
        dispatch({ type: 'RESET' })
      }, [resetKey])
      ```
    - Alternatively, if remounting is simpler and avoids stale refs:
      - Document that `key={resetKey}` can be used by parent.
    - The `useEffect` approach is chosen here to keep animations/transitions intact.

    Ensure `onSelectionChange` is not called with stale data during reset (guard with mounted ref or suppress first render callback if needed).
  </action>
  <verify>
    <automated>npx vitest run tests/components/tagging/TagPanel.test.jsx</automated>
  </verify>
  <done>
    - `tagReducer` handles RESET action.
    - `TagPanel` resets when `resetKey` changes.
    - Existing TagPanel tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire resetKey in RunnerScreen</name>
  <files>src/components/RunnerScreen.jsx, tests/components/RunnerScreen.test.jsx</files>
  <behavior>
    - Pass `currentVideoIndex` as `resetKey` to TagPanel.
    - Ensure reset happens before the new video's timer starts.
    - Verify selections from previous video are gone.
  </behavior>
  <action>
    In `src/components/RunnerScreen.jsx`:
    - Change `<TagPanel onSelectionChange={handleSelectionChange} />` to:
      `<TagPanel resetKey={currentVideoIndex} onSelectionChange={handleSelectionChange} />`
    - Verify `handleSelectionChange` still receives updated selections after reset.
    - Ensure `tagSelectionRef.current` is also reset or overwritten at the right moment.

    In `tests/components/RunnerScreen.test.jsx`:
    - Add test: select an L1 on video 1, submit verdict, advance to video 2, assert no L1 chips are selected.
    - Add test: select L1 + L2 on video 1, auto-submit via timer, advance, assert clean state.
    - Mock `playlist.videos` and `answerKeys` as needed.
  </action>
  <verify>
    <automated>npx vitest run tests/components/RunnerScreen.test.jsx</automated>
  </verify>
  <done>
    - RunnerScreen passes `currentVideoIndex` as resetKey.
    - Tag selections clear on video advance.
    - New tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regression test full suite</name>
  <files>N/A</files>
  <behavior>
    - Run full test suite to ensure reset does not break timer, scoring, or submission.
  </behavior>
  <action>
    - `npx vitest run`
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>
    - Full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client state | Tag selections are local UI state only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09-01 | Data Integrity | TagPanel reset | mitigate | Reset clears selections before new video timer starts; answers committed via store use snapshot taken at verdict time. |
| T-06-09-02 | Denial of Service | Reset race | mitigate | Dispatch RESET synchronously in useEffect; no async involved. |
</threat_model>

<verification>
- `npx vitest run tests/components/tagging/TagPanel.test.jsx`
- `npx vitest run tests/components/RunnerScreen.test.jsx`
- `npx vitest run` — full suite green
- Manual: `npm run dev`, select tags on video 1, submit, verify clean panel on video 2
</verification>

<success_criteria>
- [ ] TagPanel has RESET action or remount strategy
- [ ] RunnerScreen passes resetKey/currentVideoIndex to TagPanel
- [ ] Selections clear on manual verdict advance
- [ ] Selections clear on timer auto-submit advance
- [ ] New regression tests added
- [ ] Full suite green
</success_criteria>

<output>
Create `.planning/phases/06-polish-content-freeze-launch-gates/06-09-SUMMARY.md` when done
</output>
