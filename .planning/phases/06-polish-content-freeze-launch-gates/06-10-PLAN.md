---
phase: 06-polish-content-freeze-launch-gates
plan: 10
type: execute
wave: 3
depends_on:
  - 06-12
files_modified:
  - src/components/tagging/L1Chip.jsx
  - src/components/tagging/L2Chip.jsx
  - src/components/ui/Tooltip.jsx
  - src/styles/tooltip.css
  - tests/components/tagging/Tooltip.test.jsx
autonomous: true
requirements:
  - GUIDE-02
  - TAG-07
must_haves:
  truths:
    - "Hovering an L1 chip in the runner shows a tooltip with the L1 definition"
    - "Hovering an L2 chip in the runner shows a tooltip with the L2 definition and example"
    - "Tooltips appear on mouse enter and disappear on mouse leave"
    - "Tooltips are keyboard accessible (focus shows tooltip)"
    - "Tooltips respect prefers-reduced-motion"
    - "Tooltips do not overflow viewport; positioned above/below chip as space allows"
  artifacts:
    - path: "src/components/ui/Tooltip.jsx"
      provides: "Reusable accessible tooltip component"
      contains: "Tooltip"
    - path: "src/components/tagging/L1Chip.jsx"
      provides: "L1 chip wrapped with definition tooltip"
      contains: "Tooltip"
    - path: "src/components/tagging/L2Chip.jsx"
      provides: "L2 chip wrapped with definition + example tooltip"
      contains: "Tooltip"
  key_links:
    - from: "src/components/tagging/L1Chip.jsx"
      to: "src/components/ui/Tooltip.jsx"
      via: "Wraps button in Tooltip with category.definition"
      pattern: "Tooltip"
    - from: "src/components/tagging/L2Chip.jsx"
      to: "src/components/ui/Tooltip.jsx"
      via: "Wraps button in Tooltip with subcategory.definition + example"
      pattern: "Tooltip"
---

<objective>
Add hover tooltips to the runner's L1 and L2 tagging chips so candidates can quickly reference taxonomy definitions without leaving the screen. L1 tooltips show the definition; L2 tooltips show the definition plus an example.

Purpose: Reduce cognitive load during moderation; candidates can verify category meaning mid-test.

Output: Accessible tooltips integrated into L1Chip and L2Chip.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@src/components/tagging/L1Chip.jsx
@src/components/tagging/L2Chip.jsx
@src/components/tagging/TagPanel.jsx
@src/data/taxonomy.json
@tests/components/tagging/L1Chip.test.jsx
@tests/components/tagging/L2Chip.test.jsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build reusable Tooltip component</name>
  <files>src/components/ui/Tooltip.jsx, src/styles/tooltip.css, tests/components/ui/Tooltip.test.jsx</files>
  <behavior>
    - Custom tooltip; no new library.
    - Uses React portal or absolute positioning relative to viewport.
    - Triggers on mouseenter/mouseleave and focus/blur.
    - Animates with Framer Motion (already installed).
    - Respects reduced motion.
  </behavior>
  <action>
    Create `src/components/ui/Tooltip.jsx`:
    - Props: `content` (React node), `children`, `placement` (top|bottom|left|right, default top), `delay` (default 150ms).
    - State: `visible` controlled by mouse/focus events.
    - Refs: trigger ref and tooltip ref.
    - Positioning: measure trigger rect, compute tooltip position, clamp to viewport.
    - Render tooltip in a portal attached to `document.body` to avoid clipping.
    - Add `role="tooltip"`, `id`, and `aria-describedby` on trigger.
    - Use `AnimatePresence` for enter/exit animation.
    - Honor `useReducedMotion()`.

    Create `src/styles/tooltip.css`:
    - `.cma-tooltip`, `.cma-tooltip__content`, arrow styles.

    Create `tests/components/ui/Tooltip.test.jsx`:
    - Test hover shows tooltip.
    - Test focus shows tooltip.
    - Test unhover hides tooltip.
    - Test content renders.
  </action>
  <verify>
    <automated>npx vitest run tests/components/ui/Tooltip.test.jsx</automated>
  </verify>
  <done>
    - Tooltip component renders on hover/focus.
    - Positioning and reduced motion handled.
    - Tooltip tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire tooltips into L1Chip and L2Chip</name>
  <files>src/components/tagging/L1Chip.jsx, src/components/tagging/L2Chip.jsx, tests/components/tagging/L1Chip.test.jsx, tests/components/tagging/L2Chip.test.jsx</files>
  <behavior>
    - L1 tooltip: category.definition only.
    - L2 tooltip: category.definition + example (formatted as "Definition. Example: ...").
    - Keep existing chip styles and interactions.
    - Remove the old visually-hidden description span from L2Chip or keep it as fallback.
  </behavior>
  <action>
    In `src/components/tagging/L1Chip.jsx`:
    - Wrap the `motion.button` in `<Tooltip content={category.definition}>`.
    - Ensure the tooltip content is a string or simple node.

    In `src/components/tagging/L2Chip.jsx`:
    - Wrap the `motion.button` in `<Tooltip content={`${subcategory.definition} Example: ${subcategory.example}`}>`.
    - Remove or simplify the visually-hidden span since tooltip provides the info.
    - Keep `aria-describedby` pointing to tooltip id for screen readers.

    Update tests:
    - `L1Chip.test.jsx`: hover/focus chip, assert tooltip with definition appears.
    - `L2Chip.test.jsx`: hover/focus chip, assert tooltip with definition and example appears.
  </action>
  <verify>
    <automated>npx vitest run tests/components/tagging/L1Chip.test.jsx tests/components/tagging/L2Chip.test.jsx</automated>
  </verify>
  <done>
    - L1 and L2 chips show correct tooltips.
    - Chip tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Viewport overflow check</name>
  <files>N/A</files>
  <behavior>
    - Verify tooltips near viewport edges reposition correctly.
  </behavior>
  <action>
    - Manual dev-server check with narrow desktop viewport.
    - Verify tooltip stays within window bounds.
  </action>
  <verify>
    <manual>Hover bottom-row L2 chips and top-row L1 chips</manual>
  </verify>
  <done>
    - No tooltip clipped by viewport.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM | Tooltips render client-side only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10-01 | Information Disclosure | Tooltip content | accept | Same taxonomy data already in GuidelinesScreen. |
| T-06-10-02 | Denial of Service | Portal/tooltip mount | mitigate | Single portal per tooltip; unmount on hide to keep DOM light. |
</threat_model>

<verification>
- `npx vitest run tests/components/ui/Tooltip.test.jsx`
- `npx vitest run tests/components/tagging/L1Chip.test.jsx`
- `npx vitest run tests/components/tagging/L2Chip.test.jsx`
- `npx vitest run` — full suite green
- Manual: `npm run dev`, hover L1 and L2 chips, verify content and positioning
</verification>

<success_criteria>
- [ ] Reusable Tooltip component exists and is tested
- [ ] L1 hover shows definition tooltip
- [ ] L2 hover shows definition + example tooltip
- [ ] Keyboard focus triggers tooltip
- [ ] Reduced motion respected
- [ ] Tooltips stay within viewport
- [ ] All tests pass
</success_criteria>

<output>
Create `.planning/phases/06-polish-content-freeze-launch-gates/06-10-SUMMARY.md` when done
</output>
