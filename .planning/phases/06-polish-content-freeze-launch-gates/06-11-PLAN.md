---
phase: 06-polish-content-freeze-launch-gates
plan: 11
type: execute
wave: 3
depends_on:
  - 06-06
files_modified:
  - src/components/RunnerScreen.jsx
  - src/components/feedback/FeedbackOverlay.jsx
  - src/components/feedback/MickeyConfetti.jsx
  - src/styles/feedback.css
  - tests/components/feedback/FeedbackOverlay.test.jsx
autonomous: true
requirements:
  - VERDICT-01
  - VERDICT-02
  - VERDICT-03
must_haves:
  truths:
    - "Feedback overlay is larger and more visually prominent than the current small modal"
    - "Correct verdict triggers a celebration animation with outlined Mickey Mouse head confetti"
    - "Wrong verdict triggers a distinct shake + red glow animation"
    - "Overlay displays verdict result, the reason ('Correct: this clip contained Copyright & IP'), and partial tag match breakdown"
    - "User can click Continue to advance immediately or wait for auto-advance"
    - "Confetti respects prefers-reduced-motion (no motion for users who requested it)"
    - "Animation duration and auto-advance timing remain reasonable (~2.5s default)"
  artifacts:
    - path: "src/components/feedback/FeedbackOverlay.jsx"
      provides: "Full feedback overlay with verdict, reason, and partial match"
      contains: "FeedbackOverlay"
    - path: "src/components/feedback/MickeyConfetti.jsx"
      provides: "Canvas-based outlined Mickey head confetti burst"
      contains: "MickeyConfetti"
    - path: "src/components/RunnerScreen.jsx"
      provides: "Renders FeedbackOverlay instead of inline feedback markup"
      contains: "FeedbackOverlay"
  key_links:
    - from: "src/components/RunnerScreen.jsx"
      to: "src/components/feedback/FeedbackOverlay.jsx"
      via: "feedback prop drives overlay render"
      pattern: "FeedbackOverlay"
    - from: "src/components/feedback/FeedbackOverlay.jsx"
      to: "src/components/feedback/MickeyConfetti.jsx"
      via: "Correct verdict renders confetti"
      pattern: "MickeyConfetti"
---

<objective>
Replace the current minimal feedback overlay with a richer, more celebratory experience. Show the verdict result, why it was correct/incorrect, and a partial tag-match breakdown. Correct verdicts trigger an outlined Mickey Mouse head confetti burst; wrong verdicts trigger a distinct shake + red glow.

Purpose: Make the moment of submission feel polished and informative, reinforcing learning without revealing full answer keys.

Output: New FeedbackOverlay and MickeyConfetti components wired into RunnerScreen.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@src/components/RunnerScreen.jsx
@src/data/taxonomy.json
@src/data/answerKeys.json
@src/utils/scoring.js
@tests/components/RunnerScreen.test.jsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build MickeyConfetti component</name>
  <files>src/components/feedback/MickeyConfetti.jsx, tests/components/feedback/MickeyConfetti.test.jsx</files>
  <behavior>
    - No new library. Custom canvas-based particle system.
    - Particles are outlined Mickey Mouse head shapes (three circles).
    - Burst originates near center of overlay.
    - Particles fall with gravity, fade out, and are cleaned up.
    - Respects prefers-reduced-motion.
  </behavior>
  <action>
    Create `src/components/feedback/MickeyConfetti.jsx`:
    - Props: `active` (boolean), `particleCount` (default 80), `duration` (default 2500ms).
    - Use a canvas ref covering the overlay area.
    - On `active` true, spawn particles with random velocity/rotation.
    - Each particle draws an outlined Mickey head (two small upper circles for ears, one larger lower circle for face) using `strokeStyle` and no fill.
    - Use `requestAnimationFrame` loop for physics + render.
    - Stop loop and clear canvas after all particles fade.
    - Read `window.matchMedia('(prefers-reduced-motion: reduce)')`; if true, render a static burst or skip animation.

    Create `tests/components/feedback/MickeyConfetti.test.jsx`:
    - Test canvas is rendered when active.
    - Test reduced-motion media query skips animation.
    - Test cleanup happens after duration.
  </action>
  <verify>
    <automated>npx vitest run tests/components/feedback/MickeyConfetti.test.jsx</automated>
  </verify>
  <done>
    - MickeyConfetti renders canvas burst.
    - Reduced motion handled.
    - Tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build FeedbackOverlay component</name>
  <files>src/components/feedback/FeedbackOverlay.jsx, tests/components/feedback/FeedbackOverlay.test.jsx</files>
  <behavior>
    - Accept feedback data: isCorrect, verdict, matchedL1Label, matchedL2Labels, scoreBreakdown.
    - Render large icon (check or X), headline, reason sentence, partial tag match list.
    - Render Continue button and countdown hint.
    - Use Framer Motion for enter/exit and correct/wrong-specific animations.
  </behavior>
  <action>
    Create `src/components/feedback/FeedbackOverlay.jsx`:
    - Props:
      - `isCorrect` (bool)
      - `verdict` ('approve'|'decline')
      - `matchedCategory` (string, e.g., "Copyright & IP")
      - `matchedSubcategories` (array of strings)
      - `breakdown` (object: verdict, l1, l2 scores)
      - `onContinue` (function)
      - `autoAdvanceMs` (number, default 2500)
    - Layout: full-screen backdrop blur, large centered card.
    - Headline: "Correct — great eye" or "Missed this one".
    - Reason line: "Correct: this clip contained Copyright & IP" or "Wrong: the correct verdict was Decline".
    - Partial match section:
      - Verdict: ✓ or ✗
      - L1 match: list matched categories
      - L2 match: list matched subcategories (or "No L2 match")
    - Continue button.
    - Correct animation: scale + spring + MickeyConfetti.
    - Wrong animation: shake keyframes + red glow.

    Create `src/styles/feedback.css`:
    - `.cma-feedback`, `.cma-feedback--correct`, `.cma-feedback--wrong`, shake animation.

    Create tests:
    - Renders correct headline when isCorrect true.
    - Renders wrong headline when false.
    - Renders reason text with category name.
    - Calls onContinue when button clicked.
    - Renders MickeyConfetti only on correct.
  </action>
  <verify>
    <automated>npx vitest run tests/components/feedback/FeedbackOverlay.test.jsx</automated>
  </verify>
  <done>
    - FeedbackOverlay renders verdict, reason, partial match.
    - Correct/wrong animations distinct.
    - Tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate FeedbackOverlay into RunnerScreen</name>
  <files>src/components/RunnerScreen.jsx, tests/components/RunnerScreen.test.jsx</files>
  <behavior>
    - Replace inline feedback markup with `<FeedbackOverlay />`.
    - Compute `matchedCategory` and `matchedSubcategories` from answer key + user selection.
    - Keep existing auto-advance timing and keyboard handlers.
  </behavior>
  <action>
    In `src/components/RunnerScreen.jsx`:
    - Import `FeedbackOverlay` and `MickeyConfetti`.
    - Change `setFeedback(isCorrect ? 'correct' : 'incorrect')` to store richer state:
      ```js
      setFeedback({
        isCorrect,
        verdict,
        matchedCategory: getMatchedCategoryLabel(currentVideo.id, userL1, answerKeys),
        matchedSubcategories: getMatchedSubcategoryLabels(...),
        breakdown: computePartialBreakdown(...)
      })
      ```
    - Replace the inline feedback AnimatePresence block with `<FeedbackOverlay {...feedback} onContinue={handleContinue} />`.
    - Keep `overlayRef` focus behavior.
    - Update keyboard handler to call `handleContinue`.

    Add helpers (can live in `src/components/feedback/feedbackHelpers.js`):
    - `getMatchedCategoryLabel(videoId, userL1, answerKeys, taxonomy)`
    - `getMatchedSubcategoryLabels(videoId, userL2, answerKeys, taxonomy)`
    - `computePartialBreakdown(videoId, userL1, userL2, verdict, answerKeys)`

    Update `tests/components/RunnerScreen.test.jsx`:
    - Test correct verdict shows FeedbackOverlay with confetti.
    - Test wrong verdict shows FeedbackOverlay without confetti.
    - Test partial match breakdown appears.
  </action>
  <verify>
    <automated>npx vitest run tests/components/RunnerScreen.test.jsx</automated>
  </verify>
  <done>
    - RunnerScreen uses FeedbackOverlay.
    - Correct/wrong feedback renders correctly.
    - Tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM | Feedback overlay is client-side only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11-01 | Information Disclosure | Partial match breakdown | accept | Deliberately shows only matched categories, not full answer keys. |
| T-06-11-02 | Denial of Service | Canvas confetti | mitigate | Limited particle count; stops after duration; reduced motion disables it. |
</threat_model>

<verification>
- `npx vitest run tests/components/feedback/MickeyConfetti.test.jsx`
- `npx vitest run tests/components/feedback/FeedbackOverlay.test.jsx`
- `npx vitest run tests/components/RunnerScreen.test.jsx`
- `npx vitest run` — full suite green
- Manual: `npm run dev`, submit correct and wrong verdicts, verify confetti, animations, partial match
</verification>

<success_criteria>
- [ ] MickeyConfetti component draws outlined Mickey heads on canvas
- [ ] FeedbackOverlay shows verdict, reason, and partial tag match
- [ ] Correct verdict triggers confetti + spring animation
- [ ] Wrong verdict triggers shake + red glow
- [ ] Reduced motion disables confetti/animation
- [ ] RunnerScreen integrates new overlay
- [ ] All tests pass
</success_criteria>

<output>
Create `.planning/phases/06-polish-content-freeze-launch-gates/06-11-SUMMARY.md` when done
</output>
