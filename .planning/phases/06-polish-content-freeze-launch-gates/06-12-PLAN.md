---
phase: 06-polish-content-freeze-launch-gates
plan: 12
type: execute
wave: 2
depends_on: []
files_modified:
  - src/assets/icons/l1/index.js
  - src/assets/icons/l1/*.jsx
  - tests/assets/l1-icons.test.js
autonomous: true
requirements:
  - GUIDE-01
  - GUIDE-02
  - TAG-07
must_haves:
  truths:
    - "10 custom SVG icon components exist, one per L1 category"
    - "Each icon uses a consistent 24x24 viewBox and currentColor styling"
    - "Icon registry maps taxonomy iconKey to component"
    - "Icons render without errors in GuidelinesScreen, L1Chip, and FeedbackOverlay"
    - "Unknown iconKey falls back to a safe default icon"
  artifacts:
    - path: "src/assets/icons/l1/index.js"
      provides: "Icon registry with getL1Icon helper"
      exports:
        - "getL1Icon"
        - "CopyrightIcon"
        - "HateIcon"
        - "ViolenceIcon"
        - "SexualIcon"
        - "MinorIcon"
        - "RegulatedIcon"
        - "MisinfoIcon"
        - "SpamIcon"
        - "BrandSafetyIcon"
        - "CommunityIcon"
  key_links:
    - from: "src/assets/icons/l1/index.js"
      to: "src/data/taxonomy.json"
      via: "iconKey values match taxonomy categories"
      pattern: "iconKey"
---

<objective>
Create a complete set of 10 custom SVG icons for the L1 taxonomy categories. The icons must look premium, consistent, and integrate with the existing candy-glass color palette. Build an icon registry so other components can resolve the right icon by `iconKey`.

Purpose: Reduce text density and add visual polish across Guidelines, Runner, and Feedback screens.

Output: 10 SVG icon components + registry + tests.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@src/data/taxonomy.json
@src/components/GuidelinesScreen.jsx
@src/components/tagging/L1Chip.jsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Design and implement 10 L1 icons</name>
  <files>src/assets/icons/l1/*.jsx</files>
  <behavior>
    - Each icon is a self-contained React component returning an inline SVG.
    - Consistent visual language: 2px stroke, rounded line caps/joins, 24x24 viewBox, currentColor.
    - Icons should be metaphorical and immediately readable.
  </behavior>
  <action>
    Create directory `src/assets/icons/l1/` if not exists.

    Create these icon components (one file each):
    - `CopyrightIcon.jsx` — document/page with © symbol
    - `HateIcon.jsx` — speech bubble with a slash or warning mark
    - `ViolenceIcon.jsx` — fist or broken shield
    - `SexualIcon.jsx` — 18+/shield with heart or body silhouette
    - `MinorIcon.jsx` — child/person silhouette with shield
    - `RegulatedIcon.jsx` — scale or gavel
    - `MisinfoIcon.jsx` — magnifying glass over fake document or warning triangle
    - `SpamIcon.jsx` — robot head or repetitive envelopes
    - `BrandSafetyIcon.jsx` — ad billboard with shield
    - `CommunityIcon.jsx` — group of people or house with check

    Each file exports a default component accepting props:
    - `className`, `style`, `aria-hidden`, `aria-label`.
    - Uses `currentColor` for strokes/fills.

    Keep each file under 100 lines.
  </action>
  <verify>
    <automated>npx vitest run tests/assets/l1-icons.test.js</automated>
  </verify>
  <done>
    - 10 icon files exist and are valid JSX.
    - Icons use consistent sizing and currentColor.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build icon registry and fallback</name>
  <files>src/assets/icons/l1/index.js, tests/assets/l1-icons.test.js</files>
  <behavior>
    - Central registry maps `iconKey` to component.
    - Unknown key returns a fallback circle icon.
    - Supports both named imports and `getL1Icon` lookup.
  </behavior>
  <action>
    Create `src/assets/icons/l1/index.js`:
    - Import all 10 icon components.
    - Import/create `FallbackIcon` (simple circle/question mark).
    - Export `const ICONS = { copyright: CopyrightIcon, hate: HateIcon, ... }`.
    - Export `getL1Icon(iconKey)` that returns `ICONS[iconKey] || FallbackIcon`.
    - Export each icon component individually for direct import.

    Create `tests/assets/l1-icons.test.js`:
    - Test `getL1Icon` returns correct component for every taxonomy `iconKey`.
    - Test unknown key returns fallback.
    - Test each icon renders an `<svg>` element.
    - Test all 10 taxonomy categories have a matching iconKey.
  </action>
  <verify>
    <automated>npx vitest run tests/assets/l1-icons.test.js</automated>
  </verify>
  <done>
    - Registry maps all iconKeys.
    - Fallback works.
    - Tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static assets | Icons are SVG markup only; no runtime trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12-01 | Information Disclosure | Icon semantics | accept | Icons are abstract representations of public taxonomy categories. |
| T-06-12-02 | Tampering | SVG files | mitigate | No external URLs or scripts in SVGs; inline only. |
</threat_model>

<verification>
- `npx vitest run tests/assets/l1-icons.test.js`
- `npx vitest run` — full suite green
- Manual: `npm run dev`, verify icons appear in GuidelinesScreen and L1Chip
</verification>

<success_criteria>
- [ ] 10 custom SVG icon components created
- [ ] Consistent 24x24 viewBox, currentColor, premium outlined style
- [ ] Icon registry maps taxonomy iconKey to component
- [ ] Fallback icon for unknown keys
- [ ] Tests verify all icons render and map correctly
- [ ] Full suite green
</success_criteria>

<output>
Create `.planning/phases/06-polish-content-freeze-launch-gates/06-12-SUMMARY.md` when done
</output>
