---
phase: 07-launch-monitor
plan: 01
type: execute
wave: 1
depends_on:
  - 06-11
files_modified:
  - scripts/apps-script/Code.gs
  - scripts/apps-script/README.md
  - tests/scripts/apps-script.test.js
autonomous: true
requirements:
  - SUBMIT-01
  - SUBMIT-02
  - SUBMIT-03
  - SUBMIT-09
must_haves:
  truths:
    - "Apps Script doPost receives submissions from the deployed Pages site"
    - "Browser CORS preflight is answered by doOptions"
    - "Origin header is read from e.headers, not e.postData.headers"
    - "HMAC validation strips hmac field robustly regardless of JSON position"
    - "Submission writes a row to the configured Google Sheet"
  artifacts:
    - path: "scripts/apps-script/Code.gs"
      provides: "Production-ready doPost/doOptions webhook"
      contains: "doPost, doOptions"
    - path: "scripts/apps-script/README.md"
      provides: "Deployment and troubleshooting guide"
      contains: "CORS, ALLOWED_ORIGIN, HMAC_SECRET, SHEET_ID"
  key_links:
    - from: "scripts/apps-script/Code.gs"
      to: "src/utils/submission.js"
      via: "Client POSTs HMAC-signed JSON to Apps Script URL"
      pattern: "fetch POST"
---

<objective>
Fix the production Google Apps Script submission pipeline so candidate data is actually captured. The immediate blocker was CORS preflight failure, incorrect Origin header access, and fragile HMAC stripping. After the fix, validate the end-to-end flow from deployed Pages site to Google Sheet row.

Purpose: Ensure the assessment can be used in a real hiring cycle; submissions must land reliably in the recruiter Sheet.

Output: Updated Code.gs, README, tests, and a validated production deployment.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@scripts/apps-script/Code.gs
@scripts/apps-script/README.md
@scripts/apps-script/appsscript.json
@src/utils/submission.js
@src/components/scoreboard/ScoreboardScreen.jsx
@tests/scripts/apps-script.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Apps Script CORS and origin handling</name>
  <files>scripts/apps-script/Code.gs, tests/scripts/apps-script.test.js</files>
  <behavior>
    - Browsers POSTing cross-origin to Apps Script send a preflight OPTIONS request.
    - Apps Script web apps expose request headers on e.headers (lowercased), not e.postData.headers.
    - HMAC strip regex must handle hmac field in any object position.
  </behavior>
  <action>
    In scripts/apps-script/Code.gs:
    - Add `doOptions(e)` handler returning JSON.
    - Replace origin lookup with helper that checks `e.headers['origin']`, `e.headers['Origin']`, then legacy fallbacks.
    - Add helper functions `jsonResponse`, `getRequestOrigin`, `getClientIp`, `computeHexHmac`, `computeHexSha256`, `loadProps`.
    - Improve HMAC strip regex to handle both leading and trailing comma.
    - Keep all existing validation and Sheet-writing logic.

    Update tests/scripts/apps-script.test.js:
    - Add 'function doOptions' to REQUIRED_PATTERNS.
    - Add test that code reads Origin from e.headers.
    - Add test that doOptions handler exists.
  </action>
  <verify>
    <automated>npx vitest run tests/scripts/apps-script.test.js</automated>
  </verify>
  <done>
    - Code.gs passes all apps-script tests.
    - doOptions, e.headers origin lookup, and robust HMAC strip are present.
  </done>
</task>

<task type="manual">
  <name>Task 2: Redeploy Apps Script and run end-to-end submission test</name>
  <files>N/A</files>
  <behavior>
    - Code changes only take effect after a new Apps Script deployment.
    - The deployment URL must match VITE_APPS_SCRIPT_URL in the client .env.
  </behavior>
  <action>
    1. Open script.google.com, paste updated Code.gs.
    2. Deploy → New Deployment → Web App (Execute as: Me, Access: Anyone).
    3. Copy new Deployment URL into .env VITE_APPS_SCRIPT_URL.
    4. Run `npm run build` and deploy to GitHub Pages (or use `npm run dev` with the production URL for local smoke test).
    5. Complete the assessment once in a browser.
    6. Check the Google Sheet for a new row with email hash, name, answers JSON, scores JSON, etc.
  </action>
  <verify>
    <manual>Google Sheet shows one new row within 10 seconds of scoreboard success overlay</manual>
  </verify>
  <done>
    - Real submission lands in Sheet.
    - Deployment URL and .env are in sync.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document troubleshooting in README</name>
  <files>scripts/apps-script/README.md</files>
  <behavior>
    - Future deployers need a checklist for "client says success but no row appears".
  </behavior>
  <action>
    Update scripts/apps-script/README.md:
    - Add CORS note near endpoint format.
    - Add Troubleshooting section covering ALLOWED_ORIGIN mismatch, HMAC_SECRET mismatch, SHEET_ID, redeploy requirement, and browser CORS errors.
  </action>
  <verify>
    <automated>npx vitest run tests/scripts/apps-script.test.js (README tests)</automated>
  </verify>
  <done>
    - README has CORS note and troubleshooting checklist.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Apps Script | Cross-origin POST with HMAC signature. |
| Apps Script → Google Sheet | Server-side write with authenticated Google identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | HMAC secret in client bundle | accept | Same model as bundled answerKeys (SCORE-08). Secret also required server-side. |
| T-07-01-02 | Denial of Service | CORS preflight blocked | mitigate | doOptions handler added; deployed and tested. |
| T-07-01-03 | Spoofing | Origin header spoofable | accept | Origin is a weak check; HMAC + email dedup provide real defense. |
</threat_model>

<verification>
- `npx vitest run tests/scripts/apps-script.test.js`
- `npm test` — full suite green
- Manual: deployed site end-to-end submission → Sheet row visible
</verification>

<success_criteria>
- [ ] doOptions handler answers CORS preflight
- [ ] Origin header read from e.headers
- [ ] HMAC strip regex handles any field position
- [ ] All apps-script tests pass
- [ ] Real submission from deployed site appears in Google Sheet
- [ ] README troubleshooting section added
</success_criteria>

<output>
Create `.planning/phases/07-launch-monitor/07-01-SUMMARY.md` when done
</output>
